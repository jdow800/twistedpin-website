# Script 21 - September 25 execution record

**Status: applied and verified September 25, 2026.** The live run began `2026-09-25T14:47:03.222Z` (9:47am CT; account timezone America/New_York), and Google accepted all 9 operations individually at 9:47:12am CT.

The fresh read-only Preview at `2026-09-25T14:57:03.235Z` planned **0 operations**. It read back:
- both fundraiser keywords routed to `/fundraisers/`
- the three Brand sitelink links (Brand now has **7** enabled campaign-level sitelinks)
- the exact suffix on all four campaigns
- Bar-Led **PAUSED**
- budgets and Manual CPC unchanged, with Search Partners and Display still off

API acceptance and readback do not establish serving or policy review.

## Read-only Preview (`2026-09-25T14:42:55.875Z`)

Jon's Preview passed all checks and planned exactly 9 operations, with no writes. It also recorded this account state, which is worth keeping:

- **Search Partners and Display were already off on all four campaigns.** `networkHygiene` had nothing to change. This closes the "never verified on Events/Open Play" item from the August 29 refresh.
- **AI Max auto-upgrade triggers were all clear:**
  - AI Max is off (`enableAiMax: false` on Events, unset elsewhere).
  - Text asset automation is `OPTED_OUT` on all four campaigns, and Events is also opted out of final-URL-expansion text automation.
  - The campaign-level match type is `UNSPECIFIED` on all four.
- **Auto-tagging is ON.** There was no final URL suffix anywhere, and no lower-level URL options.
- **Legacy account-level tracking template:** `{lpurl}?gr_source=google&gr_campaign={campaignid}&gr_term={adgroupid}&gr_gadid={creative}`. It is not documented in any repository; it probably dates from before the current site. Under parallel tracking, parameters in a tracking template do not reach the landing page, which is why the tags use the final URL suffix. Left unchanged; clearing it is optional.
- Brand had 4 enabled campaign-level sitelinks and 0 ad-group-level sitelinks.
- All eight landing checks returned 200 with the expected content, including the UTM-tagged URLs.

## Accepted operations (live run)

| # | Resource | Change | Before |
|---|---|---|---|
| 1 | `adGroupCriteria/195744623684~7628967977` | `[fundraiser venue]` final URL → `/fundraisers/` | `/birthday-parties/` |
| 2 | `adGroupCriteria/195744623684~400925980077` | `[bowling fundraiser]` final URL → `/fundraisers/` | `/birthday-parties/` |
| 3 | `campaignAssets/23856370451~421097763806~SITELINK` | Brand ← Adult Birthday Parties | not linked |
| 4 | `campaignAssets/23856370451~421280548245~SITELINK` | Brand ← Kids' Birthday Packages | not linked |
| 5 | `campaignAssets/23856370451~421280548254~SITELINK` | Brand ← Company Holiday Parties | not linked |
| 6 | `campaigns/23856370451` (Search_Brand_TwistedPin_2026) | final URL suffix, `utm_campaign=brand` | none |
| 7 | `campaigns/23851071726` (Bar-Led_TwistedPin_2026) | final URL suffix, `utm_campaign=bar`; **status PAUSED** | none; ENABLED |
| 8 | `campaigns/23646227882` (General & Open Play 2026) | final URL suffix, `utm_campaign=open-play` | none |
| 9 | `campaigns/23640563901` (Events Campaign 2026 Setup) | final URL suffix, `utm_campaign=events` | none |

All resource names begin `customers/5778974265/`. Every suffix is `utm_source=google&utm_medium=cpc&utm_campaign=<slug>&utm_content={adgroupid}_{loc_physical_ms}&utm_term={keyword}`.

The following were unchanged:
- **Budgets:** Brand $6, Bar $6 (now paused), Open Play $32, Events $35.
- **Other settings:** Manual CPC, geo, bids, ads, negatives, conversion goals and asset content.
- **Events links:** the Events ad-group links to the same three sitelink assets.

## Next steps and measurement

1. **Fresh Preview: done, 9:57am CT.** It showed `planned operations: 0`, with `ALREADY ROUTED` ×2, `ALREADY LINKED` ×3, `SUFFIX ALREADY SET` ×4 and `ALREADY PAUSED`. Do not rerun live; no further script run is needed.
2. **Tag arrival:** the next Google-attributed inquiry in `avery_event` should carry `utm_campaign` and `utm_content`. Count Google inquiries by `gclid IS NOT NULL`, not by the `attribution_source` string, which WF1 now extends with the campaign slug.
3. **Measurement window:** the first fourteen complete account days after this run are **September 26–October 9**, reviewed **October 10 or later**. That's separate from the Script 19 (September 14–27), Script 20 URL (September 15–28) and Script 20 replacement-ad (September 16–29) windows. With Bar-Led paused, expect account spend to fall by about its daily budget.

## Rollback

Use the before values above.
- **Fundraiser URLs:** restore the two keyword URLs to `/birthday-parties/`.
- **Brand sitelinks:** remove only the three Brand campaign links (operations 3–5), never the shared assets.
- **Suffixes:** clear the four campaign final URL suffixes under campaign Settings → Campaign URL options.
- **Bar-Led:** set the campaign back to Enabled.

See the [Script 21 runbook](21_review_fixes.md).
