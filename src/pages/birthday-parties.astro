---
/**
 * /birthday-parties/ — Birthday parties pillar page.
 *
 * Slug flipped to the clean /birthday-parties/ canonical 2026-06-17
 * (on-page SEO spec Phase B). The legacy /birthday-parties-booking/ slug
 * now 301s here (vercel.json) — fixes the backwards redirect where the
 * clean keyword URL was redirecting to the cruft slug. The Birthday ad
 * group's final URL is repointed to /birthday-parties/ in the Ads cleanup.
 *
 * Architecture follows the established pillar pattern (.bar / .eat /
 * .vip-suite): typography hero → editorial sections (image one side,
 * copy the other, alternating) → packages card-grid → closing CTA band
 * → SnapFooter.
 *
 * Kids-led content with an adult-birthdays handoff section at the end:
 *   - Kids section explains the "we don't try to do five different
 *     activities — we do this well" angle the user articulated
 *     2026-05-11 (no trampolines / go-karts / indoor playground; just
 *     bowling + arcade + a real kitchen + the parent bar program).
 *   - Two kid packages (Suite $419 / Extra Suite $469.90) booked via
 *     Roller. Prices confirmed accurate 2026-05-11.
 *   - Adult birthdays funnel to the events team (Zite) since they need
 *     custom catering and the VIP suite — distinct from the kid-package
 *     online booking flow.
 *
 * Voice notes (per 2026-05-11 reframe in voice.md):
 *   - Don't dwell on limited slot availability (1 Sunday, 2 Saturdays,
 *     earlier-day only). Position quality + "the times kids' parties
 *     actually happen" without leaning on scarcity in body copy.
 *   - "Phenomenal" is the user's internal word — use it once, not as a
 *     refrain.
 *   - Lean into kid-validation angle: "the party your kid asks for
 *     again" — matches the two-mode positioning where the adult is
 *     always the chooser but the kid pulls them back.
 *
 * Rule-of-four triggered (.bar / .eat / .vip-* / .birthday-*) — pillar
 * CSS hoist into global.css .pillar-* utilities is overdue. Tracked in
 * Context/media-needs.md.
 *
 * Photography (2026-05-11): real birthday photo (DSC00781 → birthday-
 * table-*) lands here as the section 1 image. Adult section uses the
 * existing events-bg-* stub (same as homepage) — flag in media-needs
 * for a distinct adult-birthday/VIP suite shot when shoot happens.
 */
import Base from "../layouts/Base.astro";
import SnapFooter from "../components/snap/SnapFooter.astro";
import BreadcrumbSchema from "../components/BreadcrumbSchema.astro";
import {
  BUSINESS_URL,
  BUSINESS_ENTITY_ID,
  GOOGLE_RATING,
  GOOGLE_REVIEW_COUNT,
} from "../lib/schema";
import { ICON_PATHS } from "../lib/icons";

import { PLAN_EVENT_URL } from "../lib/links";

// Roller→TPRS cutover Phase 2 (2026-06-15): kids-birthday booking moved to the
// live /reserve/birthdays TPRS flow. Absolute URL kept so the JSON-LD Offer.url
// nodes stay valid; const name retained to avoid touching all 5 usages.
const ROLLER_KIDS_URL = "https://www.twistedpin.com/reserve/birthdays/";

// JSON-LD: two Product nodes (one per package) wrapped in an ItemList.
// Product + Offer is the right shape for SKU-priced packages — eligible
// for product rich snippets (price + availability in SERP). brand →
// BUSINESS_ENTITY_ID ties each package back to the venue entity graph.
// Source of truth is the live TPRS booking catalog at /reserve/birthdays/ --
// verify against it, not against this page. Extra Suite was stale at $489.90
// here (catalog said $469.90) until 2026-07-18; the display below now reads
// EXTRA_SUITE_BIRTHDAY_PRICE so the card and the schema Offer cannot diverge
// again. SUITE_BIRTHDAY_PRICE stays hardcoded in the card ($419 vs "419.00").
const PAGE_URL = `${BUSINESS_URL}/birthday-parties/`;
const SUITE_BIRTHDAY_PRICE = "419.00";
const EXTRA_SUITE_BIRTHDAY_PRICE = "469.90";
// Offer.validFrom — Google's Merchant-listing validator wants it on every
// Offer (same warning class GSC raised on /summer-pin-pass, 2026-07-12).
// The packages are always-on, so the honest anchor is the day this page
// (and online booking for these packages) launched: 2026-05-11 (0e3d173).
// No validThrough on purpose — these offers don't expire, and a fake end
// date would just become a stale-schema landmine (see schema-dates.ts).
const BIRTHDAY_OFFERS_VALID_FROM = "2026-05-11";
// Venue-wide Google rating, surfaced on both party Products so SERP
// results for /birthday-parties can show stars. Clears GSC
// "Missing field 'aggregateRating'" non-critical warning.
const partyAggregateRating = {
  "@type": "AggregateRating",
  ratingValue: GOOGLE_RATING,
  reviewCount: GOOGLE_REVIEW_COUNT,
  bestRating: 5,
  worstRating: 1,
};
const partyPackagesSchema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  itemListElement: [
    {
      "@type": "ListItem",
      position: 1,
      item: {
        "@type": "Product",
        "@id": `${BUSINESS_URL}/#party-suite-birthday`,
        name: "Suite Birthday Party",
        description:
          "Kids' birthday party package at Twisted Pin: 1 hour of bowling on a reserved lane, 2 hours total party time, shoes and balls, 45-min reserved party space, $10 arcade game card per child, souvenir lunch bag, invitations, and a personal party coordinator. Up to 10 guests.",
        brand: { "@id": BUSINESS_ENTITY_ID },
        url: PAGE_URL,
        aggregateRating: partyAggregateRating,
        offers: {
          "@type": "Offer",
          price: SUITE_BIRTHDAY_PRICE,
          priceCurrency: "USD",
          availability: "https://schema.org/InStock",
          validFrom: BIRTHDAY_OFFERS_VALID_FROM,
          url: ROLLER_KIDS_URL,
          seller: { "@id": BUSINESS_ENTITY_ID },
        },
      },
    },
    {
      "@type": "ListItem",
      position: 2,
      item: {
        "@type": "Product",
        "@id": `${BUSINESS_URL}/#party-extra-suite-birthday`,
        name: "Extra Suite Birthday Party",
        description:
          "Extended kids' birthday party package: 1 hour of bowling on a reserved lane, 2.5 hours total party time, shoes and balls, 1 hour reserved party space, $10 arcade game card per child, souvenir lunch bag, invitations, and a personal party coordinator. Up to 10 guests.",
        brand: { "@id": BUSINESS_ENTITY_ID },
        url: PAGE_URL,
        aggregateRating: partyAggregateRating,
        offers: {
          "@type": "Offer",
          price: EXTRA_SUITE_BIRTHDAY_PRICE,
          priceCurrency: "USD",
          availability: "https://schema.org/InStock",
          validFrom: BIRTHDAY_OFFERS_VALID_FROM,
          url: ROLLER_KIDS_URL,
          seller: { "@id": BUSINESS_ENTITY_ID },
        },
      },
    },
  ],
};

// FAQPage + on-page FAQ (2026-06-17, on-page SEO spec Phase B). Reuses the
// /faq accordion pattern (global t2-faq-* classes) + the Question/Answer
// JSON-LD shape. Search-phrased for the birthday cluster — kids packages,
// capacity, cake/decor, milestone (30/40/50), bachelorette, booking, and
// location. Voice-clean: semi-private, "curated by" Brian, no chef/discount.
interface BdayFaq {
  q: string;
  /** Plain-text answer for JSON-LD (no HTML). */
  schemaA: string;
  /** Rendered HTML answer (may include <a> links). */
  htmlA: string;
}

const birthdayFaqs: BdayFaq[] = [
  {
    q: "Can I host a kids' birthday party at Twisted Pin in Plainfield?",
    schemaA:
      "Yes — kids' birthday parties run on a reserved lane with arcade time, a souvenir bag, invitations, and a personal party coordinator. Two packages: Suite Birthday ($419) and Extra Suite Birthday ($469.90), each up to 10 kids.",
    htmlA: `<p>Yes — kids' birthday parties run on a reserved lane with arcade time, a souvenir bag, invitations, and a personal party coordinator. Two packages — <strong>Suite Birthday ($419)</strong> and <strong>Extra Suite Birthday ($469.90)</strong> — each up to 10 kids.</p><p><a href="${ROLLER_KIDS_URL}" target="_blank" rel="noopener">Book a kids' party →</a></p>`,
  },
  {
    q: "How many kids can come to the party?",
    schemaA:
      "Each package covers up to 10 kids. Add more guests for $30–$40 per child depending on the package, up to a 14-guest max.",
    htmlA: `<p>Each package covers up to <strong>10 kids</strong>. Add more guests for $30–$40 per child depending on the package, up to a <strong>14-guest max</strong>.</p>`,
  },
  {
    q: "Can I bring my own cake, cupcakes, or decorations?",
    schemaA:
      "Outside cake and decor are welcome for booked, catered parties. Confirm the specifics with your party coordinator when you book.",
    htmlA: `<p>Outside cake and decor are welcome for booked, catered parties. Confirm the specifics with your party coordinator when you book.</p>`,
  },
  {
    q: "Do you host adult or milestone birthdays — 30th, 40th, 50th?",
    schemaA:
      "Yes. Adult and milestone birthdays run in the 6-lane VIP suite, set apart from the main floor — in-house catering, the LED video wall, and the cocktail program curated by Brian Van Flandern, America's Top Mixologist. The events team builds it with you.",
    htmlA: `<p>Yes. Adult and milestone birthdays run in the <a href="/vip-suite/">6-lane VIP suite</a>, set apart from the main floor — in-house catering, the LED video wall, and the cocktail program curated by Brian Van Flandern, <em>America's Top Mixologist</em>. The events team builds it with you.</p><p><a href="${PLAN_EVENT_URL}" target="_blank" rel="noopener">Plan an event →</a></p>`,
  },
  {
    q: "Can I have a bachelorette or bachelor party here?",
    schemaA:
      "Absolutely. Take over the semi-private VIP suite for the group — your own lanes, the 28-tap self-serve beer and wine wall, and craft cocktails. Run a tab, hand out drink tickets, or set a per-head cap; the events team sets it up.",
    htmlA: `<p>Absolutely. Take over the <strong>semi-private VIP suite</strong> for the group — your own lanes, the 28-tap self-serve beer &amp; wine wall, and craft cocktails. Run a tab, hand out drink tickets, or set a per-head cap; the events team sets it up.</p><p><a href="${PLAN_EVENT_URL}" target="_blank" rel="noopener">Plan an event →</a></p>`,
  },
  {
    q: "How do I book a birthday party?",
    schemaA:
      "Kids' parties book online and pick a time slot directly. Adult, milestone, and bachelorette parties go through the events team so the suite, catering, and bar are set up for your group.",
    htmlA: `<p>Kids' parties <a href="${ROLLER_KIDS_URL}" target="_blank" rel="noopener">book online</a> and pick a time slot directly. Adult, milestone, and bachelorette parties go through the <a href="${PLAN_EVENT_URL}" target="_blank" rel="noopener">events team</a> so the suite, catering, and bar are set up for your group.</p>`,
  },
  {
    q: "Where are you located, and how far is it from Naperville?",
    schemaA:
      "We're in Plainfield at 15610 Joliet Rd — a short drive from Naperville, Joliet, Bolingbrook, Romeoville, Oswego, and Shorewood. Free on-site parking.",
    htmlA: `<p>We're in Plainfield at <strong>15610 Joliet Rd</strong> — a short drive from Naperville, Joliet, Bolingbrook, Romeoville, Oswego, and Shorewood. Free on-site parking.</p>`,
  },
];

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: birthdayFaqs.map((f) => ({
    "@type": "Question",
    name: f.q,
    acceptedAnswer: { "@type": "Answer", text: f.schemaA },
  })),
};
---
<Base
  title="Kids & Adult Birthday Parties in Plainfield, IL | Twisted Pin"
  description="Birthday parties at Twisted Pin in Plainfield, IL — kids' packages, milestone & adult birthdays, bachelorette nights. Bowling, arcade, a real kitchen, full bar."
  ogImage="/og/og-vip-suite.jpg"
  ogImageAlt="A Twisted Pin birthday party set up in the suite — table with Extra Suite Birthday placard, party cups, and reserved signage"
  lcpPreloadHref="/snap/vip-lanes-poster.avif"
  lcpPreloadMedia="(max-width: 1024px)"
>

  {/* ============================================================
      HERO — typography-led title section (no image)
      ============================================================ */}
  <section class="birthday-hero" data-hero aria-labelledby="birthday-hero-h1">
    <div class="birthday-hero-copy">
      <p class="t-eyebrow hero-fade" data-delay-ms="0">Birthday Parties</p>
      <h1 class="t-display birthday-hero-headline hero-fade" id="birthday-hero-h1" data-delay-ms="120">
        The birthday your&nbsp;kid asks&nbsp;for&nbsp;again.
      </h1>
      <p class="birthday-hero-sub hero-fade" data-delay-ms="320">
        Bowling lanes, the arcade, food they'll actually eat — and for the
        parents, a bar program that pulls its weight. The kind of party
        your kid talks about Monday.
      </p>
    </div>
  </section>

  {/* ============================================================
      KIDS — image LEFT, copy RIGHT (desktop)
      Photo: birthday-table (DSC00781 — the real table setup shot)
      ============================================================ */}
  <section class="birthday-section" aria-labelledby="birthday-kids-h2">
    <div class="birthday-section-grid">
      <div class="birthday-section-frame">
        {/* Kids Parties video — vip-lanes clip (matches the /vip-suite
            first section, since most kids' parties run in the VIP suite).
            LCP optimization (2026-05-17): direct poster= + preload via
            Base.astro lcpPreloadHref; bounded media queries on sources.
            Same recipe as /events first section. */}
        <video
          class="birthday-section-img"
          data-section-video
          preload="none"
          muted
          loop
          playsinline
          poster="/snap/vip-lanes-poster.avif"
          aria-label="The 6-lane VIP suite at Twisted Pin where kids' birthday parties run"
        >
          <source media="(max-width: 480px)" data-src="/snap/vip-lanes-mobile-av1-540.mp4"  type="video/mp4; codecs=av01.0.05M.08" />
          <source media="(max-width: 480px)" data-src="/snap/vip-lanes-mobile-h264-540.mp4" type="video/mp4" />
          <source media="(min-width: 481px)" data-src="/snap/vip-lanes-mobile-av1-1080.mp4"  type="video/mp4; codecs=av01.0.05M.08" />
          <source media="(min-width: 481px)" data-src="/snap/vip-lanes-mobile-h264-1080.mp4" type="video/mp4" />
        </video>
      </div>
      <div class="birthday-section-copy">
        <p class="t-eyebrow">Kids' Parties</p>
        <h2 class="t-display birthday-section-title" id="birthday-kids-h2">
          We don't try to do everything.
        </h2>
        <p class="birthday-section-aside">(we do this part really well)</p>

        <p class="birthday-section-body">
          Some places try to do five different kid activities at once —
          trampolines, go-karts, ninja courses, mini-golf, indoor playgrounds.
          We don't. We have bowling. We have an arcade. We have a real
          kitchen. That's the whole pitch. And we're genuinely good at it.
        </p>

        <p class="birthday-section-body">
          The lane is your group's. The arcade is yours. Food, cake,
          presents — we handle the room. A real coordinator walks you
          through it. And while the kids run the place, the parents get
          a craft cocktail program curated by Brian Van Flandern —
          <em>America's Top Mixologist</em>. Both sides win.
        </p>

        <ul class="birthday-section-list">
          <li>A reserved lane</li>
          <li>Arcade time built in</li>
          <li>Real kitchen, not a microwave</li>
          <li>A coordinator who runs the room</li>
          <li>Bar program for the parents</li>
          <li>Souvenir bags + invitations</li>
        </ul>
      </div>
    </div>
  </section>

  {/* ============================================================
      PACKAGES — centered card grid, no image
      ============================================================ */}
  <section class="birthday-packages" aria-labelledby="birthday-packages-h2">
    <div class="birthday-packages-inner">
      <p class="t-eyebrow t-eyebrow--center">Packages</p>
      <h2 class="t-display birthday-packages-title" id="birthday-packages-h2">
        Two packages.
      </h2>
      <p class="birthday-section-aside birthday-packages-aside">(pick the one that fits your group)</p>

      <div class="birthday-package-grid">
        <article class="birthday-package-card">
          <h3 class="birthday-package-name">Suite Birthday</h3>
          <p class="birthday-package-price">
            <span class="birthday-package-amount">$419</span>
            <span class="birthday-package-base">up to 10 guests</span>
          </p>
          <p class="birthday-package-meta">$30 per additional guest · 14 guest max</p>
          <ul class="birthday-package-list">
            <li>1 hour of bowling</li>
            <li>2 hours of party time total</li>
            <li>Shoes &amp; balls</li>
            <li>45-min reserved party space (food, cake, presents)</li>
            <li>$10 arcade game card per child</li>
            <li>Souvenir lunch bag</li>
            <li>Invitations (pickup at the venue)</li>
            <li>Food &amp; beverage choice</li>
            <li>Thank-you cards</li>
            <li>Personal party coordinator</li>
          </ul>
          <a
            class="btn btn-solid birthday-package-cta"
            href={ROLLER_KIDS_URL}
            target="_blank"
            rel="noopener"
          >Book Suite Birthday</a>
        </article>

        <article class="birthday-package-card birthday-package-card--featured">
          <p class="birthday-package-badge">More time, more space</p>
          <h3 class="birthday-package-name">Extra Suite Birthday</h3>
          <p class="birthday-package-price">
            <span class="birthday-package-amount">${EXTRA_SUITE_BIRTHDAY_PRICE}</span>
            <span class="birthday-package-base">up to 10 guests</span>
          </p>
          <p class="birthday-package-meta">$40 per additional guest · 14 guest max</p>
          <ul class="birthday-package-list">
            <li>1 hour of bowling</li>
            <li>2.5 hours of party time total</li>
            <li>Shoes &amp; balls</li>
            <li>1 hour reserved party space (food, drink, presents)</li>
            <li>$10 arcade game card per child</li>
            <li>Souvenir lunch bag</li>
            <li>Invitations (pickup at the venue)</li>
            <li>Food &amp; beverage choice</li>
            <li>Thank-you cards</li>
            <li>Personal party coordinator</li>
          </ul>
          <a
            class="btn btn-solid birthday-package-cta"
            href={ROLLER_KIDS_URL}
            target="_blank"
            rel="noopener"
          >Book Extra Suite Birthday</a>
        </article>
      </div>
    </div>
  </section>

  {/* ============================================================
      ADULT BIRTHDAYS — image RIGHT, copy LEFT (reverse)
      ============================================================ */}
  <section id="adults" class="birthday-section birthday-section--reverse" aria-labelledby="birthday-adult-h2">
    <div class="birthday-section-grid">
      <div class="birthday-section-frame">
        {/* Adult Birthdays video — buffet clip (matches the /events
            first section, since the adult birthday is operationally a
            group event with catering). Lazy-loaded via
            data-section-video. (Updated 2026-05-15 from a stub
            events-bg still photo to the buffet video.) */}
        <video
          class="birthday-section-img"
          data-section-video
          preload="none"
          muted
          loop
          playsinline
          data-poster="/snap/buffet-poster.avif"
          aria-label="An adult birthday catering buffet at Twisted Pin"
        >
          <source media="(max-width: 480px)" data-src="/snap/buffet-mobile-av1-540.mp4"  type="video/mp4; codecs=av01.0.05M.08" />
          <source media="(max-width: 480px)" data-src="/snap/buffet-mobile-h264-540.mp4" type="video/mp4" />
          <source media="(min-width: 481px)" data-src="/snap/buffet-mobile-av1-1080.mp4"  type="video/mp4; codecs=av01.0.05M.08" />
          <source media="(min-width: 481px)" data-src="/snap/buffet-mobile-h264-1080.mp4" type="video/mp4" />
        </video>
      </div>
      <div class="birthday-section-copy">
        <p class="t-eyebrow">Adult Birthdays</p>
        <h2 class="t-display birthday-section-title" id="birthday-adult-h2">
          Or &mdash; yours.
        </h2>
        <p class="birthday-section-aside">(grown-up edition)</p>

        <p class="birthday-section-body">
          Adult birthdays at Twisted Pin live in the VIP suite. Different
          gig from the kids' packages. Six lanes set apart from the main
          floor, the LED video wall, in-house catering through the kitchen,
          and the same craft cocktail program as the rest of the venue.
        </p>

        <p class="birthday-section-body">
          Custom drink ticket setups, open tab, or per-person —
          pick whatever billing model fits the group. The events team
          builds it with you.
        </p>

        <a
          class="btn btn-outline"
          href={PLAN_EVENT_URL}
          target="_blank"
          rel="noopener"
        >Plan an event</a>
      </div>
    </div>
  </section>

  {/* ============================================================
      OCCASIONS — adult birthday flavors. Text-led cards capturing the
      milestone / bachelorette / teen long-tail (per the on-page SEO
      spec). All route to the events team (custom suite + catering).
      ============================================================ */}
  <section class="birthday-occasions" aria-labelledby="birthday-occasions-h2">
    <div class="birthday-occasions-inner">
      <p class="t-eyebrow t-eyebrow--center">More than candles</p>
      <h2 class="t-display birthday-occasions-title" id="birthday-occasions-h2">
        Every kind of birthday.
      </h2>
      <ul class="birthday-occasions-grid">
        <li class="birthday-occasion-card">
          <p class="birthday-occasion-label">Milestone Birthdays</p>
          <h3 class="birthday-occasion-headline">30th, 40th, 50th — done right.</h3>
          <p class="birthday-occasion-body">
            The big ones deserve more than a back table. Take over the
            VIP suite — catering, the LED video wall, and craft
            cocktails. No banquet-hall energy.
          </p>
          <a class="birthday-occasion-link" href={PLAN_EVENT_URL} target="_blank" rel="noopener">Plan it →</a>
        </li>
        <li class="birthday-occasion-card">
          <p class="birthday-occasion-label">Bachelorette &amp; Bachelor</p>
          <h3 class="birthday-occasion-headline">The night before the big one.</h3>
          <p class="birthday-occasion-body">
            Your own lanes, the 28-tap self-serve wall, and cocktails
            curated by America's Top Mixologist. The kind of night the
            whole group actually remembers.
          </p>
          <a class="birthday-occasion-link" href={PLAN_EVENT_URL} target="_blank" rel="noopener">Plan it →</a>
        </li>
        <li class="birthday-occasion-card">
          <p class="birthday-occasion-label">Sweet 16 &amp; Teen</p>
          <h3 class="birthday-occasion-headline">Old enough to be picky.</h3>
          <p class="birthday-occasion-body">
            Reserved lanes, the arcade, and food they'll actually eat —
            a teen birthday that doesn't feel like a kids' party.
          </p>
          <a class="birthday-occasion-link" href={PLAN_EVENT_URL} target="_blank" rel="noopener">Plan it →</a>
        </li>
      </ul>
    </div>
  </section>

  {/* ============================================================
      CLOSING CTA BAND — split flow: kids → Roller, adult → Zite.
      Sits ABOVE the FAQ (per user 2026-06-26) so the booking buttons
      come right after the occasions/proof, and the FAQ closes out the
      page as a quiet reference beat before the footer.
      ============================================================ */}
  <section class="birthday-closing" aria-labelledby="birthday-closing-h2">
    <div class="birthday-closing-inner">
      <h2 class="t-display birthday-closing-title" id="birthday-closing-h2">
        Ready when you are.
      </h2>
      <div class="birthday-closing-ctas">
        <a class="btn btn-solid" href={ROLLER_KIDS_URL} target="_blank" rel="noopener">
          Book a kids' party
        </a>
        <a class="btn btn-outline" href={PLAN_EVENT_URL} target="_blank" rel="noopener">
          Plan an adult event
        </a>
      </div>
    </div>
  </section>

  {/* ============================================================
      FAQ — search-phrased birthday Q&As. Global .t2-faq* accordion;
      emits FAQPage JSON-LD (birthdayFaqs in frontmatter). Closes out
      the page below the booking CTA as a quiet reference beat.
      ============================================================ */}
  <section class="birthday-faq" aria-labelledby="birthday-faq-h2">
    <div class="birthday-faq-inner">
      <p class="t-eyebrow">Questions</p>
      <h2 class="t-display birthday-faq-title" id="birthday-faq-h2">
        Birthday party FAQs.
      </h2>
      <div class="t2-faq">
        {birthdayFaqs.map((item) => (
          <details class="t2-faq-item">
            <summary class="t2-faq-q">
              <h3 class="t2-faq-q-text">{item.q}</h3>
              <svg
                class="t2-faq-chevron"
                viewBox="0 0 24 24"
                width="18"
                height="18"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
                set:html={ICON_PATHS["chevron-down"]}
              />
            </summary>
            <div class="t2-faq-a" set:html={item.htmlA} />
          </details>
        ))}
      </div>
    </div>
  </section>

  <SnapFooter />

  <script type="application/ld+json" set:html={JSON.stringify(partyPackagesSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <BreadcrumbSchema crumbs={[
    { name: "Home", path: "/" },
    { name: "Birthday Parties", path: "/birthday-parties/" },
  ]} />
</Base>

<style>
  /* ============================================================
     /birthday-parties — same pattern as /bar, /eat,
     /vip-suite. Rule-of-four triggered; next refactor should hoist
     shared rules to .pillar-* utilities in global.css (see
     Context/media-needs.md tech debt).
     ============================================================ */

  /* ---------- hero (typography-led, no image) ---------- */
  .birthday-hero {
    position: relative;
    display: flex;
    align-items: center;
    min-height: clamp(320px, 42vh, 420px);
    padding: calc(var(--header-height) + 28px) 22px 24px;
  }
  @media (min-width: 1025px) {
    .birthday-hero {
      min-height: clamp(440px, 55vh, 640px);
      padding: calc(var(--header-height) + 64px) 64px 64px;
      justify-content: center;
    }
  }

  .birthday-hero-copy {
    max-width: 720px;
    color: var(--warm-white);
  }
  @media (min-width: 1025px) {
    .birthday-hero-copy {
      text-align: center;
    }
    .birthday-hero-sub {
      margin-left: auto;
      margin-right: auto;
    }
  }

  .birthday-hero-headline {
    color: var(--warm-white);
    font-size: clamp(28px, 8vw, 56px);
    letter-spacing: -0.005em;
    line-height: 0.95;
    margin-top: 12px;
  }
  @media (min-width: 1025px) {
    .birthday-hero-headline { font-size: clamp(48px, 5.2vw, 72px); }
  }

  .birthday-hero-sub {
    margin-top: 18px;
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.5;
    color: #ece6d6;
    max-width: 32em;
  }
  @media (min-width: 1025px) {
    .birthday-hero-sub { margin-top: 22px; font-size: 19px; max-width: 36em; }
  }

  /* ============================================================
     section pattern (mirrors .vip-section)
     ============================================================ */
  .birthday-section {
    background: transparent;
    padding: 5px 22px;
  }
  @media (min-width: 1025px) {
    .birthday-section { padding: 24px 64px; }
  }

  /* #adults deep-link target (e.g. /birthday-parties/#adults). Offset for
     the fixed header so the section title doesn't land hidden behind it —
     same values as the menu/leagues anchor pattern. */
  #adults { scroll-margin-top: 120px; }
  @media (min-width: 1025px) {
    #adults { scroll-margin-top: 140px; }
  }

  .birthday-section-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 32px;
    max-width: 1280px;
    margin: 0 auto;
    align-items: start;
  }
  @media (min-width: 1025px) {
    .birthday-section-grid {
      grid-template-columns: minmax(0, 1fr) minmax(0, 1.1fr);
      gap: 64px;
    }
    .birthday-section--reverse .birthday-section-grid > .birthday-section-frame {
      order: 2;
    }
  }

  .birthday-section-frame {
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-surface);
    aspect-ratio: 4 / 5;
  }
  @media (min-width: 1025px) {
    .birthday-section-frame { aspect-ratio: 3 / 4; max-height: 640px; }
  }

  .birthday-section-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .birthday-section-copy {
    color: var(--warm-white);
    max-width: 36em;
  }

  .birthday-section-title {
    color: var(--warm-white);
    font-size: clamp(28px, 7.5vw, 42px);
    line-height: 0.98;
    letter-spacing: -0.005em;
    margin-top: 10px;
  }
  @media (min-width: 1025px) {
    .birthday-section-title { font-size: clamp(40px, 4.2vw, 56px); }
  }

  .birthday-section-aside {
    margin-top: 6px;
    font-family: var(--font-body);
    font-size: 15px;
    color: var(--warm-dim);
  }
  @media (min-width: 1025px) {
    .birthday-section-aside { font-size: 17px; }
  }

  .birthday-section-body {
    margin-top: 18px;
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.6;
    color: var(--warm-mid);
  }
  @media (min-width: 1025px) {
    .birthday-section-body { font-size: 17.5px; line-height: 1.65; }
  }

  .birthday-section-list {
    margin-top: 22px;
    padding-left: 0;
    list-style: none;
    font-family: var(--font-ui);
    font-weight: 600;
    font-size: 13px;
    letter-spacing: 0.04em;
    color: var(--warm-white);
  }
  .birthday-section-list li {
    padding: 8px 0 8px 22px;
    border-bottom: 1px solid rgba(244, 239, 227, 0.08);
    position: relative;
  }
  .birthday-section-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--glow);
    opacity: 0.85;
  }

  .birthday-section-copy .btn {
    margin-top: 28px;
  }

  /* ============================================================
     packages — centered card grid
     ============================================================ */
  .birthday-packages {
    padding: 36px 22px;
    background: transparent;
  }
  @media (min-width: 1025px) {
    .birthday-packages { padding: 56px 64px; }
  }

  .birthday-packages-inner {
    max-width: 1100px;
    margin: 0 auto;
    text-align: center;
  }

  .t-eyebrow--center {
    text-align: center;
  }

  .birthday-packages-title {
    color: var(--warm-white);
    font-size: clamp(32px, 9vw, 48px);
    line-height: 0.98;
    margin-top: 8px;
  }
  @media (min-width: 1025px) {
    .birthday-packages-title { font-size: clamp(44px, 4.5vw, 60px); }
  }

  .birthday-packages-aside {
    /* Centered version of the section aside */
    text-align: center;
    margin-bottom: 28px;
  }
  @media (min-width: 1025px) {
    .birthday-packages-aside { margin-bottom: 36px; }
  }

  .birthday-package-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 18px;
    text-align: left;
  }
  @media (min-width: 768px) {
    .birthday-package-grid {
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }
  }

  .birthday-package-card {
    position: relative;
    padding: 28px 24px 28px;
    border-radius: var(--radius-surface);
    background: rgba(244, 239, 227, 0.03);
    border: 1px solid rgba(244, 239, 227, 0.10);
    display: flex;
    flex-direction: column;
  }
  .birthday-package-card--featured {
    border-color: rgba(78, 236, 196, 0.55);
    background: rgba(78, 236, 196, 0.04);
  }

  .birthday-package-badge {
    position: absolute;
    top: -10px;
    left: 50%;
    transform: translateX(-50%);
    padding: 4px 10px;
    background: var(--glow);
    color: var(--indigo-deep);
    font-family: var(--font-ui);
    font-weight: 700;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    border-radius: 999px;
    white-space: nowrap;
  }

  .birthday-package-name {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(24px, 6vw, 30px);
    line-height: 1;
    letter-spacing: -0.005em;
    color: var(--warm-white);
    text-transform: uppercase;
    margin: 0;
  }

  .birthday-package-price {
    margin: 16px 0 4px;
    display: flex;
    align-items: baseline;
    gap: 10px;
    flex-wrap: wrap;
  }
  .birthday-package-amount {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(32px, 9vw, 44px);
    line-height: 1;
    color: var(--glow);
    letter-spacing: -0.01em;
  }
  .birthday-package-base {
    font-family: var(--font-body);
    font-size: 14px;
    color: var(--warm-mid);
  }

  .birthday-package-meta {
    margin: 0 0 18px;
    font-family: var(--font-body);
    font-size: 13px;
    color: var(--warm-dim);
  }

  .birthday-package-list {
    margin: 0;
    padding: 0;
    list-style: none;
    flex: 1;
  }
  .birthday-package-list li {
    position: relative;
    padding: 8px 0 8px 22px;
    font-family: var(--font-body);
    font-size: 14.5px;
    line-height: 1.4;
    color: var(--warm-mid);
    border-bottom: 1px solid rgba(244, 239, 227, 0.06);
  }
  .birthday-package-list li:last-child { border-bottom: 0; }
  .birthday-package-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 14px;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--glow);
    opacity: 0.7;
  }

  .birthday-package-cta {
    margin-top: 24px;
    width: 100%;
    text-align: center;
  }

  /* ============================================================
     occasions — adult birthday flavors (milestone / bachelorette /
     teen). Text-led cards.
     ============================================================ */
  .birthday-occasions {
    padding: 36px 22px 8px;
    background: transparent;
  }
  @media (min-width: 1025px) {
    .birthday-occasions { padding: 56px 64px 16px; }
  }
  .birthday-occasions-inner {
    max-width: 1100px;
    margin: 0 auto;
    text-align: center;
  }
  .birthday-occasions-title {
    color: var(--warm-white);
    font-size: clamp(28px, 8vw, 44px);
    line-height: 0.98;
    margin-top: 8px;
    margin-bottom: 28px;
  }
  @media (min-width: 1025px) {
    .birthday-occasions-title { font-size: clamp(40px, 4.2vw, 54px); margin-bottom: 36px; }
  }
  .birthday-occasions-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 18px;
    padding: 0;
    list-style: none;
    text-align: left;
  }
  @media (min-width: 768px) {
    .birthday-occasions-grid { grid-template-columns: 1fr 1fr 1fr; gap: 20px; }
  }
  .birthday-occasion-card {
    border: 1px solid rgba(244, 239, 227, 0.10);
    border-radius: var(--radius-surface);
    background: rgba(244, 239, 227, 0.03);
    padding: 24px 22px;
    display: flex;
    flex-direction: column;
  }
  .birthday-occasion-label {
    font-family: var(--font-ui);
    font-weight: 700;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--warm-dim);
  }
  .birthday-occasion-headline {
    color: var(--warm-white);
    font-family: var(--font-display);
    font-size: clamp(20px, 5.4vw, 24px);
    line-height: 1.02;
    letter-spacing: -0.005em;
    margin-top: 8px;
  }
  .birthday-occasion-body {
    margin-top: 12px;
    font-family: var(--font-body);
    font-size: 15px;
    line-height: 1.55;
    color: var(--warm-mid);
    flex: 1;
  }
  .birthday-occasion-link {
    margin-top: 14px;
    font-family: var(--font-ui);
    font-weight: 600;
    font-size: 13px;
    letter-spacing: 0.04em;
    color: var(--warm-white);
    text-decoration: none;
    transition: color 200ms ease;
  }
  .birthday-occasion-link:hover { color: var(--glow); }

  /* ---------- FAQ (reuses the global .t2-faq* accordion) ---------- */
  .birthday-faq {
    background: transparent;
    padding: 36px 22px 8px;
  }
  @media (min-width: 1025px) {
    .birthday-faq { padding: 56px 64px 24px; }
  }
  .birthday-faq-inner {
    max-width: 760px;
    margin: 0 auto;
  }
  .birthday-faq-title {
    color: var(--warm-white);
    font-size: clamp(28px, 7vw, 40px);
    line-height: 0.98;
    letter-spacing: -0.005em;
    margin-top: 10px;
  }
  @media (min-width: 1025px) {
    .birthday-faq-title { font-size: clamp(36px, 3.6vw, 48px); }
  }
  .birthday-faq .t2-faq { margin-top: 20px; }

  /* ============================================================
     closing CTA band
     ============================================================ */
  .birthday-closing {
    padding: 48px 22px 64px;
    background: rgba(14, 10, 31, 0.30);
    text-align: center;
    border-top: 1px solid rgba(244, 239, 227, 0.06);
    border-bottom: 1px solid rgba(244, 239, 227, 0.06);
  }
  @media (min-width: 1025px) {
    .birthday-closing { padding: 80px 64px 96px; }
  }

  .birthday-closing-inner {
    max-width: 720px;
    margin: 0 auto;
  }

  .birthday-closing-title {
    color: var(--warm-white);
    font-size: clamp(30px, 8vw, 44px);
    line-height: 1;
    letter-spacing: -0.005em;
  }
  @media (min-width: 1025px) {
    .birthday-closing-title { font-size: clamp(40px, 4.4vw, 56px); }
  }

  .birthday-closing-ctas {
    margin-top: 28px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 12px;
  }
  .birthday-closing-ctas .btn {
    flex: 0 1 auto;
  }
</style>
</content>
</invoke>