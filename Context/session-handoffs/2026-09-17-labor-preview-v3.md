# Labor V2: third owner preview

Implemented and owner test delivered September 17, 2026. Jon retired the earlier owner test because its analytical basis was insufficient. The original brand/logo and working login are retained; this test replaces the previous launch candidate.

`ProposalCard.tsx` adds current/proposed shifts, dated comparison and event context, Yes/No/Maybe, short text/voice feedback, explicit save/readback, scope for remembered explanations and existing history/outcome correction. `NextSchedule.tsx` retains Yes/Maybe items across review weeks and previews the consolidated follow-up. The backend queues one recap 90 minutes after completion, including Maybe-only reviews, with delivery separately disabled for the owner preview. Current and proposed span-hours are not labeled cash savings. Real examples remain conditional on service, tasks, crew and available coverage.

The existing `?pilot=1` app and auth remain the home. Legacy question packets keep their original rendering. No production deploy or GM message. The owner review remains unanswered, with save/edit/undo/outcome checks performed in a separate local QA schema. Mobile/desktop page checks at 320/390/736/1280 and email checks at 320/390/680 passed without exceptions or overflow; selected phone/desktop/shift/recap screenshots were visually inspected. The owner email links to a clearly labeled follow-up example so reviewing it requires no fabricated saved answer. Development toolbar is disabled to keep the local preview unobstructed.

Backend: 45 relevant tests plus TypeScript passed. Website prebuild passes; its compiler retains the same six unrelated reservation/estimate errors and no labor-file diagnostics. Native dictation, actual mail-client rendering and GM completion time remain user checks. No full production release check is claimed. The durable scoped context and 90-minute recap implementation live in the TPRS pilot branch; production generation and mail remain inactive.
