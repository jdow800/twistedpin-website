# 2026-09-01 — Event Builder: first end-to-end Send, crossroads reply, WF2 hardening, congruence audit (handoff)

**Read this first tomorrow.** The previous session crashed mid-test; this one reconstructed it, shipped a lot, and ended with Jon closing for the night. Everything below is committed and pushed in `Marketing Avery` (HEAD `ae75d46`) and `Website` (HEAD after this file). Repos: `C:\Users\jdow8\dev\Marketing Avery` (Avery brain/n8n), `Website` (estimate proxy + docs), `tprs` (read-only tonight).

## State at close (verified live)

| Thing | State |
|---|---|
| `Avery — Builder Submit` (`bbIbkgJegEqz5x3P`) | **DISARMED** (inactive) — re-arm for a test with `node brain/deploy/test-builder-send.mjs --arm --no-post --eid E-0068352`; test link `https://menu.twistedpin.com/b/es/E-0068352` |
| WF2 (`dYG_0_MVmIpS_EQCBZ-Tl`) | active, versionId `372146f7` (tonight: `57ce1e1d → e536660d → 164ca2aa → b4f88557 → ee46bf0c → 42d384d8 → 372146f7`), drift-check CLEAN |
| WF1 | untouched tonight |
| Live KB Doc | untouched tonight. Rewrite target = `brain/kb/Avery_KB_builder-draft.md` (byte copy) |
| Website `/api/estimate/menu/` | now returns `rules: {vip_3h_floor:49, trad_only_floor:60, vip_ceiling:80}` (Zite reads it; values unchanged) |
| Zite | v3 plan approved + built (pizza removed, tp-well, rules-from-API, typeable steppers, "Send to Twisted Pin", submitted_by gone). Owed back: fix-list 2–10 status + photos, Phase-2 verification captures (visitor path NEVER run) |
| Golden suite | **retired as a gate (Jon 9/1)** — all deploys use `--skip-golden`; make it default on `deploy-kb.mjs` too |

## What shipped tonight (all staged on a WF2 clone first via `brain/deploy/stage-on-clone.mjs`)

1. Builder-turn crash fixes: retry path (`Build Retry Request`) and `quote_confirmation_skipped` builder-aware; 5 plumbing guards (`patch-wf2-menu-path-guards.mjs`). Audit tool `audit-menu-path.mjs` (58 inbound-only refs reachable from the menu webhook; 9 guarded).
2. Quote params PINNED to the submission on builder turns (the page said $2,450, Avery quoted $2,285 — 11 arcade cards lost to bare slugs). Pin moved ABOVE the delivery-plan auto-fix at 00:40 (audit finding).
3. **First builder Send completed end to end** (WF2 exec 24841, 6:12→6:20pm CT). Dedupe absorbed a double-press.
4. Crossroads reply (`patch-wf2-builder-crossroads-render.mjs`, snippet `brain/deploy/snippets/format-quote-builder-crossroads.js`): Avery's one-sentence personalized opener + fixed "Your total came in at about $X ($Y deposit). Any questions for me? If you're ready, I can send the deposit link over - heads up, they're valid for 48 hours." No refundable line, no "how does that sound", no upsell; email variant with compact recap; short-lead total-only. Preface budget check `builder_preface_invalid` + retry entry.
5. Staff notes stacked + 12-hour (Builder Submit `dc9f844c`), quote-breakdown note 12-hour.
6. Congruence audit (Workflow `wf_0b01d930-e2f`, 44 Sonnet agents): `Marketing Avery/audit-2026-09/CONGRUENCE-REPORT.md`, `AVERY-SYSTEM-MAP.md`, cards, KB sweeps, critic.

## Rulings recorded tonight (all in `zite-event-builder-brief-2026-08-30.md` §2.2c–2.2e, §2.3a, §2.3c–2.3e and `vip-capacity-rebanding-spec-2026-09-01.md`)

- VIP bands: 2=16, 4=32, 6 lanes to **60 everyone**, **61–75 company only** (Avery closes autonomously), 76+ staff; near-miss → NA. **3 hours NEVER required**, suggested from 43. Page stays type-blind to 75; WF2 sorts at Send (non-company 61–75 built in VIP → NA directly). Spec knobs still open: 61–75 vibe-line wording; "default 3h, both offered" encoding; default lane at 61–75.
- Availability: recourse is mandatory (earlier/later/traditional); Tier 1 = day-level closures from TPRS (no Zite work); Tier 2 = slot alternatives (TPRS endpoint + WF2 ladder); page popup only after Tier 2.
- Refund = an ANSWER when asked, never announced. Headcount: **say "seven days" to guests; day 6 is internal.** Copy audit owed (KB 14 lines, WF2 2 files, WF5/FP/fundraiser templates, tprs 5 files, pay page date) — as one pass with the KB §7 rewrite. Open: does "answer not announcement" retire the 8/31 refundable line on the STANDARD quote too?
- "I just sent it" race: never "nothing on my end yet"; hold line + 10–15 min timer + NA. Collision Gate CONFIRMED to eat the builder quote when a guest texts in the 5-min window (fix with the hold).
- WF1 first touch = greet + ONE routing question, **NO link** (REVERSED late 9/1: a linked first message that gets silence is unreadable - spam vs unseen vs objection - so the link lands on WF2 turn 1 after the guest's first reply; first-touch theory in brief §2.3a); form still drops date + time-of-day post-widen. Big light switch: KB deploy + WF1/WF2-turn-1 flip + menu-link constants + Builder Submit armed + proxy constants in one window, old links/KB kept as revert.

## Next (plan of record: `builder-rollout-review-2026-09-01.md` §6 + §6a)

1. Jon: clone the live KB Google Doc → "Avery KB — BUILDER DRAFT", same sharing, send the URL. Then: `deploy-kb.mjs` gains a target-doc option; staging clones' Fetch KB point at the draft Doc.
2. B1 guards from the audit: `Set Route` `inZone` company branch; Engagement Nudge `copy_variant` split from the company dwell exemption.
3. `Queue API Failure` webhook-agnostic + replay routing (builder-turn API failures currently die into one Gmail).
4. Collision Gate builder-awareness + the "I just sent it" hold/timer/NA (also scope WF1's zero-wait visitor fire).
5. Redacted-export tooling (WF2 revert exists only on this PC); key/PAT rotation.
6. TPRS season port (Nov 17–Apr 1 + Sunday P&P) — P0 before Nov 1 (memory `tprs-engine-season-gap`).
7. KB rewrite against the report's §2 worklist, in the draft file.

## Gotchas learned tonight (save yourself an hour)

- **The Bash tool strips backslashes and chokes on multi-line quoted heredocs** — write files with the Write/Edit tools; keep Bash for commands. (Regex literals sent via Bash arrived as `/$s?d/`.)
- `String.replace` with a replacement STRING expands `$'` — splicing code that contains `about $' +` truncated Format Quote once. Use a function replacer (`s.replace(a, () => b)`).
- The auto-mode permission classifier (Sonnet) rate-limits while a Sonnet fan-out is running; wait it out.
- On a fresh Send, wait out collision (5m) + humanize (≤9m) before declaring anything missing — the "relay is broken" scare was a timing artifact.
- Zite's Send is a SERVER-SIDE relay (`workflows.zite.com` → `builderSubmit`/`builderInquiry`), not a browser POST: CORS irrelevant; their runner log is the first place to look.
- `stage-on-clone.mjs` before every prod WF2 change; `audit-menu-path.mjs --evidence <execIds>` after any WF2 wiring change.
## Late-night addendum (after Jon's "go ahead on the new KB doc", ~11pm–1:30am CT)

- **KB draft = the builder-world KB, in Jon's cloned Doc** (`1JKQdjnFnfoG8kX07KMMvm1DbyBmODurNVRoQ0okpDBs`, id in `brain/deploy/.env` as `KB_DRAFT_DOC_ID`). Plumbing: KB Utility accepts `doc_id` (patch `patch-kb-utility-doc-id.mjs`, default LIVE), `deploy-kb.mjs --doc draft` pushes `brain/kb/Avery_KB_builder-draft.md` to it (canary untouched; golden opt-in only; a LIVE deploy REFUSES a file carrying the draft STATUS banner). Live Doc verified untouched after every push.
- **Content applied to the draft tonight:** Group A rewrite (41 edits: bands to 60/75, 3h suggested never required, day-7 spoken, refund as answer, builder scoping); **Section 21 Builder Submission Handling** (21.1–21.10 incl. the availability-aware crossroads 21.2, the re-quoter play 21.6a, turn-1 link rule 21.8, the hold 21.4, silence posture 21.5) + companions (NN17/NN19 carve-outs, Link Directory, Section 19 bullet); **21.2b inquiry-vs-build headcount shrink**; **three trim passes** (retired menu-link mechanic, provenance strip ledgered at `audit-2026-09/trim/provenance-ledger.md`, duplicate clusters, S6 first touch = greet + one routing question, S7/S8/S15/S16/S18/S20 compression). **Result: 341,237 → 303,553 chars — UNDER live (307,192) with all of Section 21 inside.** Tools: `kb-trim-map.mjs` (measure), `apply-kb-edits.mjs` (apply reviewed journal edits; `--order`; empty new = deletion). Edit sets of record under `audit-2026-09/rewrite`, `trim`, `trim2`, `trim3`.
- **Five `[DEFAULT APPLIED - Jon to confirm]` tags in the draft Doc** (search the string): bar-adjacency reassurance held until asked (21.6); 76+ near-miss framing only when VIP was wanted (21.6); 10–14 birthday-family builds = qualify first, Option B (21.7); shrink threshold 75%-and-≥5 (21.2b); one-turn shrink shape (21.2b). Plus a `[DRAFT NOTE]` atop Section 21 and a `[SYSTEM NOTE]` in 21.6a — all removed at the switch.
- **WF2 also changed after the handoff above:** `cad4dd42` crossroads states availability when the same-turn TPRS check says open ("currently open" + "secure it with your deposit", never hold). **Builder Submit** `15eb74b2` captures `builder_payload._prior` (pre-build row values) for the shrink check; DORMANT.
- **Rulings added:** WF1 first touch does NOT deliver the link (greet + one routing question; link on WF2 turn 1); blocked slot = Avery re-quotes (ladder promoted to before D1); headcount shrink rule.
- **Trimming stops here** until the clone rehearsal has exercised the draft end to end; further cuts should be driven by what the rehearsal proves unnecessary.
- **Next block:** clone WF1+WF2 (stage-on-clone), point the clones' `Fetch KB` nodes at the draft Doc id, point Builder Submit's fire URL at the WF2 clone, run the test row + a visitor Send; then the pre-flip plumbing (Set Route ceiling, EN split, replay-queue fix, collision/hold, WF1 kids guard, TPRS alternatives).

## 2026-09-02 morning — REHEARSAL WORLD BUILT; five Sends; three builder-turn defects fixed clone-first then prod

- **Tool:** `Marketing Avery/brain/deploy/rehearsal-clones.mjs` (`create|status|activate|deactivate|refresh-brain|point-builder-submit --to rehearsal|prod|delete`; state in gitignored `.rehearsal.json`). Clones: WF2 `1cr3uefJsCvca9S4`, WF1 `LpS1XuqMzr5VjMoS` ("ZZ REHEARSAL — … — keep"), ACTIVE, own webhook paths (`rh-menu-inbound`, `rh-en-nudge-send`, fresh UUIDs), Fetch KB on the DRAFT Doc, AH-replay + WF1 fire URLs rewritten into the clone world (After-Hours Release verified to replay to the URL stored on the row). **Builder Submit fires the clone** (`20a41ab6`) and is ARMED — every Send is answered by the clone from the draft KB. Why not UI Duplicate: path collisions on activate, three URLs to re-point, canary/drift address prod by id. **Isolation limit:** guest replies on the test thread still land on prod (Missive rule) — Jon's call on a `REHEARSAL` label rule. Visitor lap tool: `test-builder-visitor.mjs --send` (rehearsal WF1 by default). `test-builder-send.mjs` gained `--date/--time/--guests/--lane/--hours/--food`.
- **Findings (memory [[avery-builder-rehearsal-world]]; WF2.changelog 2026-09-02 morning):** (1) the crossroads "Good news - open" branch had NEVER fired — render read `available/key` flat, the State node nests them under `tprs_availability` (harness mocked the flat shape); (2) a same-key re-Send skips the TPRS call, so the render must honor the gate's STORED read; gate now re-checks a stored read older than 6h on builder turns only (`patch-wf2-availability-gate-builder-ttl.mjs`); (3) third identical Send: the model acked with `quote_needed:false`, `bar_upsell_missing` failed it and the retry ADDED the $15 pitch (validator-forced #13) — now in-band builds are FORCED onto the quote route (`builder_quote_forced`) and builder turns skip the bar-upsell check; prompt lane a says a re-Send is still a quote turn. Open-branch wording = Jon's: *"Good news - Sat, Oct 24th at 6pm is currently available. Your total came in at about $X and can be held with the 50% deposit ($Y). Any questions for me, or should I go ahead and get you that deposit link over?"*
- **Fourth finding (Jon, reading the texts): builder/menu turns texted from the RETIRED Twilio number +18156765704.** The four SMS reply builders reply on the number the guest texted (read off the Inbound Webhook) with a hardcoded fallback for turns that have no inbound webhook, and the fallback was the Twilio literal. Now SignalWire `+17793030261` (brain `email/*.js` + `patch-wf2-goodnight-from-number.mjs`; prod `-> 1c75eb3c`). Audit of 289 prod executions: one 815 send ever (the 9/1 test to Jon), no guests; the 136 inbound SMS turns of the last five days all arrived on 779. Rehearsal texts before this fix landed on Jon's 815 thread; from Send #7 on they land on the 779 thread, which is the one to reply on.
- **Prod WF2:** `f2a57b49 -> ab7420a2` (render, gate TTL, brain), drift CLEAN; builder path unreachable in prod until the flip. Harness `test-builder-turn.mjs` 156 green (fixture factory — the shim passes objects by reference). `test-late-night-span.mjs` has 2 PRE-EXISTING reds (section 6 "bookable ask does not flag" -> "silent") — not from today; needs its own look.
- **TURN TWO+ REHEARSED END TO END (9/2 late morning) - the whole builder conversation ran in the copy world with prod silent.** Missive routing is IN PLACE: Jon's new incoming-SMS rule (To `+17793030261`, From `+12073139616`, label not Needs Attention -> clone inbound `bea7ff61…`) and the prod rule gained `From is not +12073139616` (the first attempt without the `+1` never matched - Missive compares the E.164 string as-is; prod double-fired once, exec 25013, live-KB refund answer "about a week out"). Then on the clone: Send #7 crossroads from 779 (exec 25011) -> "Is the deposit refundable?" (prod, before the exclusion) -> "Can we bring a cake?" (clone 25017: catered package + commercial-bakery rule) -> "Yes, go ahead and send the link" (clone 25020: no email on the row -> "what's the best email for your receipt?") -> email (clone 25147: `book_event_avery`, TPRS **INV-2026-00513**, $2,810 / $1,405 deposit, link `pay.twistedpin.com/m/p4J5r1m…`, expires 2026-09-04 16:05Z). **INV-2026-00513 is a real pending_deposit booking on Jon's test number holding 4 VIP lanes Oct 24 6pm - it auto-cancels at the 48h expiry, or cancel it in admin.** Draft-KB refund answer now carries Jon's wording ("7 days prior to your event I will reach out, ask for a final headcount and send you the final payment reflecting that") - the LIVE KB still says "about a week out" (day-7 copy audit item). **Copy to rule on:** `Build Link Reply` (WF2 node, static) closes with "Once the event gets closer, we will finalize your headcount and send you a final payment link" and prints "$1405" without a comma.
- **SMS DELAYS RE-REVIEWED AND RULED FINE (Jon, 9/2 late morning) - do not re-pitch.** Map: WF2 `Wait - Debounce` 45s fixed -> model/checks/engine ~30-90s -> `Quiet Hours Gate` pacing SMS 150-210s random / email 360-540s (Jon 7/7 "human pacing") -> send; a builder Send adds Builder Submit's 5-min `Collision Settle Wait` in front, so Sends land 8-9 min after the press while inbound replies take ~4m15s. Jon: "positioning this as a real human responding ... sufficiently fast", likes the random variability. The stacked 9 minutes on a Send was raised and accepted.
- **Link-reply copy SHIPPED (Jon approved 9/2, digits): `Build Link Reply` closes with *"7 days before your event I'll reach out for your final headcount and send the final payment link reflecting that count."*, prints "$1,405", and uses blank-line spacing on email (paragraphs) vs single newlines on SMS.** Safe unconditionally: Set Route sends any commit with <10 days lead / >60 guests / >365 days to `book_event_staff_tag`, so this node never renders on a short-notice booking. `patch-wf2-link-reply-copy.mjs` (clone `d178da46`, prod `6e4c4702`, drift clean); harness `test-book-event-idempotency.mjs` 47/47.
- **TO-DO (Jon, 9/2): metrics + monitoring must follow the new flows before the flip** - WF1/WF2 Claude-usage rows (`Metrics: Initial/Retry` -> `avery_metrics_buffer`, WF7 aggregates + Cache_Efficiency), Needs-Attention tracking, KB-gap tracking: double-wire or swap. Known now: the `workflow` label is hardcoded ("WF1"/"WF2"), Builder Submit writes no metrics, and the clones wrote ~10 WF2-labelled rows on 9/2 morning before `rehearsal-clones.mjs tag-metrics` relabelled them `WF2-REHEARSAL`/`WF1-REHEARSAL` (now part of `create`). Evaluation item, not a build yet - `builder-rollout-review-2026-09-01.md` §6c.
- **FAST MODE IS ON (since 9/2 17:18Z, Jon's ask): `rehearsal-clones.mjs fast-mode on` shortened WF2 clone debounce + pacing and WF1 clone first-touch to 3 s and Builder Submit's collision wait to 10 s (Builder Submit `20a41ab6 -> fe6876a2` - PROD workflow).** A builder Send now lands in ~1-2 min. **`fast-mode off` is a hard item on the switch checklist** (`status` nags, `delete` refuses while on). NYE re-fire after the decline fix (exec 25175): model set `builder_decline: nye_ticketed_night`, no force, reply = "Dec 31 is NYE, and that night is party reservations only, so it books separately at twistedpin.com/nye ... keep your Oct 24 setup as is, or were you looking to move off that date?"
- **COLLISION GATE WAS EATING SENDS (exec 25182, fixed 9/2 midday):** Builder Submit's gate read the sender from `from_field.address || phone_number`; Avery's SignalWire texts carry only `id` + `username`, so they read as guest messages and any Send within 5.5 min of an Avery reply was silently suppressed (no WF2 turn, no note). Installer fixed (username/id/account id/`author.me`), redeployed `23dfa5ec`, re-pointed + fast mode re-applied (`5e50ba83`); harness section E runs the live gate on both shapes. The GENUINE guest-text-in-window suppression remains by design → the "I just sent it" hold is still owed.
- **CAPACITY-COLLISION LAP (Sept 19 3pm VIP, Karla Doherty's INV-2026-00138 holds 6/6) + THE ALTERNATIVES BUILD, both halves (9/2 midday):** exec 25180 showed today's blocked play - "Quick heads up - Sep 19 at 15:00 is not open on the VIP suite ... start at 7:00 PM ... or keep 15:00 over on our traditional lanes" (raw 24h time, no weekday, no BEFORE option). **Built:** (1) WF2 `patch-wf2-blocked-reply-template.mjs` - Set Route's `_blockedReply()` is now Jon's template ("Hey Jon - so unfortunately Sat, Sep 19 at 3pm is spoken for already in our VIP suite. / We could look at moving to a 1pm or 7pm start, which are both available - or we do have room on the traditional side. / Also could look at another day if you prefer. Let me know."), renders two/one/no starts, traditional line only when the probe found room, 12-hour formatting computed in code, weekday from the ISO date; `TPRS Availability State` maps `previous_available_start -> prev_start`; harness `test-blocked-reply-template.mjs` 14/14; on the clone (`645c2acc`), **prod `-> bae10a1a` after exec 25184 proved it** (live inbound blocked turns now use the owner wording too). (2) **tprs PR #146 MERGED 9/2 17:44Z (`ae7b65c`, squash; worktree removed)**: check-availability walks BACKWARDS 30 min at a time on the same CT date and reports `previous_available_start`; each probe runs the full engine check for the build's real duration + buffers, so it lands on noon for a 2h build against a 3-6 block and 11am for a 3h one (Jon's examples); test file 12/12; CI green. Jon 9/2: "you should be able to push and merge" - `gh pr merge` worked from the Bash tool this time (only the post-merge local branch switch failed, harmlessly, because of the worktree). Fast-mode incident en route: the first Quiet Hours Gate rewrite swallowed `const now` (exec 25177 error email to Jon) - fixed, restored, re-applied.
- **BLOCKED-SLOT LAP (NYE 6pm, exec 25172) found two things:** (1) **TPRS has no event block on 12/31** - the holiday seed only `removed`-overrides `customer_bookable_online` products, so the catered event lane products stay bookable and the Avery check said OPEN; **tprs to-do (Jon/Shanna, seed pattern): `removed` overrides for the EVENT lane products on 12/31 and any other never-book-events date** (product-level, not a pool closure - the NYE packages sell on the same pools). (2) **The model correctly declined ("NYE is booked separately at twistedpin.com/nye") and this morning's `builder_quote_forced` overrode it** (validator-forced #14). Fixed: prompt lane d + `builder_decline` field, force skips on it and never on 12/31; prod WF2 `-> 07e18778`, harness 162. A true VIP capacity-collision lap is still owed (NYE tested the ticketed-night path, not the "spoken for" path) - use a known-full Saturday from `/admin/lane-capacity`.
- **TO-DO (Jon, 9/2): readback + pre-payment edits** - (1) "what did I choose?" -> deterministic "your picks" render from the row (slugs humanized, channel-aware), KB rule; (2) "change the beer wall cards to X" on a pending_deposit booking -> new Set Route branch calling the EXISTING `POST /api/avery/edit-booking` (re-prices, capacity re-check, magic link URL byte-identical -> "same link, just refresh it"); scope bar/add-ons/food only, unpaid only, row written before the edit request; today this raises Needs Attention. Rollout review §6d.
- **Next:** Jon's Missive-label decision for turn two (DONE 9/2 - label rule + prod exclusion in place); the visitor-mode Send on the WF1 clone (`test-builder-visitor.mjs --send`, creates a row on Jon's cell — archive after); a BLOCKED-slot Send (`--date 2026-12-31`) to see the bad-news turn; then the pre-flip plumbing list above. Builder Submit stays pointed at the clone (disarm with `test-builder-send.mjs --disarm` when the window closes).

