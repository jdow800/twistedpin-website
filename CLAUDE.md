# Twisted Pin Website — Project Context

This file is the entry point for any session working on this project. Read it first.

---

## Refined Thesis (load-bearing)

**Twisted Pin is a bar-program-led venue.** The website sells the bar, not the bowling alley.

Two cornerstone differentiators carry equal weight:

1. **A craft cocktail program built by *America's Top Mixologist*** (Food Network) — Brian Van Flandern, who set the program up as a consultant and is back to retrain the team summer 2026. He is not staff, not an owner, not a daily presence; the credential is the lever, the relationship is the truth. Per Se / Thomas Keller / Michelin three-star framing is **fully retired** — does not appear anywhere on the website, including long-form body copy (see 2026-05-03 decisions log).
2. **The 28-tap self-serve beer & wine wall** — unique in our immediate market.

The bowling experience exists, but it isn't the lead. **When bowling shows up on the homepage, it's the VIP suite specifically** — that's the actual differentiator vs Bowlero / Lucky Strike, not "we have lanes." Traditional lanes get acknowledged lower on the page.

**Reference frame:** Flight Club Chicago and similar dark experiential venues — they lead with food/beverage, the activity is implied, customers figure it out fast. We do the same.

**Adults-first, family-welcome — segmented by hour, not by audience.** The venue self-segments by hour: Saturday at 6pm is family-dinner-and-bowling; Saturday at 11pm is friends-and-cocktails. **Same room, different night.** Adult-quality is the *lever* (the reason to choose us over Chuck E. Cheese / Bowlero / D&B); family-welcome is the *permission slip* (the reason it's OK to bring the kids). Don't lead with family copy ("family-friendly fun" — banned); don't write copy that implies kids aren't welcome either. The adult is always the chooser; the choice is easier when the parent knows the kid will ask to come back. Female-led decision-making, mixed-gender photography, real groups. The 1am close is a positioning asset, not just an ops fact — it signals adult time exists, which is what gives family time permission to exist without contradiction. (Earlier *"adults-first, group-friendly, family-welcome in that order"* formulation read as a grudging hierarchy; reframed 2026-05-11 to co-equal time-segmented modes.)

---

## Context Files (the brief)

All context lives in `Context/`. Read all four for any meaningful work.

| File | What it owns |
|---|---|
| **`Context/voice.md`** | Voice, tone, words to use / avoid, locked headline copy, Brian Van Flandern credential lines |
| **`Context/visual-direction.md`** | Web visual thesis (current). Color usage, typography application, photo direction, hero specs. Overrides the original neon brief. |
| **`Context/brand-guidelines.md`** | Logo system, typography roster, hex values, "the Pin" / Formation marks. Mood description partially deprecated (see header note); everything else is authoritative. |
| **`Context/seo.md`** | Keywords, page structure, URL migration plan, page-speed targets, meta requirements |
| **`Context/media-needs.md`** | Running list of imagery / video / copy / data assets needed for inner pages. Update whenever a new pillar ships — flag any homepage-reuse stubs, placeholders, or missing real assets so the user can source / brief / replace. |
| **`Context/adding-events.md`** | **How to put a night on `/upcoming-events/`** — one-off vs weekly-recurring frontmatter, the DST offset table, the traps (never drop a non-event `.md` in `src/content/events/`; quote `skip` dates, don't quote `until`), CTA rules, and why event art is schema-only. Read before adding any event. |
| **`Context/launch-checklist.md`** | Historical launch checklist. Site is live at twistedpin.com (2026-05-17). Retained for the 301 redirect map of record + any residual ops-data placeholders / counsel review items still open. |

Supporting assets:
- `Context/pictures/` — photo library (with `events-catering/` subfolder)
- `Context/videos/` — vertical reels and event footage
- `Context/visual-inspiration/` — reference site mobile screenshots
- `Context/logos/` — brand mark files

---

## Working Preferences

- **Mobile-first.** 90% of traffic is mobile. Design and test mobile before desktop. Don't build desktop and squish.
- **Mobile and desktop hero are separate decisions.** Vertical video hero on mobile; hero photograph on desktop. Never letterbox or stretch the vertical video to fit desktop.
- **Page-speed targets are non-negotiable.** Mobile LCP < 2.5s, performance score 85+. See `seo.md` for the full table. Every other SEO win is wasted if speed isn't there.
- **Lock visual decisions before structure work.** Color, type, hero composition decided first. Then sections. Then components.
- **Push the edge, don't play it safe.** I'd rather see you push the attitude on copy and I dial back than see safe-corporate defaults. Confident and playful with attitude is the register. Just not gross.
- **Don't bake new design language into everything before it's approved.** If you propose a new pattern (e.g., the scribble art treatment), mock it up on a placeholder page first.
- **No homepage decoration that fights the moody thesis.** Arcade and LED-video-wall photography do not appear on the homepage. They live on `/arcade/` and `/bowling/` respectively.
- **Specifics beat adjectives.** *"17 traditional lanes + a 6-lane VIP suite"* beats *"upscale bowling experience."* Use real numbers in copy and alt text.
- **Brian Van Flandern in cocktail/bar contexts only.** Don't force the name into food, bowling, arcade, or generic copy.
- **The wording bans below DON'T apply inside `/playbook`.** Jon's ruling 2026-07-20: *"ignore wording bans when it comes to play/guide books."* These are **marketing-positioning rules for guest-facing surfaces**; the Playbook and Guidebook are internal documents describing how the business actually runs, and e.g. "private event" is just what the operation calls it. A brand sweep that greps for banned words will hit `src/content/playbook/*` — leave those files alone.
- **"Private" is banned in copy.** No surface (page, ad, bio, schema, alt text) describes the VIP suite — or anything else at the venue — as "private." It's *semi-private*. Use "yours for the night," "reserved for your group," "set apart from the main floor," or "semi-private suite" if access nuance matters. The full rule is in `voice.md` (Words to Avoid). User has flagged this multiple times — don't break it.
- **"Cheap / discount / value / deals / budget-friendly" is banned in copy.** Premium positioning — never write price-discount language anywhere on the site, including the `/coupon` page (renamed from `/free-10` 2026-05-04). Frame offers without leaning into discount vocabulary. Full rule in `voice.md` Words to Avoid.
- **Family-welcome reality vs adults-first positioning.** The venue IS family-friendly — most attendees are families. But copy leads adults-first because the adult is the one who needs convincing (the inverse of Chuck E. Cheese: there it's family-friendly and adults don't want to be there, so they market families; here it's family-friendly and adults DO want to be there, so we market adults). Don't deny families; don't market with "family-friendly fun" / "fun for the whole family" / "family-friendly" as the lead noun. Family-friendliness is a fact, not a brand angle. **Two-mode framing (2026-05-11):** the venue self-segments by hour — family-dinner at 6, friends-and-cocktails at 11, same room, different night. The 1am close is the structural signal that adult time exists, which gives family time permission to exist without contradiction. See `voice.md` "Adults-first, family-welcome — segmented by hour, not by audience" section.

---

## Locked Decisions (so far)

| Decision | Locked value |
|---|---|
| Lane phrasing (canonical) | *"17 traditional lanes + a 6-lane VIP suite"* — traditional first, VIP as upgrade |
| Mobile hero source | Splice direction locked: pour (`Bank Vs Stories.mp4`) → tap wall (`Beer Wall.mov`) → cocktail (`Best Things To Order.mov`). Total ≤4–5s at current CRF. **Specific window timestamps pending user.** Currently live: Bank Vs Stories 0–4s, single shot. |
| Desktop hero | Video placeholder (`After Social Highlight…_v2.mp4`, 8s recut). Final desktop hero (photo or video) still pending shoot. |
| Hero eyebrow | Retired per 2026-05-01 promotion. Live `/` and `/snap-test/` ship without an eyebrow. (Old: *PLAINFIELD, IL* — warm-white, no Glow.) |
| Hero headline | *"Built for adults."* / *"Fine, bring the kids."* — two lines, all caps, Barlow Cond Black 900 |
| Hero subhead | *"Plainfield's premier night out. Bowling optional."* — Roboto Slab Regular, no italics. (Old stat-trio *"28 self-serve taps · A 6-lane VIP suite · 17 traditional lanes · A chef-inspired menu"* retired same day.) |
| Hero CTA | **None in the hero.** Sticky bar carries all conversion (*Reserve a lane* solid Glow + *Plan an event* outlined). |
| Closing band (homepage) | *"You can keep doing dinner-and-a-movie. Or you can do this."* |
| Cocktail credential (primary) | *America's Top Mixologist* (Food Network), with cocktails served in 40+ countries. Per Se / Thomas Keller / Michelin three-star framing **fully retired** — does not appear anywhere on the website (long-form carve-out also retired, 2026-05-03). |
| Type stack (substitutes) | Display: Barlow Condensed 900 · UI: Montserrat 700 · Body: Roboto Slab 400. Production swap to Adobe Fonts (Proxima Nova Extra Cond Black + Proxima Nova + Yorkten Slab) deferred until kit ID is provided. One CSS-variable change. |
| Headline scale | `clamp(34px, 11vw, 80px)` mobile + `letter-spacing: -0.015em`. Verified glyph fits at 360 / 390 / 412 with ≥8px slack. |
| Eyebrow color rule | Warm-white only. Glow is reserved for the primary CTA in the first viewport. |
| Button radius | 7px on all buttons (not pill, not sharp) |
| Brand mark (hero, top-left) | Logo image (`LogoGBED_Horizontal_White`). Mobile 41px / desktop 56px. Round-4 sizes. |
| Drawer header | Logo image (`Logo_Horizontal_GlowInTheDark`, no GBED tagline). Mobile 73px / desktop 84px. Replaces the retired *"The works."* text. |
| Drawer rows | Lucide line icons right-aligned, 24px, `currentColor` (Glow on hover): martini / bowling-pin / calendar / utensils-crossed / map-pin. Bowling pin hand-drawn in matching Lucide stroke style — Lucide doesn't ship one. |
| Sticky CTA bar (mobile) | Always-visible bottom bar. *Reserve a lane* = primary (Glow solid). *Plan an event* = deliberate alternate (Indigo Deep solid + warm-white outlined border + warm-white text). Lives in layout, not Hero. |
| CTA hierarchy (global) | Primary: Glow solid. Alternate: Indigo Deep solid + warm-white outlined. Same recipe in desktop header and mobile sticky bar. *Restraint as confidence* — the deliberate user finds the quieter button. |
| Persistent header (desktop) | `SiteHeader` global component. Logo (left) + inline nav BAR · EAT · BOWL · GAME · EVENTS · VIP SUITE · MORE ▼ (center) + Reserve / Plan CTAs (right). Light translucent scrim + backdrop blur. MORE dropdown click-only. Active-page underline (Glow, 1px) on current page only. **VIP SUITE is the 6th inline item (promoted from MORE 2026-05-04) — tier-1 conversion page, sits to the right of Events as the natural pairing.** |
| Persistent header (mobile) | Logo (left) + hamburger (right). Inline nav + CTAs hidden; CTAs live in the bottom sticky bar. |
| Coupon banner | Retired (built and removed same-day). Coupon reaches users via MORE dropdown (`/coupon/`, label "Coupon") + footer. Renamed from legacy `/free-10` 2026-05-04 — old URL gets a 301 to `/coupon` at launch. |
| Visual mood | Moody/neutral, dark backgrounds, warm wood + copper accents, photo-led, bold display type. *Not* neon. |
| Bowling positioning | VIP suite is the bowling shot on the homepage. Traditional lanes acknowledged lower. |
| Reserved copy (cocktail/bar section H2) | Short: *"Curated by America's Top Mixologist. (Their words, not ours.)"* · Full: *"Cocktails this serious aren't supposed to live at bowling alleys. Menu curated by Brian Van Flandern, America's Top Mixologist."* — held for the future cocktail/bar block. Don't pre-spend. (Earlier "Built by" form retired 2026-05-17 — see Decisions Log.) |
| Deprecated copy | *"The bar that bowls."* (retired) · *"Built for adults. Kids will come."* (superseded by *"Fine, bring the kids."*) · *"Built by [X]" / "Designed by [X]"* — entire constructions retired 2026-05-17 across the site; replaced everywhere by *"Curated by [X]"* (Brian is a consultant, not staff/owner — see voice.md Words to Avoid) · *"Chef-inspired" / "Chef-driven"* — entire family retired 2026-05-17 (venue has no executive chef); replacements: *"from-scratch,"* *"built to share,"* or describe the food itself. |
| Desktop section architecture (`/snap-test/`) | Parallel mobile/desktop tracks via `display: none/flex` swap at 1025px. Mobile retains 8-snap track exactly. Desktop renders Hero (100dvh) → Tap Wall (70vh, split) → Cocktails (70vh, split, alternates) → Events (60vh, full-bleed) → EBG (70vh, gated on stills) → Footer. Reveal motion: `[data-reveal]` fade-up 12px / 450ms via IntersectionObserver + window/.snap scroll fallback. |
| Bowl copy (two variants) | Mobile snap 6: *"17 traditional lanes plus a 6-lane VIP suite. (yes, you can take it over)"* — VIP-leading with aside. Desktop EBG card: *"17 lanes plus a 6-lane VIP suite."* — tighter, no aside, VIP differentiator preserved. |
| Cocktails copy (two variants) | Mobile snap 4: *"Curated by America's Top Mixologist. (their words, not ours)"* — terse, fits stub. Desktop section 3 (50/50 split): *"Cocktails this serious aren't supposed to live at bowling alleys. Menu curated by Brian Van Flandern, America's Top Mixologist."* — long-form. (Both variants used "Built by" until 2026-05-17 — see Decisions Log.) |
| Workflow | Direct-to-main pushes after one-time `feat/hero-round-2` round. Vercel auto-deploys on push. |

---

## In Progress
- **2026-09-02 (morning) — EVENT BUILDER REHEARSAL WORLD built and in use; five Sends found three builder-turn defects, fixed clone-first then prod (WF2 `f2a57b49 -> ab7420a2`).** Record: [session-handoffs/2026-09-01-builder-first-send-crossroads-audit.md](Context/session-handoffs/2026-09-01-builder-first-send-crossroads-audit.md) (2026-09-02 section), `Marketing Avery/n8n/workflows/WF2.changelog.md` (2026-09-02 morning), `builder-rollout-review-2026-09-01.md` §6b, memory [[avery-builder-rehearsal-world]]. **Tool:** `brain/deploy/rehearsal-clones.mjs` — persistent WF1 (`LpS1XuqMzr5VjMoS`) + WF2 (`1cr3uefJsCvca9S4`) clones on their own `rh-*` webhook paths reading the BUILDER-DRAFT KB Doc; AH-replay + WF1 fire URLs rewritten into the clone world; **Builder Submit fires the clone and is ARMED — every /build Send is answered by the clone from the draft KB, never prod** (`point-builder-submit --to prod` reverses; `create-builder-submit.mjs --yes` also resets it). Isolation limit: Missive posts guest REPLIES to prod, so turn two of a rehearsal thread needs a `REHEARSAL` label rule (Jon's call, open). **Findings:** the crossroads "Good news - open" branch had never fired (render read `available/key` flat; the State node nests them under `tprs_availability`; the harness had mocked the flat shape); a same-key re-Send skips the TPRS call so the render must honor the gate's STORED read (+ builder-only 6h TTL in the gate, `patch-wf2-availability-gate-builder-ttl.mjs`); on the third identical Send the model acked with `quote_needed:false` and `bar_upsell_missing` made the retry ADD the $15 pitch (validator-forced #13) — in-band builds are now FORCED onto the quote route (`builder_quote_forced`) and builder turns skip the bar-upsell check. **Open-branch copy = Jon's 9/2 wording:** opener (model) / *"Good news - Sat, Oct 24th at 6pm is currently available."* / *"Your total came in at about $X, and the 50% deposit ($Y) holds the date and time."* / *"Any questions for me? If not, I can get a deposit link over to you (they are only valid for 48 hours)."* Harness `test-builder-turn.mjs` 156 green. **9/2 EVENING — the "I just sent it" HOLD is BUILT (rollout A1+A2, brief §2.2c):** Pre-Assemble `submission_state` (claim / pending build / open hold), Set Route hold line *"Great - that should land on my end shortly, I will review and message you back."* + 15-min `claim_hold_*` columns (Loyalty/db/076, applied), silent stand-down on a pending build, freshness gate never supersedes a builder turn, `Avery - Claim Hold Watcher` (`3Ycte48ZMnNoYdS3`, ACTIVE, 5 min → NA note when nothing landed), Builder Submit Collision Gate no longer suppresses; `receipt_denial` check + retry entry; harness `test-claim-hold.mjs` 69/69. **Both rehearsal laps passed on the clone; LIVE ON PROD WF2 `684ddff0` (~4:20pm CT) — the deposit-claim hold and legacy-menu "sent it" handling are reachable by real guests today, builder paths stay unreachable until the flip. Lap 2 caught that only one of THREE freshness gates had the builder exemption (Quote twin dropped the crossroads, exec 25272) - all three patched.** **9/2 LATE EVENING — FIRST TOUCH REDESIGNED (Jon): form shrinks to type/name/phone/count/notes; WF1 NEVER sends the build link, its first touch is a fixed open-ended offer (*"If you'd like, I can send over menu options and pricing for you to put together and review. Let me know."*); WF2 delivers the builder link on the first eligible reply turn (`menu.twistedpin.com/b/es|el/{eid}` - Zite's routes, confirmed by Jon 9/2 late; `/b/es/` = Essential with Pizza & Pop permitted, `/b/el/` = Elevated; the query form is fallback only). BUILT + GATED (`BUILDER_OPENER_ENABLED`, `BUILDER_LINK_ON_TURN` in gates.json "event_builder_switch"), staged on clones, inert on prod (WF1 6400aa81 / WF2 35978960), harness `test-builder-link-turn.mjs` 51/51; KB draft Section 6 + 21.8 rewritten. Also: "build" banned to guests (`builder_word_leak`). Memory [[avery-builder-first-touch-opener]]. **9/2 LATE (cont.):** routes confirmed (`/b/es|el/{eid}`); menu endpoint 500 fixed; Zite Paste 1 (bands) + Paste 2 (five-field form, held) sent, two apps = two pastes; five KB defaults RULED; 12/31 event-lane gating LIVE (tprs #150 merged + prod rows); **EN nudge variants BUILT + DEPLOYED inert** (`builder_offer` 24h / `builder_link` 48h, no link in nudges; db/077 stamps from WF1/WF2; audit #2 split live) - copy awaits Jon. **Next:** the flip runbook + clone dry run; headcount-shrink check (21.2b); Queue API Failure replay; label swap; joint window with Zite. `test-late-night-span.mjs` carries 2 PRE-EXISTING reds (not from today).
- **2026-09-01/02 — A self-booked kids party Avery couldn't see; customer-row split found and fixed (LIVE, n8n `TPRS Outbox Listener`).** Full narrative, verification, rollback, traps: [session-handoffs/2026-09-01-self-serve-kids-party-avery-blindness.md](Context/session-handoffs/2026-09-01-self-serve-kids-party-avery-blindness.md); design record (email proof) artifact `65dea992`. **The finding: `avery_event.tprs_booking_id` is a single point of failure** — WF2's whole booking lateral hangs off it, and with it NULL Avery loses paid-tier language, the money line, and `_bkInviteClause`, which ALREADY contains the entire invitation/RSVP curriculum ("Invitation, event-dashboard, or RSVP questions are NOT booking changes"). The curriculum was never missing; the valve was stuck. **Root cause: `findOrCreateCustomerAtCheckout` matches on EMAIL ONLY while WF1 looks up an SMS lead by PHONE**, so Olga Mogildea (INV-2026-00512) had two `customers` rows with an identical phone — one with email NULL — and `ae.contact_id = b.customer_id` could never match (exec 24859: `Resolve Missive Convo` 0 rows, `Ack Context` 0 rows). **SHIPPED:** both nodes now match phone-last-10 OR email (the `reconcile_avery_conversation_ids()` predicate), with **asymmetric guards by stakes** — the note surface has NO date requirement, the writeback KEEPS a strict `event_date` guard because it decides what Avery *says*; **`Ack Context` deliberately untouched** (widening it is the Dina Hejja wrong-date ack), so zero new guest messages. Verified pre-apply via Supabase MCP: predicate `old_match=false/new_match=true` on the real rows, `EXPLAIN` writeback index-backed cost ~22, note query seq-scan is pre-existing not a regression. **The cron already self-heals the link in ~30 min — what does NOT self-heal is the Missive note**, so that (plus the blind window at the moment of booking) is what the fix actually buys. **Also proven: the invitation minted 2 SECONDS after payment and the "Your event page is ready" block DID render in her receipt** — the standalone email is a prominence fix, not a delivery gap. Olga was emailed by hand (Gmail, "Lavinia's party page is ready"). **NOT BUILT: (B)** kids facts into `Lookup Event` + `_bk`; **(C)** the T+10 email as a real template — `email_type` is a **Postgres enum**, so it needs a migration before merge; timing rides the existing `next_attempt_at` column (no cron, no new table). **⚠️ OPEN RULING:** linking makes these bookings eligible for the day-after check-in and the ~275-day rebook invite (`booked_shape='occasion'`) — but that is ALREADY true via the cron, so the question is whether to *gate them out*. **FUNNEL CHECKED 9/2 and the recommendation is STOP HERE:** TPRS has 3 kids parties all-time (1/mo) and NO Roller history (earliest event 2026-06-15); the "52 kids inquiries" number is the INQUIRY type, not the product - split by size it is 24 kids-package-eligible (3 booked) vs 28 too-big-for-the-cap (2 booked), and the second group is the catered leak the event builder already targets. So **B and C are NOT worth their cost at this volume** (C buys prominence on a link the receipt already carries, for a Postgres enum migration + Render deploy). Only cheap option left: a template-only hoist of the invitation block ABOVE the receipt table in `fully-paid-confirmation.eta`. Also DROPPED: ungating `isDeposit` on the Avery ack hub link - `Ack Context` needs `tprs_booking_id` already set while the writeback runs in PARALLEL, so it is near-dead code for self-books. Memory: [[avery-tprs-customer-row-split]].
- **2026-09-01 — EVENT BUILDER: first end-to-end Send completed, WF2 hardened, crossroads reply live, VIP re-banding + availability + headcount-framing RULED, Sonnet congruence audit run. Handoff of record: [session-handoffs/2026-09-01-builder-first-send-crossroads-audit.md](Context/session-handoffs/2026-09-01-builder-first-send-crossroads-audit.md) — READ IT FIRST; plan of record `Marketing Avery/builder-rollout-review-2026-09-01.md` §6+§6a; rulings in `zite-event-builder-brief-2026-08-30.md` §2.2c–2.2e/§2.3a/§2.3c–e and `vip-capacity-rebanding-spec-2026-09-01.md`; audit synthesis `Marketing Avery/audit-2026-09/CONGRUENCE-REPORT.md` + generated `AVERY-SYSTEM-MAP.md`.** State at close: Builder Submit DISARMED (`15eb74b2`, now captures `builder_payload._prior` for the headcount-shrink check); WF2 `cad4dd42` (8 deploys tonight, every one staged on a clone via `brain/deploy/stage-on-clone.mjs` first, drift clean; last = crossroads states availability when the same-turn TPRS check says open); **live KB untouched — the BUILDER-WORLD KB DRAFT lives in Jon's cloned Google Doc (`KB_DRAFT_DOC_ID` in `.env`; `deploy-kb.mjs --doc draft`), carries Section 21 + 21.2b + three trim passes, and is 303.6k chars — UNDER live (307k); five `[DEFAULT APPLIED - Jon to confirm]` tags await him (see the handoff's late-night addendum);** Website `/api/estimate/menu/` publishes `rules` (49/60/80, unchanged values) which Zite's v3 build now reads; **golden suite retired as a gate (Jon).** Shipped: retry path + `quote_confirmation_skipped` builder-aware, 5 menu-path plumbing guards, quote params PINNED to the submission (page $2,450 vs Avery $2,285 = 11 arcade cards lost to bare slugs), pin ordered above the delivery-plan auto-fix, the CROSSROADS reply (opener + "Your total came in at about $X ($Y deposit). Any questions for me? If you're ready, I can send the deposit link over - heads up, they're valid for 48 hours."; no refundable line, no upsell), stacked 12-hour staff notes. Rulings: VIP 6 lanes to 60 everyone / 61–75 COMPANY ONLY autonomous / 3h NEVER required (suggested from 43) / page type-blind to 75, WF2 sorts at Send; availability = recourse mandatory, Tier 1 day-level closures from TPRS then Tier 2 alternatives; refund = answer never announcement; say "seven days" to guests, day 6 internal; "I just sent it" = hold + timer + NA (Collision Gate CONFIRMED to eat the builder quote); WF1 first touch = greet + ONE routing question, NO link (REVERSED later that night: a linked first message that gets silence is unreadable - spam vs unseen vs objection; the link lands on WF2 turn 1 after the reply); one big light switch (KB deploy + WF1/WF2 turn-1 flip + link constants + arm + proxy constants). Audit's must-fix before B1: `Set Route` `inZone <= 60` company branch (not in the spec!), Engagement Nudge copy-variant split; before the flip: `Queue API Failure` webhook-agnostic + replay routing, collision/hold, WF1 kids-routing guard. Jon's next action: clone the live KB Google Doc → "Avery KB — BUILDER DRAFT" and send the URL. Memories: [[vip-capacity-rebanding-2026-09-01]], [[wf2-menu-path-landmines-and-clone-staging]], [[avery-builder-congruence-audit-2026-09-01]], [[tprs-engine-season-gap]] (P0 before Nov 1, unrelated to the builder).
- **2026-08-31 (evening) — Two guest-losing Avery threads traced, and the CATERED CANCELLATION TERMS ruled and shipped end to end (cross-repo: Marketing Avery brain+KB, tprs #143 MERGED + #144 pending, Website FAQ).** Memories of record: [[avery-bare-hour-and-range-parsing]] and [[tprs-catered-cancellation-terms]] — read both before touching bare-hour/range time parsing, `unnecessary_3hr_mention`, the cancellation/refund KB rules, `booking_summary.is_catered`, or the pay page's cancellation line. **Thread 1 (Nick Blazen, E-2198627):** asked "start at 10 and go until 1?" on a Saturday — bookable — and was told "we open at 11am Sat, so latest start is 8pm" then "events run as a 2-hour block"; he left. Avery's FIRST drafts were right both turns and `unnecessary_3hr_mention` deleted them (validator-forced-defect instances 10-11). Shipped (WF2 `8c504fa7`→`e6049c3d`): bare hours resolve via the inquiry form's time-of-day window first; a RANGE-FIRST pass (the audit found "6 to 10pm" was taking the END as the start, which also silently defeated the seasonal blocked-slot gate; "9 to midnight" set the start to midnight; "5 to 8"/"8-11" parsed as nothing); `guest_requested_span_hours` drives latest_start/past_closing pre-duration and exempts the 3hr check; `latest_start_3hr`; run-to-close policy in prompt+KB (owner ruling: events may END AT close — Fri/Sat 1am, Sun-Thu 10pm; opening time NEVER caps a late start); `timing_refusal_flagged` forces escalate:true on a real past-closing/before-opening refusal (Jon: "we either do silent escalations or we respond and flag" — the handoff-language ban + the past-closing rewrite keep Avery speaking as herself on those turns). Harness `test-late-night-span.mjs` 73 asserts, 52 red vs prior. **Thread 2 (Alexis Bonavota, E-7166869, $3,560 quote):** asked the refund policy as her LAST question before a $1,780 deposit and got silence — `checks_passed: true`; she was obeying the blanket "cancellation/refund question" silent-escalation trigger, on an event that WASN'T BOOKED YET, while TPRS printed a 72h policy on the very pay page her link would open. **The 72h line was a straggler from before the 2026-07-18 day-6 alignment** (humanize.ts's own comment). **Jon's terms, CATERED ONLY, iterated live: full refund (not credit — rejected; not food-order-hinged — rejected; NO short-lead carve-out — rejected, "it's simple") until FINAL PAYMENT is collected (asked day 7 with day 6 the printed backstop; the boundary is the PAYMENT, not a date), final after. Lane reservations keep 72h.** Shipped: tprs #143 (`booking_summary.is_catered` from CATERING_PACKAGE_CODES — same test as the Event Hub gate; pay.eta branches deposit/final; deposit-link email both shapes, deposit-paid confirmation, balance-link email, host dashboard via `v.shape==='catered'` — email plumbing = `BookingEmailContext.isCatered`, deliberately NOT `hasCatering` which is true for any f&b line); Website `/faq` two entries (count-drops listed first) + FAQPage schema (`fec41bc`); KB scoped triggers (policy question pre-booking = ANSWER; actual request or existing booking = escalate, paid-tier outranks) + the terms, deployed minutes after Jon merged #143. **Guard `test-kb-cancellation-policy.mjs` (18 asserts) reads pay.eta from ORIGIN/MAIN via git show (what Render runs, not the checked-out branch) and holds KB↔pay-page in lockstep.** **#144 MERGED same night: balance-link email is form-aware for catered (closes the fp-link session's SIDE-FIND above - that debt is PAID, ignore the older bullet's "unfixed" note).** Also fixed: `test-fp-link-send.mjs` sliced UTC ISO dates — red every evening 7pm-midnight CT with no code change; fixtures now use local date parts. ALL FOUR OPENS CLOSED SAME NIGHT (Jon: no counsel, "pluck the low-hanging fruit ... call it a day"): QUOTE LINE live (Format Quote `e6049c3d`→`5f516828` via `patch-wf2-quote-refundable-line.mjs` - every deposit-flow quote says "refundable in full until your final payment"; short-lead/bowling renders untouched); RESCHEDULING = free before final payment subject to availability (printed /faq + /terms; Avery may STATE it, the move stays Section 15 human-only; deliberately NO printed after-payment policy - staff discretion); KIDS = the 72h rule ratified (/birthday-parties Q&A); /TERMS Section 10 written IN-HOUSE with a discretion clause (courtesy refunds never contradict the printed rule) - counsel pass explicitly declined. Terms doc of record: artifact `31a940f6`. SAME NIGHT, fourth validator incident (Le'royia Campbell E-7738825, exec 24403): "they pay for their own food" missed the `pre_pushback_rationale_leak` exemption BY ONE PRONOUN and that failure had NO build-retry-request entry - guaranteed exhaustion, NA in 80s on a correct draft. Fixed (WF2 `e864ba51`): concept-wide exemption, first-ever retry instruction, and the KB pushback play now LEADS WITH THE PARTY ANGLE on birthday-type events (cake/presents/decor come only with an event package - Jon: "the birthday IS the vantage point"). Harness `test-pushback-rationale.mjs` (19 asserts) includes an INTEGRATION case running the KB's worked answer through EVERY check - it caught the exemplar at 306 chars (6 over the SMS cap) pre-deploy. **STANDING LESSON: a new deterministic check without a build-retry-request entry is a guaranteed-exhaustion trap - grep the retry prompt for the failure name before shipping any check.** WATCH: first catered deposit email, first quote with the refundable line, first Avery cancellation-policy answer, first birthday pay-own-food pushback.
- **2026-08-31 — Avery sends the FINAL PAYMENT link herself on an unchanged-count headcount confirm (ALL LIVE, cross-repo: Marketing Avery WF2 + Shanna-nudge workflow + KB + Supabase).** Jon's ruling from the Lucy Rosario review (E-7709683: Avery promised "I'll get your payment link over to you" — only staff could — and said "due 6 days before" when the deadline was tomorrow). **Spec of record: `Marketing Avery/fp-link-autonomy-spec-2026-08-31.md`; build record: WF2.changelog 2026-08-31 entry — read both before touching the `send_fp_link` route, `fp_link_url`, the Shanna nudge's note branches, or the `_termsLine` paid-tier carve-out.** Architecture = book-event's deterministic-append: the LLM only signals `fp_send` with a bare ack; Set Route re-verifies eligibility (deposit_paid + PartiallyPaid + hc warn9/ask7_sent + no link yet + lead 0–9d); the route reads the live balance (GET /api/avery/bookings/:id), mints `avery_final_payment`, writes `fp_link_url`+`fp_link_sent_at`+`hc_status='resolved'` PRE-send, and composes the money line with a CONCRETE due date. Failure → ack alone + post-send NA. **Load-bearing choices: `fp_status` is NEVER written (scratch column with two clobber paths); `booking_status` never → `final_payment_due` and the "Final Payment Link Sent" label never applied (either arms WF5's dormant FP-chase ladder against the 8/8 no-friendly-chases ruling).** Shanna's day-6 nudge = non-responder backstop (third note branch off `fp_link_sent_at`; a paid guest drops out of its query — the closed-loop win). **Count CHANGES stay staff/NA PERMANENTLY (phase 2 designed, sized, REJECTED — don't re-pitch); link-in-ask7-template also declined** (count-before-money ordering + static templates bypass checks). KB: NN19 exception + §15 HC hook split. Harness `test-fp-link-send.mjs` 35/35, 6 red vs HEAD. Rollback: WF2 `7d93c582`→`157a4c92`→`ee8bb8ef`→`8c504fa7` (form-aware wording); Shanna `87a82dea`→`424320e3`. **Delivery wording is FORM-AWARE** (Jon's same-day note): all four catered packages gate the pay page behind their food form (8/8 Event Hub gate), so catered rows get "lock in your food selections and take care of that balance ... two quick steps"; branch keys on food_package PRESENCE, deliberately not a third copy of the gated-package set; the three FP code-node bodies redeploy via `add-fp-link-nodes.mjs --update-code` (installer = the one source of truth). **SIDE-FIND, unfixed: tprs `balance-payment-link.eta` (staff-sent friendly balance email) still says "Pay balance / once it's paid you're all set" with no form mention — copy tweak next time that template is touched.** **WATCH: first real ask7 confirm (WF5 cron 11am CT); a count-change reply (must look like today: ack + NA); first Avery-sent-link day-6 Shanna note.** Same session: the VIP 60-min front-buffer question (Terrell Davis, package-aware turnover) was investigated and PARKED by Jon — knob is `products.front_buffer_minutes` on codes 9/10, availability is package-blind by design, don't build without a new ruling. Memory: [[avery-headcount-payment-cadence]].
- **2026-08-30/31 — Avery double-booked a guest against her own booking, then told her the slot was taken (Betzaida, E-2717737). BOTH HALVES NOW LIVE — tprs #139 merged 8/31 17:35Z (`8e20e7b`), migration 0161 applied in prod (column + partial index verified, ledger 161 = journal 161, /health 200).** Full narrative + rulings: memory [[avery-book-event-idempotency]]; WF2 changelog entry 2026-08-30. Two WF2 executions overlapped on one conversation: exec 23946 read `tprs_booking_id` empty, booked **INV-2026-00505** at 20:46:54 with a live link, and then had its reply **suppressed by `Pre-Send Freshness Gate`** (a newer guest message had landed) — so she held a real booking, a 48h clock and no link. Exec 23948 POSTed book-event again 90s later and TPRS refused it 409 `capacity_unavailable` *"Conflicts with 1 existing booking: INV-2026-00505 (**Betzaida Betzaida**)"* → Avery: *"that exact time may have just been claimed while we were talking."* **The shape: WF2 decides at T and speaks at T+150–540s (the humanize wait in `Quiet Hours Gate`; email is 360–540s) and EVERY guard runs at T** — the 2026-08-12 live-booking hard block in `Set Route` fired 99 seconds *before* the booking existed. So exactly-once cannot live in WF2. **Jon's reframe, which is the whole fix: an existing booking for THIS event is context, not a conflict** — blocking harder was rejected (the hard block's canned reply carries no link and raises Needs Attention). **LIVE (WF2 `8ef60750`→`7d93c582`, `patch-wf2-book-event-idempotency.mjs`, Marketing Avery `f6b81d0`):** `Build Book-Event Request` sends `avery_event_id`; `Build Link Reply` gains a `deduplicated` branch (**required** — the standard line promises "valid for 48 hours" and a replay reuses a token that may have hours left); **`Pre-Send Freshness Gate` never drops `book201_this_turn === true`** (this is the fix that cost her the link, and it works independently of TPRS); `Build Booking Conflict` detects self-collision by booking reference OR the guest's first name in TPRS's parentheses (name-matching is loose on purpose — a false positive can only downgrade to the neutral copy, which is the 2026-08-18 fail-toward-vague rule's own direction). **PENDING: tprs PR #139** (migration 0161 `bookings.avery_event_id` + PARTIAL unique index over live statuses; event-grained never conversation-grained; replay REUSES the live link and omits `quote_summary` because reproducing it = re-pricing an in-flight booking) — merged and deployed 8/31. **Trap for future sessions: `gh pr merge` is blocked by the Claude Code auto-mode classifier, so a human merges tprs PRs unless a `Bash(gh pr merge:*)` permission rule is added to settings.** New tooling: `patch-*.mjs --emit` writes patched node source to `brain/deploy/.emit/` so `brain/checks/test-book-event-idempotency.mjs` (37 assertions, 11 red against HEAD) tests the exact source that ships. **STILL OPEN, same defect on a different field: the ESCALATION send path has NO freshness gate and NO pre-send NA check at all** (`Build Escalation Note 2 → Build Escalation Reply → Rewrite URLs (Escalation) → Send Escalation Reply`) — same thread 20 min later, Avery sent *"Let me double-check that deposit on my side"* + Needs Attention + AI pause **two minutes after** the TPRS Outbox Listener had already confirmed the deposit; nothing arrived from the guest in that window, so message-freshness could never catch it — **what went stale was STATE**. Needs a pre-send state re-read (3 nodes + a rewire). Also parked: moving the humanize wait AHEAD of the state read and LLM call (a rewire, not new logic — collapses the window to ~40s).
- **2026-08-29/30 — Marketing research refresh + the 60+ pipeline finding + Zite EVENT BUILDER brief — PROXY BUILT 8/30 evening, needs `GAS_PRICING_URL` on Vercel.** Refresh report of record: `Twisted Pin Full System/Reference/marketing_refresh_2026-08-29.md` (artifact published; memory [[marketing-refresh-2026-08-29]]). The one finding that matters: since Jun 1, 36 inquiries at 60+ guests → 1 quote → 0 bookings (30–59 band: 61 → 15 → 8); ten Q4 company rows at 60–200 heads sit in `gathering_info`, all parked at the "pick a food package" step, 13 of 15 with no human message — ~$48k of quotable Q4 revenue converting at 0%. Root cause is the missing NUMBER, not speed (Avery is first but withholds price until food is chosen; the old Heyflow inquiry had a calculator that qualified guests BEFORE the inquiry). Direction chosen with Jon: an event builder on the existing Zite menu build at `/build` — date+time+guests+food required, Calculate → estimate from OUR engine via a Vercel proxy `/api/estimate/` (GAS batch {vip,trad}×{2h,3h} in one call; never call GAS from the browser), Send to Avery in three modes (`eid` / `via=sms` / contact step → WF1), availability NOT in v1, 60+ shows "from about $X". Brief + paste-ready Zite prompt + our-side spec + 10 open decisions: `Marketing Avery/zite-event-builder-brief-2026-08-30.md` — **read it before building the proxy, `builder-submit`, or touching the menu-link KB lines.** **Built 8/30 evening (Website):** `/api/estimate/` + `/api/estimate/menu/` (`src/pages/api/estimate/`, `src/lib/estimate/*`, `scripts/check-estimate.mjs` in prebuild) — live-verified against the deployed engine (lines sum to the engine total; P&P gate mirror = engine flag; rates read from the engine). Labels are space-first with NO lane count (Jon 8/30; `LANE_COUNT_IN_LABEL` in shape.ts). **8/31: LIVE end to end** — `GAS_PRICING_URL` set on Vercel, engine parity verified to the dollar on the published page (neighbors:null crash fixed `5edd622` — see [[contract-deviation-must-hold-shape]]); design converged over ~9 Zite rounds (venue voice everywhere, accordion dead, expanded-card + tonal steppers; spec of record `Marketing Avery/zite-builder-design-spec-2026-08-31.md`); Jon greenlit moving forward. **Baseline snapshot taken before ANY brain/KB/n8n builder edits: marketing-avery tag `pre-builder-baseline-2026-08-31`** (all 55 workflows via new `brain/deploy/snapshot.mjs`, KB git==live, drift-clean; raw exports w/ secrets stay gitignored). ⚠️ Found en route: the PRODUCTION Anthropic key is hardcoded in 5 workflows and sits in marketing-avery git history (`c5f811f`) — rotation owed, see [[avery-anthropic-key-in-git-history]]. Inquiry-form "When" block simplification = post-widen only (brief §2.3a). **STAGE A PLUMBING BUILT + DEPLOYED DORMANT (8/31 late):** `avery_event.builder_payload` in prod; `Avery — Builder Submit` (`bbIbkgJegEqz5x3P`, INACTIVE, installer `brain/deploy/create-builder-submit.mjs`); WF2 builder turn live-dormant (`7bbf8054` — trigger allowlist, availability on the menu path, 3 Load Context landmines defused, channel-aware ctx, builder prompt branch); harness 57 asserts red-vs-HEAD; golden's tiffany case is red on a pre-existing date re-bomb (bump it). WF1 bare-mode ALSO built dormant (84fc90f2 — body.builder on the inquiry webhook, BUILDER MODE first touch, fire-WF2-after-send no-wait; harness 78 asserts). Not built: KB edits (Part-2 gate). Test lap = one command: `brain/deploy/test-builder-send.mjs --arm/--disarm` (preflight green). Next: supervised test, KB-dive rulings doc. Tiffany golden red = pre-existing (model quote_needed on restate — needs its own look). Memory: [[zite-event-builder-brief]].

- **2026-08-29 — Avery fabricated availability (Robert Drobny E-0123702) + Sarah Asa NA: forensic review, 5 brain nodes + KB + one wiring edit ALL LIVE (cross-repo: Marketing Avery + n8n).** Chronology + watch list: [session-handoffs/2026-08-29-avery-fabricated-availability-drobny.md](Context/session-handoffs/2026-08-29-avery-fabricated-availability-drobny.md); 8-defect review of record with fix classification: `Marketing Avery/avery-drobny-review-2026-08-29.md` — **read it before touching `blocked_slot_not_addressed`, the weekday binder, the VIP band checks, or the blocked CALENDAR branch.** Headlines: ONE real TPRS check (Sep 19 3pm VIP, blocked) became three "already spoken for" answers about three dates (**Sep 12 had zero VIP holds**); Avery's first draft was correct on 4 of 6 turns and `blocked_slot_not_addressed` + its retry rule FORCED the fabrication (validator-authored, [[avery-validator-can-force-the-defect]] instances 6-9); false DAY/DATE MISMATCH on "I can move into a Sunday" (offer scored as claim); "5 per lane vs 6 in VIP" (6 is the self-book cap, catered VIP ~8); `vip_lane_capitulation` eating "if you want" (`really` was optional — that was the Sarah Asa NA). Shipped: CALENDAR line names its slot + "APPLIES TO NO OTHER DATE"; stand-down when the guest moved off the checked date (both check files); `suppressed:hypothetical_weekday`; `really` mandatory + Traditional-pivot skip; per-lane numbers removed; thread_summary availability rule 5a; alt-lane result now PERSISTS (`patch-wf2-alt-availability-persist.mjs` — n8n-mcp addConnection fails on this workflow, use the script). **Jon's ruling: Traditional quoting ceiling = 8 (engine authoritative)** — KB was at 10/11+ with no changelog; guard `brain/checks/test-kb-engine-lane-policy.mjs`. Four new offline harnesses (56 assertions, every one red against HEAD). Golden suite skipped live (Jon: cost). Rollback: WF2 `6a733c27` → `27bdc607` (brain) → `8ef60750` (wiring). Jon handled Robert + Sarah by hand; Avery stays off E-0123702. Memory: [[avery-blocked-slot-forced-fabrication]], [[avery-vip-capitulation-if-you-want]], [[avery-vip-per-lane-capacity-8-not-6]].

- **2026-08-28 — Ads check-in (Aug 14–28) + Script 18 LIVE + first attribution read + SiteGuru / alcohol-policy triage.** Full narrative + rulings + open items: [session-handoffs/2026-08-28-ads-checkin-script18-attribution-siteguru.md](Context/session-handoffs/2026-08-28-ads-checkin-script18-attribution-siteguru.md) — **read it before the ~Sept 14 Ads check-in.** Headlines: 9 real gclid inquiries (5 company events, two 100-head Oct 20 / Dec 12) vs 3 counted; **Script 18 ran live** — Aurora penalties removed on both campaigns (Jon: removal only, never a boost), Romeoville boost moved OP→Events, Shorewood boost removed, 30 negatives (`gift` singular — negatives don't stem); **Naperville/Events HELD by Jon** ("huge possibility, takes time"); **Bar-Led $12→$6 / Events →$41 built but OFF pending ruling**; **calls item CLOSED** — 2 real calls/14d, the July "$1.91/call" note misread the Interactions column. **Attribution lives in `avery_event.attribution_source`** (Jul 1→now: Direct 139/17 booked, Google 40/5, **Meta 9/0 — spend unknown, biggest blind spot**, ChatGPT 5/0; 237 no-attr SMS rows are feedback/rebook, not inquiries). SiteGuru 97% = nothing actionable (fresh PSI: /rewards 99, **/pricing 99 — closes the 5/17 "3.6s LCP" item**); `/es/bowl` KEEP (organic case survives cancelled ads). Google's Sept-30 alcohol-policy email = no US change; 0%-cert/suspension language is Egypt/India/Indonesia only. Memory: [[google-ads-checkin-2026-07-07]].

- **2026-08-28 — Avery takes Thursday profit-share FUNDRAISERS end to end (ALL LIVE, cross-repo: tprs + Marketing Avery WF1/WF2/KB + n8n + Website).** Full narrative, traps, rollback versionIds: [session-handoffs/2026-08-28-avery-fundraiser-autonomy.md](Context/session-handoffs/2026-08-28-avery-fundraiser-autonomy.md); spec of record + rulings: `Marketing Avery/fundraiser-autonomy-spec-2026-08-28.md`; **owner's fiddle guide (symptoms → where to look, knobs, rollback): `Marketing Avery/FUNDRAISER-MAKEOVER.md`** — **read both before touching KB §7, the fundraiser guards in Deterministic Checks / Re-Check, the WF2 fundraiser nodes, or tprs `services/fundraiser.ts`.** Before: one scripted first touch → NA → WF2 forced silence, staff retyping the same explainer in 13 threads. Now: Avery answers from KB §7 (we host / you promote; 5–9; one org per Thursday; nothing reserved; F&B never counts; **arcade cards only at the front desk, never kiosks**; no prizes; checks to organizations only), checks Thursdays through TPRS (**month-anchored, never a year of dates**; bookable = no other fundraiser + not a D202/NYE blackout + ≥10 Traditional lanes free every hour 5–9 with walk-in reserves released), books the $0 placeholder herself on a confirmed OPEN Thursday (`POST /api/avery/book-fundraiser`, advisory-locked per date, asserts the comp path), then gathers payable-to / EIN / mailing address → **every details write lands Needs Attention for human review** (we never pay an individual; the review gate refuses incomplete rows). Chase workflow `Avery - Fundraiser Details Chase` (`Un65e8ncwFlifgZK`, 9/7-day asks + day-6 NA) is ACTIVE (Jon, 8/28). **Invitations went live the same evening** — the confirmation's invitation sentence is a PATCH on the `Fundraiser Booked Reply` node (`patch-wf2-fundraiser-booked-reply.mjs`); re-run it after any edit to that node — see [2026-08-28-invitations-brief-for-fundraiser-session.md](Context/session-handoffs/2026-08-28-invitations-brief-for-fundraiser-session.md). Shipped alongside: the 2026-08-27 LATE SIDE work (Jon / Rachel Smith) found authored-but-undeployed in the same brain files. **Traps:** node names are load-bearing (`Fundraiser Gate` / `Fundraiser State` / `Fundraiser Booked Reply`); Route by Action fallback moved from output 6 to 7; code-2 `default_price` must stay $0; n8n PUT needs `sanitizeSettings`; `tail` masks a failed deploy step's exit code. Memory: [[avery-fundraiser-autonomy-initiative]].

- **2026-08-26 — TPRS activity → loyalty sync LIVE (`Loyalty/db/073-tprs-activity-sync.sql`, applied to prod via two apply_migration steps + one-shot backfill).** Triggered by Michelle Regan (INV-2026-00385) — joined loyalty 10:21, booked online 10:22 with SAVE10 + the SMS opt-in reward stacked, then rescheduled 3× and was refunded; her `signup-10off` grant was still unredeemed because **TPRS never told loyalty anything** (the hook was specced 6/07 in `Loyalty/docs/onboarding-drip.md`, parked "until the DBs converge", and never revisited after they converged in July). Consequence before the fix: online bookers read as non-converters → day-14 "don't forget your $10" nag + day-31 "$20 — we DOUBLED it" on top of a used coupon; blast segments on visit fields couldn't see them. **Built:** `sync_tprs_activity()` on pg_cron every 15 min — (1) each `booking_dates.checked_in_at` on a live booking → `customer_event('visit', source='tprs', source_ref=booking_dates.id)`, db/008 trigger sets `last_visit_at`; (2) SAVE10/SAVE20 `discount_redemption` on a live booking stamps the unredeemed `signup-10off`/`20-off` grant (`redemption_source='tprs_online'`, new enum value; redeemed_at = the TPRS time — bypasses `mark_grant_redeemed` deliberately, which stamps now() and refuses expired grants). Backfill: 77 visits / 7 grants / 0 messages fired; second run 0/0. **Load-bearing facts:** `public.customer` is a VIEW over TPRS `public.customers` — same ids, so the join is `bookings.customer_id`, never phone/email; **visit = check-in, not `completed`** (the completion cron is time-based — no-shows complete too; adoption was 77/77 since 8/12); event type `'visit'` not `'kiosk_checkin'` because points (db/010) and the +2h feedback text (db/043) key on kiosk_checkin only, while db/048 `reactivate_on_visit` does clear `do_not_market` on a TPRS visit (intended). Cancelled/refunded/failed bookings are skipped, so the by-hand release of a refunded guest's TPRS redemption rows (`DELETE FROM discount_redemption WHERE booking_id=…` — TPRS never releases them on refund by design, ADR-0028 §8e, and there is no admin button) keeps her loyalty grant unredeemed too. Michelle's rows released + note on booking; she can re-use both $10s online. **Open:** Zite admin should render `source='tprs'` visits in activity history (cosmetic); watch the first cron ticks; Stripe `refund_state` on her payment record still read `none` at cleanup time (likely webhook lag). Brief for Zite: `Loyalty/docs/brief-tprs-visit-and-redemption-sync.md`. **Same session, `Loyalty/db/074`:** the kiosk +2h feedback text now DEFERS TO AVERY for online bookers (Jon: "she does it better") — a same-day live `self_service` booking makes visit-1 send the onboarding line without "How'd we do?" and visit-3 send nothing; the gate reads `bookings` directly (no sync dependency); replay over 252 kiosk check-ins since 8/12 suppressed exactly the 5 known double-asks. **`Loyalty/db/075`:** gate widened to `avery_event` too (Jon: Avery does a next-day post-event follow-up); event hosts' front-desk check-ins still count as loyalty visits — tracking only, nothing texts off a `visit` event, so no need to exclude events from `last_visit_at`; same migration fixed the `20-off` offer's `fine_print` to carry the SAVE20 online path (049's never-actioned NOTE). **Rulings 8/26:** online visits earn NO points (fine as-is); SAVE10 not being tied to membership online is FINE — the $10 is redeemable in store without a booking, so those redemptions aren't a leak. Don't re-raise either. Memory: [[tprs-loyalty-activity-sync]].

- **2026-08-25 — Zite inquiry form "When" block + Avery reads it end to end (ALL LIVE, cross-repo: Zite + Marketing Avery WF1/WF2/KB + Supabase).** Full narrative, rulings, traps, and the bucket-change recipe: [session-handoffs/2026-08-25-zite-when-block-vague-inquiries.md](Context/session-handoffs/2026-08-25-zite-when-block-vague-inquiries.md) — **read it before changing the form's time-of-day buckets, WF1's date parsing, or WF2's EARLIEST START HELPER / TIME WINDOW rules.** Triggered by a corporate lead (Barbara, E-3921018) who wouldn't give a date or time and demanded pricing first; Avery's behavior was correct throughout — the form was the cause (date was a hint in a textarea, time of day never asked). Shipped: form gains an optional date picker (iOS `min` guard + weekday echo *"That's a Saturday."*), a REQUIRED time-of-day radio (Before 2 PM / Afternoon 2–5 / Early Evening 5–8 / Later Evening 8–11; no "flexible" option by ruling; "Before 2 PM" added late-day for Sat/Sun midday with no Sat/Sun hint on purpose), "What's the occasion?", Group Type subtitles; WF1 reads `eventDate`/`timeOfDay` (structured date outranks prose; weekday-mismatch play fires on a KNOWN date; window never becomes `start_time`); `avery_event.preferred_time_of_day` + Create Event / Prep Event Update / Load Context plumbing; WF2 `time_window_anchor` = max(window start, open+30m) replaces `earliest_event_start` for form guests. Verified live on E-0068352 (turn two: 7pm + menu, never "as early as 11:30am"). **Rulings:** per-person budget anchor stays STAFF-ONLY (Jon's `$100/head` to Barbara was a staff shortcut Avery may not take — declined, don't re-pitch); the window ANCHORS suggestions, never GATES the guest; **"slot" and "window" are banned to guests** and *"easy to move/shift later"* is retired — approved phrasing *"we can change the time later if you want"* / *"or should I plan for a bit later?"* (KB jargon bullet + 4 prompt surfaces). **Traps:** the n8n API key is a 30-DAY JWT living in `.env` AND the canary credential (expired 8/25 ~03:00Z, renewed as `brain-deploy2`, next ~9/24); the slug→hours map lives in TWO files (WF1 `TIME_OF_DAY_MAP`, WF2 `TIME_OF_DAY_WINDOWS`); a test date ≤3 days out trips short-notice escalate. **Open:** Jon's 60+ ruling (`KB:588/621` silent-escalate vs `KB:660` quote-with-handoff — Animal Medical Center, 70, sits on it); `KB:649` has no *"what dates are open?"* analogue; optional first-touch pick-and-proceed; three Marketing Avery commits (`4c0c9eb`, `5d5320f`, `0a8c544`) unpushed. **Late addition (handoff §7b):** the menu page's desktop no-eid "works best on your phone" overlay is a dead end (Christine Gallagher, E-0645923 — then asked for email, and Avery's summary promised an email nothing can send; staff email the menu by hand w/ `?eid=`). Jon REJECTED eid-on-SMS-links (would move every phone submission to web POST; shared links could submit on the inquirer's event) and sent Zite a browse-mode-instead-of-overlay change. Unbuilt: an "I'll email that" guard; mid-thread email-out; notes-based channel override. Memory: [[feedback-slot-window-banned-wording]], [[avery-per-person-anchor-staff-only]], [[n8n-api-key-30-day-expiry]].

- **2026-08-21 → 08-28 — In-house invitations/RSVP product: LIVE FOR REAL GUESTS. Maintenance manual of record: [session-handoffs/2026-08-28-invitations-product-handoff.md](Context/session-handoffs/2026-08-28-invitations-product-handoff.md) — file map, 13 invariants, rulings, symptom→check table, runbooks (add a look, re-render previews, KB/prompt deploy, env), watch items. READ IT before touching invitations; the paragraph below is the chronology.** Original: (PRs #96 + #97, migrations 0148–0150). Free evite replacement for event hosts (Jon: *"definitively not to make money — it makes TPRS better"*; corporate first, kids second, fundraisers as a staffing signal). **The load-bearing ruling: money is a property of the SURFACE, not the viewer** — `event_invitation` carries two tokens (`/m/host/:token` = booker's dashboard, money renders; `/i/:token` = guest page whose `GuestInvitationView` has NO money field at all, so a leak has nowhere to land). RSVP rulings implemented: coming/can't-make-it (no maybe) **with party size** (headcount = SUM(party_size), unit by shape — kids count KIDS cap 6, fundraiser people cap 12, catered pinned to 1); **fundraisers accept RSVPs to event END**, others close at START; **accept-with-alert after final payment** (fully-paid CATERED + head-adding yes → sticky `late_addition` + host banner + internal_notes line + one `admin.invitation_late_rsvp` email keyed to the false→true transition — catered-only because kids parties pay 100% at booking and would flag every child). Edit story = normalized-name upsert (same name, any device, newer answer wins — no accounts/cookies). Guest emails are reminder-only PII **the host never sees**; blank-on-edit clears. Admin: "Invitation & RSVPs" card on booking detail (idempotent mint; revoke→re-mint rotates both tokens = leaked-link recovery); dev preview at `/admin/dev/invitation-preview` (14 states, view models imported so projection drift breaks compile). **Nothing guest-visible until staff press Create — zero invitations exist in prod.** **Pre-test slice shipped 2026-08-22 (PRs #98 + #99):** staff can NAME events (title/host fields on the admin card; edits never rotate links; hidden `intent` field stops stale mint-form presses from nulling saved titles), add-to-calendar on the guest page (Google URL + token-scoped `.ics`), and the **purge sweep is REAL** (`workers/invitee-purge-cron.ts` — deletes guest rows 7 days past event END, runs once daily in the 4am CT hour per Jon's "hourly is overkill"). An adversarial-review workflow on that diff confirmed 4 defects pre-merge, incl. a broken worker-count test my piped `vitest | tail` had masked — see memory [[piped-test-exit-codes-lie]]. **2026-08-23 UPDATE — GUEST EMAIL RAIL LIVE (PR #104, migration 0152) + host self-serve slice (PR pending):** separate Resend team (Transactional Pro, domain `invites.twistedpin.com`, DNS in Vercel, verified; `INVITATIONS_RESEND_API_KEY` + `INVITATIONS_EMAIL_ALLOWLIST` set on Render — allowlist = in-house test mode, non-approved recipients get a terminal readable suppression). First live confirmation email delivered 8/23 (Jon, jdow800@gmail.com). Gmail's "Be careful" banner on it = Workspace same-display-name heuristic on a zero-history domain, NOT an auth failure — SPF/DKIM pass and apex DMARC `sp=quarantine` covers the subdomain; guests won't see the org-impersonation banner. Rulings shipped same day: **email REQUIRED on coming answers** (optional on declines — nothing to send a no), label "Your email"; **pretty HOST urls** (slug + 12-char tail, 72-bit — money renders, so longer than the guest 8; legacy 22-char resolve via `hostTokenCandidates`); **hosts self-serve title/host-name on their dashboard** (POST `/m/host/:token/details`, shared `cleanInvitationDetail`, edits never rotate tokens; staff card = override only — "I don't envision us really coming in here"); **manage-RSVP page** replaces one-tap decline (GET is READ-ONLY — mail-scanner prefetch could silently cancel RSVPs; explicit "Confirm — I can't make it" + party resize through `submitRsvp`, so the late-addition machinery applies on every door); RSVP count rider in the admin header strip next to GUESTS; guest-cancel alerts NO ONE by design (host sees the count move; "we care about a final number, not that George isn't coming"). Fundraisers mint at CREATION regardless of deposit-vs-full-pay setup (Jon's 8/23 note — verified in `staff-direct-booking.ts` onlyShapes). **2026-08-28:** Jon's full test lap on a fake fundraiser PASSED (confirm-decline, resize, self-serve details, reschedule self-heal). Shipped same day: details-section ATTENTION STATE (red "(please fill this out if using invitations)" + breathing border, only while both fields blank — #108); **refund double-submit fix (#109)** — his cancel emailed guest + info@ TWICE (two requests 1.2s apart; `executeStaffRefund` had no row lock/status check) → `FOR UPDATE` + `RefundBookingAlreadyCancelledError` on cancelling modes (`partial` stays open) + form submit disable; **cancel/reschedule GUEST FAN-OUT LIVE (#110)** — `fanOutInvitationChange` hooked INSIDE `sendCancellationNotification`/`sendRescheduleNotification` (every door by construction; SAVEPOINT fail-open; env-read rail gate mirroring start.ts): coming-with-email invitees get `guest.event_cancelled` / `guest.event_rescheduled` (fresh .ics, same UID). **Go-live switch for guest email = delete `INVITATIONS_EMAIL_ALLOWLIST` on Render** (test-mode only, never a live feature). **Day-of reminder cron LIVE (#111, migration 0155 `event_invitee.reminder_sent_at`)** — `workers/invitation-reminder-cron.ts`: due = start − 4.5h; if that lands before 9am CT → 6pm CT the calendar day BEFORE (Jon's dawn rule; "Today"/"Tomorrow" subject word follows); once-only via guarded stamp UPDATE; reschedule fan-out clears the stamp; guests who RSVP'd after the reminder moment are stamped-not-mailed; mounts iff guest rail key + link base (worker count +1 conditional). **The guest email package is now COMPLETE** (confirmation, reminder, cancel, reschedule). **Confirmation emails carry the dashboard + guest links (#112)** - the go-live flip; auto-mint now runs BEFORE the confirmation send. **Avery curriculum LIVE 8/28** (KB Section 20 subsection + DEPOSIT PAID prompt carve-out; WF5 "HC welcome" is a fixed headcount chase, so the deposit confirmation EMAIL is the pitch vehicle; then SAME EVENING Jon ruled the end state is Avery DELIVERING the hub URL herself (SMS or email, optional framing) - shipped: Lookup Event + Load Context plumbing patched (brain/deploy/patch-wf2-invitation-context.mjs), Pre-Assemble builds ctx.invitation_host_url with TPRS-parity slug rules, EVENT PAGE line shared ONCE on DEPOSIT PAID turns, link_not_from_tprs allowance in both checks; WF2 versionId 7d93bb2f, drift clean - memory [[avery-invitations-rsvp-curriculum]]). WATCH: first real DEPOSIT PAID turn on a booking with an invitation (Justine E-1240170 / Bri E-8556125 already qualify). **Later that night (cc09702): the DEPOSIT ACK (TPRS Outbox Listener) now carries the hub URL with Jon-approved shortened copy (299 chars, 2 segments, deposit-only, final-payment ack link-free), and FUNDRAISERS get theirs appended to the Fundraiser Booked Reply confirmation ("Optional digital invitations for your fundraiser, if you want them: <url>") + a fundraiser-mode EVENT PAGE line; scope = the auto-mint matrix (catered full-pay never, per Jon). Patch scripts: patch-outbox-ack-invitation.mjs, patch-wf2-fundraiser-booked-reply.mjs. Harness 46/46; drift clean. **THEME SYSTEM ROUND 1 LIVE (PR #113, migration 0156):** registry (vibes, not categories; all six approved looks reserved, `available` gates the picker), storage (theme/tagline/note), guest page on a four-part partial contract (venue-owned blocks shared, styled per theme), Classic + Confetti Strike ported, host-dashboard swatch picker with a LIVE iframe preview of the real guest page (`/m/host/:token/preview`, read-only, `?customize=1` outlines the slots) + Customize wording, kids mint as Confetti. **ROUND 2 (PR #116, 8/28 late): ALL SEVEN LOOKS PORTED** (one-night-only, answered-poster, morning-light, letterpress-ceremony, eight-oclock-cover; `available: true` everywhere, dev-preview state per look, every-look sweep test) + the **shape-aware kicker** computed once in `invitation.eta` (`it.kicker` / `it.kickerTail` / `it.kickerShaped`): *"<Host> invited you to a fundraiser"* / *"... a party"* (kids) / plain for catered (Jon: FUNDRAISER was too easy to miss on the ticket tab alone). Cover photo follows the room rule (bar / VIP suite / traditional lanes). Defaults still kids→Confetti, else Classic, pending Jon. **Screenshot trap:** headless Chrome floors the viewport at 500px wide, so `--window-size=390` silently renders at 500 and crops; wrap the page in a 390px iframe to see the real phone layout. **ROUND 3 (migration 0157, same night): HOST PHOTO / LOGO SLOT LIVE** — `POST /m/host/:token/photo` (base64 JSON → existing product-image pipeline, `invitation` kind, PUBLIC bucket) saves on pick from the Customize fold, `/photo/remove` restores the venue photo; rendered by six looks, Answered Poster ignores it by design. **ROUND 4 (same night): guest-list DOWNLOAD `GET /m/host/:token/guests.csv` (name / answer / party size / late) + purge retention 7→14 days past event END.** Jon's rulings: no host "blast" through our platform EVER (abuse + our domain rep; "they can CC ppl"). **Reversed later 8/28 (#127): guest EMAILS ARE in the download**, disclosed on the RSVP form ("shared with your event host, who may reach out"); the dashboard screen still never shows them. Also #125 catered party size 4 (migration 0158), #126 dashboard shows 5 most recent RSVPs + fold, and `/invite-previews/` on twistedpin.com (real templates, 21 static renders, unindexed). **REVIEW HARDENING (#128 + #129, same night):** four-lens adversarial review found and fixed: rate limiters keyed on the LB ip (one global 30/min bucket!), only `cancelled` treated as terminal (refunded/no_show/failed_payment now close + fan out), auto-expiry cancel silent to guests, same-name concurrent INSERT → 500, re-mint not fresh (rows/photo/purge stamp carried), purge one-way door on reschedule, manage-decline bypassing every gate, reminder/reschedule race, no invitee cap (email cannon), photo blobs never deleted, purge leaving guest emails in outbox rows, no decode ceiling, no frame-ancestors. Avery KB/prompt corrected (email contradiction, no-URL branch over-promise, support facts). **OPEN RULINGS:** reactivation after cancel (re-notify vs revoke), package change after mint; **Jon action:** add a delivery webhook on the invites Resend team (code accepts `INVITATIONS_RESEND_WEBHOOK_SECRET` once shipped). **ROUND 5 (same night): og:image on the guest page** (host photo, else the room JPG by shape; `summary_large_image`) + Website `/invite-designs` gallery DELETED (page, public dir, sitemap filter, vercel header, robots line). REMAINING: per-theme RENDERED OG cards (title in the look's type) — hold until Jon picks favorites; per-shape default looks (Jon); confirm-headcount signal (parked); **ALLOWLIST DELETED 8/28 evening — guest email is LIVE for real guests** (SPF/DKIM/DMARC verified aligned; Postmaster Tools optional, monitoring only). Design-phase decisions parked: whether guests see the going-count; whether hosts ever see guest emails. Memory: [[tprs-invitations-rsvp-build]].

- **2026-08-13 — Avery conversation rulings + the staff-link 48h fix (4 shipped, 1 routing change still unbuilt; cross-repo: tprs + Marketing Avery + n8n).** Rulings of record + build log + ordered plan for the remainder: `Marketing Avery/avery-conversation-rulings-2026-08-13.md` — **read it before touching birthday routing, the no-food-package rules, vague-time handling, or the link/digest thresholds.** Triggered by Jon reviewing two live threads (Naz `E-1023544`, 5 guests / 14-year-old, told *"at that age it's a catered event"*; Caleb Meents, 17 guests, offered *"numbers … without a food package"* one paragraph after arguing FOR the package). **Forensics: nothing failed** — every deterministic check passed first-attempt on all three LLM turns. These were owner rulings, not bugs. **SHIPPED:** (1) **tprs #79 `1efa03e`** — staff deposit links honour the ratified **48h** TTL; two call sites (`admin/bookings.ts`, `admin/new-event.ts`) hardcoded `expiresInHours: 72` and outranked the default map for ~3 months (22 of 23 staff tokens at 72h; the Avery path, which omits the override, was correctly 48h). (2) **WF2 `920ece86`** — `no_food_pricing_offered`, a **deliberately UNGATED** text detector + prompt shape rule + retry remediation; nine KB lines and prompt line 591 already banned this and both existing detectors were blind because they key on the *quote fields* (`delivery_plan`, `quote_needed`) while Avery merely OFFERED, in prose, on a non-quote turn. (3) **WF2 `18fb2f2b`** — band-word vague times ("dinner", "evening", "lunch") anchor INSIDE the guest's band instead of at the earliest start; `dinner` appeared zero times in the prompt. (4) **Digest `eVXMYwPzUy3tGNTz`** money clock 48h→**24h** — ⚠️ **coupled to the link TTL with nothing but a comment enforcing it**; at 48h links a 48h window matches every live link from birth and the digest becomes the standing inventory Jon rejected. **STILL UNBUILT: Ruling 1, the birthday headcount floors** (under 8 → lane-first with both prep floors named; 8–14 → quote catered, Pizza & Pop at its 10-person floor; 15+ unchanged). ⚠️ **`brain/wf1/clean-and-flag.js:147` is a hard blocker** — its allowlist has no headcount term, so a correct under-8 self-book answer HARD-FAILS at first touch unless amended in the same edit; and **run the golden suite for that one** (it changes routing, unlike everything above). **THE DURABLE FINDING: 7 cases examined, 7 where the correct rule ALREADY EXISTED and was scoped away from the case that needed it** — five behind a wrong gate, one keyed to vocabulary the model doesn't use (KB:912 says *"never quotes or offers"*; she offered in different words), one over-broad. The KB is not underwritten, it is mis-scoped; grep before authoring, and prefer ungated checks. Memory: [[avery-rules-are-misscoped-not-missing]], [[tprs-staff-deposit-link-48h]].

- **2026-08-12 — Needs-Attention resume safety: ALL LIVE (cross-repo: Marketing Avery n8n+brain + Supabase + tprs doc).** Staff can now REMOVE the NA label at "staff completion" and Avery resumes competently. Full narrative + rulings + watch list: [session-handoffs/2026-08-12-na-resume-safety-build.md](Context/session-handoffs/2026-08-12-na-resume-safety-build.md) — **read it before touching `reconcile_avery_booking_state()` (now heals `pending_deposit` + link columns; write-nothing ambiguity guards), the dollar whitelist (booking + staff-typed figures, guest never), Set Route (live-booking hard block), WF3 case 0 (receipt note), or the Label Hygiene Digest (`eVXMYwPzUy3tGNTz`, money-clock-only after Jon rejected the v1 inventory wall).** Core finding: Avery is FULLY BLIND during NA (Missive-rule level) — no cursor is maintainable; the missing piece was `tprs_booking_id` NULL, healed by the 15-min reconciler. Rulings: label ON = staff own the money clock (reminder `ai_status` clause STAYS; the digest is the countdown); label OFF = Avery owns the cadence; **LLM catch-up + awareness cursor deliberately SHELVED** (recorded in 3 changelogs — don't re-propose without a real bad resume). Trap: the n8n MCP patch matcher is fuzzy (false duplicate matches on similar sentences) — brain nodes deploy via `deploy-n8n.mjs --skip-golden` only. Watch: first real NA removal (receipt note), Ornella E-8243279's link (dies Fri 8/14 9:05pm CT (2026-08-15 02:05Z); digest counts it down from 8/13 4:50am), first money question on a booked row. Memory: [[avery-na-resume-safety-build]].

- **2026-08-11/12 — Front-desk check-in board LIVE IN PROD (tprs PR #78 → main `389edf9` + legend hotfix `7ee03b8`; migration 0138 applied to prod Supabase BEFORE the merge).** Full narrative + maintenance manual: [session-handoffs/2026-08-11-front-desk-checkin-board.md](Context/session-handoffs/2026-08-11-front-desk-checkin-board.md) — **read it before touching `/admin/bookings` list visibility, the check-in/lane routes, `lib/catering-codes.ts` (now TRIPLY load-bearing: form gate + 60-min linger + menu-line bolding), or `views/admin/bookings/list.eta` (Eta traps documented; mixed line endings — prefer Edit over scripted patches).** The board gained an attendance axis (`booking_dates.checked_in_at/by` + free-text `lane_assignment`, 0138): checked-in rows linger 30 min (60 catered) then clear; un-checked-in rows stay until END time with a counting red "Late Xm" (the 60s auto-reload is the ticker), then land in the **Cleared view** UNMARKED — **nothing automatic ever marks a no-show, attendance never touches booking_status, check-in never gates payment, lane is typed free text NEVER a picker (Jon), and the touch-first rail sequencing (big Set-lane target → inert lane + full-width check-in button; lane edits via the Notes-cell "Lane 5 ✎" chip) is UI-ONLY — the endpoint doesn't require a lane.** Shaped by 13 live-demo review rounds with Jon (the demo caught two real defects my HTML-level tests structurally could not: the item cap never collapsing, dead Set-lane taps on late rows). Deploy facts: **Render runs `scripts/migrate.ts` as preDeployCommand** on every tprs deploy; **prod's drizzle ledger UNDERCOUNTED by one** (a past SQL-editor-applied migration skips the ledger — healed 8/12, ledger = journal = 139; hand-applied SQL must insert its ledger row); **no emoji on staff-hardware surfaces** (desk monitor's older emoji font → tofu; legend dots are CSS now). Rollback: tag `pre-checkin-board-snapshot` + code revert (migration additive). **Watch: first busy Saturday — the Cleared count is the adoption meter (rows piling to end-time = staff not tapping check-in).** Parked by Jon: AI lane-assignment suggestions (the typed lane field is quietly accumulating the training data). Memory: [[tprs-front-desk-checkin-build]].

- **2026-08-03/04 — Roller→TPRS port planning + the CONSENT-ENTITLEMENT SPLIT (ALL LIVE, cross-repo: Website + tprs + Loyalty + prod DB + Zite).** Full narrative + open items: [session-handoffs/2026-08-03-roller-ports-consent-entitlement-split.md](Context/session-handoffs/2026-08-03-roller-ports-consent-entitlement-split.md) — **read it before touching the Pin Pass / Jingle Bowl builds, `loyalty_segment_predicate` / windowed programs, the FKB signup form, or the kiosk consent flow.** Headlines: (1) **Jon's ruling: marketing consent gates OUTBOUND MESSAGING ONLY** — never the kiosk, points, redemption, or entitlements; a 15-agent audit traced every violation to ONE line (the predicate's hardcoded send-hygiene seed) → **`Loyalty/db/062`** adds `p_require_messaging_consent DEFAULT true` (default is LOAD-BEARING — send paths byte-identical), mint paths pass false but KEEP `do_not_market` (the `reactivate_on_visit` trigger would otherwise let a redeemed grant silently resurrect marketing). Verified 8/4: 810 blocked FKB members recovered, 0 messages. Severity honestly: ~1 real person harmed (a landline native registrant); the 810 were dormant imports — but the Pin Pass rides this engine at $159.95/head. (2) **`Loyalty/db/063` + Zite published:** STOP-senders no longer re-prompted at the kiosk (`has_explicit_sms_optout`, checked BEFORE the opt-in flag); never-asked imports still prompted; live-tested on Jon's record. (3) **Website `beb968b`:** FKB form consent checkbox now OPTIONAL + landline hard-stop demoted to warning + success panel self-adjusts — sequencing mattered (062 first; the required checkbox had been shielding the bug). (4) **Roller ports:** Pin Pass rail DECIDED = direct Stripe (Option A; runbook `tprs/docs/RUNBOOK-summer-pin-pass-tprs-setup.md`); Jingle Bowl CONFIRMED Dec 2026, config-only NYE-pattern (`RUNBOOK-jingle-bowl-tprs-setup.md`); 3 ack forms built in prod, all unattached. **⏰ Only hard deadline: both Jingle Santa images off `cdn.rollerdigital.com` before Roller dies ~Sep 1.** (5) **8/04–05 EVENING: the transactional audit RAN (53 send paths, 7 wrongly gated, 4 money-at-risk) and the FULL P1–P6 FIX ROLLOUT SHIPPED same night — ledger of record: `Loyalty/docs/consent-fix-ledger-2026-08-04.md` (READ IT before touching any consent gate, WF5 Filter Eligible, the loyalty drain, or `record_sms_consent`).** Headlines: money mail (WF5 hc/fp chases, 22h Link Reminder) ungated from `do_not_market`; paused/bounced+balance-due now ESCALATES to staff instead of silence; `scheduled_message.kind` (064) — staff replies + health alerts pass on deliverability only; **per-number STOP overlays (065): `avery_sms_opt_out`/`loyalty_sms_opt_out`, keyword traffic no longer touches the global flag, START can't manufacture consent, 21610→loyalty overlay, Lane Rebook program-filtered**; bounce = fact-not-consent (tprs `68065f3`) + clear-on-phone-change trigger. LIVE-TESTED on Jon's number (loyalty STOP → staff reply still delivered w/ real SID → per-number isolation held → START revert). **8/05 rulings: the Zite event form deliberately captures NO marketing consent (R3-via-event-form retired); annual_rebook + fundraiser_loyalty re-ruled GREY — no opt-in, bounded structurally to one cycle per event row (`greyFollowupOk`; Avery-STOP/DNM/pause/bounce still gate); their populations are EMPTY until ~Oct–Nov 2026 (fundraiser ≥165d) / ~Feb 2027 (rebook ≥275d), so zero immediate sends.** DEFERRED (ledger P5): Avery-rail bounce detection (capability-check Missive DLRs FIRST — the landmine is already defused), `email_bounced` TPRS slice, admin unflag button, daily-report bounce-line split. Memory: [[consent-must-not-gate-entitlements]], [[roller-to-tprs-event-ports]].

- **2026-08-02/03 — Avery cost pass: 1h KB-cache TTL on WF1+WF2, Sonnet 4.6→5 migration, standing cache observability (ALL LIVE, cross-repo: Marketing Avery + n8n).** Full narrative + watch items: [session-handoffs/2026-08-02-avery-cache-ttl-sonnet5-observability.md](Context/session-handoffs/2026-08-02-avery-cache-ttl-sonnet5-observability.md) — **read it before touching cache_control fields, WF7 pricing maps, or running cache economics.** Headlines: (1) WF2 KB cache SMS-only-5m → **1h TTL both channels** (brain pipeline, golden 9/9, versionId `5eaa1854`; measured 82.6% vs 62% hit rate on live replay, ~$14/mo; hour-windowing simulated and REJECTED — orphan-write ceiling ~$6/mo). (2) **WF1 caching re-added 8/3** — it had NONE since a *measured* 6/12 removal; warm-chain math supersedes it: inquiries open conversations, and 48 WF2 calls/30d had a WF1 write as their only prior-hour warmth → net ~+$26/mo despite WF1's own 45.5% hit rate (median inquiry gap 76 min — Jon's "they're quite apart" instinct was right, and it's the wrong frame). (3) WF5 PE/FL/AR + WF2 Test Driver → **claude-sonnet-5** with `thinking:{type:'disabled'}` added in the SAME edit (Sonnet 5 runs adaptive thinking when the param is omitted — the exec-8701 empty-reply class) + TD's temperature removed; smoke test = Mon 8/3 ~11am CT cron (expect `model=claude-sonnet-5` in metrics rows). (4) WF7: Opus cacheWrite $10/MTok + new daily **Cache_Efficiency** Sheets tab — 8/1 measured caching NET-NEGATIVE (−$2.94) under the old 5m policy, the before-picture for the flip. (5) Re-runnable analysis kit: `Marketing Avery/brain/analysis/` (sim + 5 SQL queries incl. the WF1 warm-chain pair + README with baselines and re-run triggers). **Jon circles back ~early Sep for the next cache read** (Sonnet 5 intro pricing also lapses 9/1). Also corrected en route: brain telemetry was never broken — the 6/12 "stop" was the un-changelogged Sheets→Postgres flip; **`avery_metrics_buffer` (Supabase) is the source of truth, the Google Sheet tab is WF5-only.** Batch API assessed and rejected (no non-urgent Claude volume). Memory: [[avery-kb-cache-ttl-analysis]], [[avery-metrics-buffer-postgres]].

- **2026-08-02 — `/signatures` internal Gmail signature installer (LIVE, `985f6ce`→`88a4be5`).** Staff page at `twistedpin.com/signatures/` where **Jon Dow · John Valianos · Shanna Dow** each edit their details and hit Copy; the signature is table-based email HTML with inline styles, and the Copy button ships `innerHTML` to the clipboard. Ported from the Claude Design project *"Gmail signature optimization"* — specifically `Signature Install.dc.html`, the cleaned-up version of variant **2c** in the sibling exploration doc `Email Signatures.dc.html` (that sibling is a one-person previewer whose defaults are Jon's details — which is why Jon appeared "missing" at first; he was never in the file that was implemented). Unindexed the same four ways as `/liquor` `/money` `/playbook`: noindex,nofollow + robots.txt Disallow + `astro.config.mjs` sitemap filter + `STAFF_PATHS` in `Base.astro`. Orphan by design. **Add a teammate by adding ONE entry to `PEOPLE`** — step-1 fields, the preview and the Copy button all generate from it; `<key>PhoneHref` derives by suffix, and keys are `jonDow`/`johnV` deliberately (a one-letter `jon`/`john` difference between two real people's state keys is a bug waiting to happen).
  - **`public/email/` is load-bearing.** Gmail only renders images at a public URL, so the 8 PNGs live there and serve from `twistedpin.com/email/*`. The on-page preview uses `/email/…`; Copy rewrites that prefix to the absolute base. Never re-host on Drive/Dropbox/Docs — Gmail blocks those. Filenames are stable, so swapping an icon needs no re-paste (Google's image proxy may cache an old copy on already-sent mail).
  - **⚠️ THE MOBILE TRAP.** Gmail's phone app does **not** scale a too-wide message down — it forces the table to the column width and lets cells fight over the remainder. The design doc's claim that a 460px table *"renders identically on desktop and mobile without stacking"* is **false**; measured at 300/336/360/412px it is not. While the logo cell pinned `width="200"` the text column ate the whole shortfall and the phone number broke across **three lines** on a real device. Fixed by choosing which side yields: table `max-width:100%`, **no width on the logo cell**, logo img `max-width:100%`, `white-space:nowrap` on name/title/contact labels. Logo now rides 200px→45px. **Do not re-add a width to that cell.** Media queries are not available — Gmail strips `<style>` from pasted signatures.
  - **⚠️ Browser measurement runs optimistic here.** Gmail Android substitutes **Roboto for Arial**, and Roboto is wider for letterspaced uppercase — the footer fit on one line at 336px in Chrome yet wrapped on a real phone at the same width. Any one-line element needs margin, not a hairline fit. The footer is now two *deliberate* lines (explicit `<br>`), so width can't break it.
  - **Google badge → `MAPS_VENUE_URL`** (the listing), not the review form; `GOOGLE_REVIEW_URL` (new in `schema.ts`, built from `PLACE_ID`) is kept for surfaces where the ask *is* the point. Both derived, never pasted: the design hardcoded `maps.app.goo.gl/yyiVoLzTsHA2TNGW8` (**dead** — Firebase Dynamic Links, sunset Aug 2025) and `share.google/…` links resolve to a *search results* page carrying share-flow `utm_source` tracking. Clearing the field removes the badge entirely rather than hiding it, so an empty `href` can't reach the clipboard.
  - **Icons were rebuilt, not imported.** The design MCP returned corrupted base64 for 5 of 8 PNGs; they were recovered pixel-exact from the `-white` variants (identical alpha, RGB recoloured to `#111111`). The phone and Google glyphs were then **redrawn** — the phone was the only filled shape in an outlined set and read as a blob at 26px; the Google mark nested a second ring inside the ring. Ring geometry for any future icon: 56×56, centre r=24, 3px stroke, `#111111`, glyph ~24-26px.
  - **RESOLVED 2026-08-09 — the repo was right.** Facebook is `/twistedpin` (as in `schema.ts` `sameAs`, SnapFooter, `signatures.astro`, and `tprs/pageConfig.ts`); the design doc's `/twistedpinplainfield` was the wrong one. Jon confirmed against the live profile — **Facebook login-walls automated fetches, so this is not verifiable from the repo or by WebFetch; it needs a human with the page open.** `LocalBusiness` structured data has been pointing Google at the correct profile all along. Don't re-raise. Memory: [[gmail-signature-mobile-constraints]].

- **2026-08-03 — Frozen-quote-snapshot fix: payment paths must NEVER re-price (LIVE, tprs `7feeded` / PR #68).** INV-2026-00119 (Eric Stoudt / Penske, 8/5 event) paid his balance IN FULL and got the split-payment *"you still have a balance"* email 3 minutes later — while the admin alert, reading a different PaymentRecord, said *"($0.00 left)"* in the same subject line. **Cause:** the magic-link balance path REBUILT the quote snapshot on every payment. Invisible while a rebuild reproduced the frozen number — until the **8.5%→8.75% sales-tax bump (2026-08-01)**. His deposit froze 6/26 at $2,481.56; his balance payment minted a rebuilt $2,485.12. The amount CHARGED came from the freeze (correct) but the webhook's `paid >= invoiceTotal` read the rebuild and failed by **$3.56**, so the booking never reached `fully_paid`. **Four sites needed the invoice total and each decided independently, drifting into three behaviours** — POS/external preferred the freeze, the admin detail page preferred the freeze, and the magic-link + orphan-adoption paths always rebuilt. *Had he paid cash at the desk it would have settled correctly.* **Fix:** all four call one helper, **`getFrozenOrRebuiltQuoteSnapshot`** (`services/payment.ts`) — reuse the EARLIEST frozen snapshot, rebuild only when the booking has none. **Re-quoting is the EDIT path's job** (it rebuilds once and writes to every PaymentRecord — the other half of the ADR-0006 §4 SS-3(ii) invariant). **`SALES_TAX_RATE_BPS` needs no effective-dating — the frozen snapshot IS how an in-flight booking keeps its quoted rate, and that only holds while payment paths refuse to re-price.** Partial snapshots are BACKFILLED, never rejected (rejecting = rebuilding = re-pricing). Both balance readers now `ORDER BY created_at ASC` — they took an arbitrary row, so on a divergent booking the answer was heap order, and that figure drives the readiness digest's "unpaid" red flag + its one-click cancellation-notice button. **Verified on live prod:** Eric's public receipt renders Total $2,481.56 / Paid $2,481.56 / **Balance $0.00**; 0 divergent bookings system-wide; 0 errors post-deploy. **Jon's rulings:** honour the frozen quote, never chase the delta (total exposure across ALL affected bookings was **$11.04**); forward-looking fixes over retroactive cleanup. Eric's booking repaired by hand + staff note in `internal_notes` + paid-in-full confirmation sent. **Still owed: spot-check INV-2026-00271 when it pays ~8/14** (first balance on a pre-8/1 deposit post-fix; expect `fully_paid`, one snapshot). Memory: [[tprs-frozen-snapshot-never-reprice]].

- **2026-08-03 — Inna birthday incident → kids-party-first routing overhaul (ALL LIVE, Marketing Avery: WF1 + WF2 + KB v16.0 + After-Hours Release; golden suite 9/9).** Full narrative + root-cause map + open items: [session-handoffs/2026-08-03-inna-birthday-kids-party-first.md](Context/session-handoffs/2026-08-03-inna-birthday-kids-party-first.md) — **read it before touching Avery's birthday/kids routing, the bowling-only flow, or the After-Hours Release.** Headlines: a birthday lead got the lanes-vs-event fork WF1's own prompt forbids (Shane Coyne regression — every rule Jon wanted already existed; the failures were flag-vs-prompt-vs-KB-vs-check LAYER CONTRADICTIONS, and scripts beat rules). Shipped per Jon's rulings: birthday first touch is now qualify-then-route (age 4-12 + ≤14 → twistedpin.com/kb kids-party message; age unknown → age question; 13+/15+ → catered; /book on a birthday type is a hard check-fail), kids-party-first is the default (adults PRESENT never reroutes — only guest-raised adult bowling/eating does; "needing lane seating" removed as a catered trigger KB-wide), lanes-only declines ROUTE instead of quote (kids-fit → cake caveat → /book ≤15 → silent-escalate 16+; the dead bowling_compare prompt play + "food nudge declined" flag lie removed; retry double-bind fixed), and the 9:10am After-Hours Release now live-checks Missive labels and skips NA'd threads entirely. **Open: WF2's escalation send path still has NO pre-send NA gate; NA-skip awaits first natural exercise; golden tiffany case date-bump before Aug 30.** Memory: [[wf1-birthday-selfbook-regression]].

- **2026-08-02 — Deposit-link lifecycle + WF3 fanout fix + stale-money fix (ALL LIVE, cross-repo: tprs + Marketing Avery + n8n).** Full narrative: [session-handoffs/2026-08-02-deposit-link-lifecycle-wf3-fanout-stale-money.md](Context/session-handoffs/2026-08-02-deposit-link-lifecycle-wf3-fanout-stale-money.md) — **read it before touching WF3, the Outbox Listener, the TPRS Link Reminder / Expiry Win-back workflows, `/admin/bookings/:id/reactivate`, or Avery's money/resurrect prompt rules.** Headlines: (1) **WF3 case-0 fanout FIXED** — clearing a Needs Attention label was stamping `booking_status='cancelled'` + re-pausing the contact on every clear (that's what killed Kris/Tiffany's payment reminders); **Jon declined the ~36-row backfill — forward-only** (runbook shelved in Marketing Avery repo; rollback of WF3 silently resurrects the bug — canary doesn't watch connections). (2) **Full expired-deposit-link lifecycle now live:** hardened 22h reminder → `link_expired` state → money-free win-back SMS (fires ONLY on cron expiry, structurally can't hit staff cancels) → staff Reactivate button (tprs PR #67; original link URL revives, capacity re-checked) → `booking.reactivated` outbox event clears the Missive Cancelled label + heals Avery's mirror for BOTH staff and Avery paths. (3) **Avery resurrect un-gated** (KB carve-outs + prompt, versionId `16628241`) — "my link expired" = readiness, zone-gated ≤60 guests/10-365 days; has still never fired live. (4) **Stale-money fix:** per-package quote panel (`Lookup Quote Log (Context)` → `ctx.quote_history`) replaces the last-quote scalars in the prompt + validator whitelist; paid-tier money questions escalate; golden suite is now 9 cases — **`tiffany-multiquote-offer-beat` needs a date-bump before Aug 30 or it blocks deploys.** Kris + Tiffany stay Needs-Attention/manual per Jon. Memory: [[deposit-link-lifecycle-build]], [[wf3-fanout-contamination]], [[avery-event-money-columns-are-last-quote]].

- **2026-08-01 MEGA-SESSION — ads check-in + holistic SEO review + seasonal pre-staging + FKB 2027 transition (ALL LIVE).** Full narrative, error ledger, and open-items table: [session-handoffs/2026-08-01-ads-checkin-seo-review-seasonal-prestaging.md](Context/session-handoffs/2026-08-01-ads-checkin-seo-review-seasonal-prestaging.md) — **read it before touching ads, seasonal pages, or the FKB/Pin-Pass flows.** Headlines: (1) **Ads** — Jul 18–31 read + Script 16 live (Q4 ramp funded: Events $35, corporate exacts $7; 51 negatives; geo verdicts — Romeoville conquest CONVERTED); ground truth beat the dashboard (9 real inquiries vs 2 counted, incl. a deposit-paid $790 company event) → standing rule: cross-check `avery_event` before trusting Ads conversion columns. Next check-in ~Aug 31. (2) **Verdicts** — NO paid GEO tracker (quarterly DIY prompt audit instead), NO blog cadence; the Yelp→OpenAI deal makes **Yelp/Bing Places/Facebook/TripAdvisor the ChatGPT supply chain → listings refresh is Jon's highest-leverage open item.** (3) **Site** (`4d55743`+`963cf8d`+`4d87cc4`) — font-swap CLS killed site-wide (size-adjusted fallbacks, capsize-computed), footer "Explore" row de-orphaned the 7 why-us pages + 3 guides, /holiday-parties got its FAQ block + FAQPage schema, birthday hero carries both audiences, league interest form on /leagues → Resend → contactus@, /reserve-preview2 deleted (was leaking into the sitemap). (4) **THE DATE-GATE PATTERN:** /free-kids-bowling → 2027-waitlist mode and /summer-pin-pass → off-season mode **flip THEMSELVES at midnight CT Aug 15** via build-time date gates + the daily cron rebuild (both future states build-verified; promo bar hands off to "Holiday parties — December books fast" the same morning, NYE joins Nov 15). **Waitlist sends NO confirmation text (Jon)** — the page panel is the confirmation; the n8n intake was fixed + live-verified same session (without it, waitlist joiners would've gotten the $10 SAVE10 coupon text). NO rollover: 2026's 3,307 (≈2,490 marketable) re-register in 2027; spring send = one SMS to both cohorts. **Nothing remains before Aug 15.** Memory: [[google-ads-checkin-2026-07-07]], [[seo-ai-search-posture-2026]], [[fkb-2027-transition-plan]].

- **Liquor app — case-size capture, a pre-submit accuracy check, and an empty recipe queue (2026-07-26, all LIVE).** 11 PRs (Website #8 #9 #10 #12 #13 #14 · tprs #35 #37 #38 #40 #41), ~12 code defects fixed, 15 recipe classifications entered on a surface that had never held one. **The constraint that drove it: the variance report is a ONE-WAY DOOR** — `bar_variance_report` has `UNIQUE(session_id)`, the sweep skips any session that already has a row, and nothing anywhere deletes or recomputes one. So every check ran *after* the last moment anything could be fixed. Now `GET /admin/bar/counts/:id/precheck` runs at the Submit tap (read-only by construction; **it must never write that table**), flagging *impossible* (`end > start + purchased` — the case-vs-bottle slip, and the only unscanned-invoice detector we have), *not counted*, and *overuse*. Verified: zero findings on the identity case across all 93 variance SKUs, while a simulated 12× case slip fires at $3,366–$10,512. **Its copy never says "recount this"** — an impossible number and a missing delivery look identical, and only one is the counter's fault. Also shipped: `+ case size` (add-only; **pack size is a BRAND fact, not a size fact — Grey Goose 1L is 6/case, Tito's 1L is 12 — so it asks, never suggests**), 6/12/24 quick-picks (all 38 case sizes ever recorded here are those three), and the 🍒 non-bottle marker (an emoji appears **iff** the item isn't a bottle). Every bug fixed was "two correct functions never wired together," never "this function is wrong." **Owed: the 10-minute dry run — nothing but the layout has been touched on a real phone.** Full narrative, rulings, and my own errors: [session-handoffs/2026-07-26-liquor-case-sizes-presubmit-check-recipes.md](Context/session-handoffs/2026-07-26-liquor-case-sizes-presubmit-check-recipes.md). Memory: [[bar-variance-report-mechanics]], [[bar-case-counting-shipped]], [[bar-option-level-recipes]], [[bar-email-routing]].

- **Lane Rebook campaign 🟢 FULLY LIVE (built 2026-07-27, live 2026-08-01 on Jon's go)** — the lever that makes the $10 checkout opt-in profitable. n8n **`WF-Lane-Rebook-Campaign`** (`SRMB0xdrcKmZuigE`; ACTIVE + ARMED=true + TEST_MODE=false — no human step remains; kill switch = deactivate or ARMED=false): attended non-refunded lane booking (codes 4/5/121/123) → ~4 weeks later (28–34d in practice — first Thursday past day 28; 42d hard age-out per Jon, retimed from 21d/56d) → 50%-off-one-lane single-use code (one parent discount per run cohort via TPRS `POST /api/avery/coupons`, `max_discounted_quantity:1`, 14-day book-by) → SMS **Thursdays 6pm CT** (family-at-dinner weekend planning; was daily 11:30) from the **loyalty number** via `scheduled_message` (rail re-checks consent at send; proven STOP handler + 21610 feed), plus a final-Thursday `cap_exempt` reminder on the book-by day ("expires tonight"). Eligibility query owns its consent predicates (`sms_marketing_opt_in` + `do_not_market` + `sms_bounced` + newest-explicit-decision-on-the-phone rule — deliberately NOT Visit Feedback's transactional predicates), UNCAPPED per run (Jon 2026-07-30 killed the cap; rail paces 1 msg/~4s), oldest-first, holds out 15% deterministically (phone hash), **per-VISIT re-entry** (each new attended visit re-arms the guest — redeem-and-return earns the next cycle, per Jon; max 3 entries/phone/365d backstop; deterministic holdout = permanent control group). Jon's 3yr baseline (80% never rebook, ~0% quickly) → redemptions ≈ pure lift; low single digits = success. **Audience narrowed 7/30 (Jon): CHECKOUT opt-ins ONLY** — must have `consent_event(source='checkout', action='opt_in')`; Patch/kiosk/web-form consent excluded (the 16 Patch guests the 7/27–7/30 park-lift exposed are OUT; dry-run under new scope: 0 eligible today, first cohort ~Thu Aug 20). Copy: offer + reminder both APPROVED (Jon 7/30). **Checkout-optin park REMOVED 2026-07-30 (Jon's ruling: the checkout box IS marketing consent)** — trigger `trg_customers_park_checkout_optin` dropped + 8 holds released, all 13 audience phones sendable; verified no enabled lifecycle automation touches them (first touch = the 4-week offer, Jon explicitly wants NO welcome text). Timing anchors on `event_date` (bowling day), not booking-created date. **LIVE 8/1 — nothing remains but observation: verify the Aug 6 zero-send run executed; audit the first real cohort the morning after Thu Aug 20 6pm (ledger, /admin/discounts rollup row, delivery, STOP rate); weekly funnel checks; holdout-lift read ~6wk in.** — runbook `scripts/rebook-campaign/README.md`, measurement (incl. the holdout-lift query) `scripts/rebook-campaign/measurement.sql`. Anniversary nudge + Part A "The Return" NOT built (offer designs still rejected/unapproved). **2026-07-30 additions:** (1) Jon's live test exposed that lane products are carve_out packages — the 50% code discounted only the $76.15 lane-time remainder ($38.08 off a $99.95 lane, not $49.98); **tprs PR #54** makes a package-scoped rule cover the full sticker with the quantity cap counting PACKAGES — **merge + Render deploy is now an arming precondition** (runbook step 0). (2) **`/book/<CODE>` magic link SHIPPED** (Website `b48b76a`): 302 → `/reserve/?code=` → wizard prefill + "code is ready" note + auto-preview at checkout; invalid link-landed codes self-clear after showing why. SMS drafts in Campaign Config now use `twistedpin.com/book/{code}`. Test code QQD9JMAN (wrong basis) deactivated; mint a fresh one post-PR-54-deploy. Memory: [[lane-rebook-campaign-built]], [[tprs-discount-package-basis]].

- **Stranded payments — architectural fix FULLY LIVE (2026-07-19, tprs PR #8 merged → Render w/ migration 0108 verified in prod; Website `f12a954`).** Supersedes the reactive 2026-07-15 layer (PR #7's pre-charge check + cron stay as backstop). Session insight: refund coverage was opt-in (3 of 13 post-capture convert error paths refunded) and the sales cutoff — a scheduling preference, not a physical constraint — was being enforced by taking money then refusing the booking. Now: (1) **cutoff never rejects post-charge** — convert lands the booking flagged `booked_inside_cutoff` + audit row w/ concrete lateness + **"SHORT NOTICE:"** admin booking-alert email; pre-charge rejection at /payment-intents unchanged. Tuning knob = the per-product cutoff number in admin (moves 1:1). (2) **Refund-by-default** — every terminal convert branch auto-refunds (incl. capacity), plus a commit-ack-guarded fail-safe on unmapped errors; retryable branches (coupon, hold-expired) deliberately don't. (3) **Capacity checked before hold-liveness** — capacity-gone is terminal+refunded with honest UI (the old sold-out blocker told charged guests "your card hasn't been charged"); hold-expired now means retry genuinely works. (4) **Hold grace GREATEST(existing, now+180s)** — was a hard SET to 60s that truncated healthy holds right before 3DS. **The reconciliation cron is now the canary: any email from it = genuinely new failure class.** Deferred by choice: grid/cart-add padding (add only if SHORT NOTICE fires too often). Prior narrative: [session-handoffs/2026-07-15-stranded-payment-cutoff-fix-reconciliation-cron.md](Context/session-handoffs/2026-07-15-stranded-payment-cutoff-fix-reconciliation-cron.md). Memory: [[tprs-stranded-payment]].
- **🟢 SITE IS LIVE at https://twistedpin.com (cutover 2026-05-17).** New build replaces the legacy site. All 18+ production 301 redirects are load-bearing for real traffic; GSC actions affect real indexing; copy/schema/perf changes ship straight to production.
- **Audience-funnel event pages shipped (2026-05-18)** — 4 new pillar pages split out from `/events` to capture audience-specific search intent: `/corporate-events/` (`corporate event venue near me`), `/holiday-parties/` (seasonal nav Sep 1→Jan 5), `/showers/` (bridal + baby combined), `/wedding-receptions/` (intimate weddings + rehearsal dinners). All use the same recipe — pillar template, vip-lanes-* LCP + buffet-* lazy below the fold, Service schema with 8-city `areaServed`, Heyflow closing CTA. 4 legacy 301s retargeted from `/events/#corporate` (hash drops on 301) to the new top-level URLs. 11 `/why-us/*-il/` city pages cross-linked. Stage 6 from the punch list (which planned `/events/{type}/` URLs) is now SUPERSEDED — see Decisions Log 2026-05-18. **Latest session captured in [session-handoffs/2026-05-17-punch-list-stages-0-4.md](Context/session-handoffs/2026-05-17-punch-list-stages-0-4.md) — read first.** Prior session: [2026-05-17-schema-lcp-perf-sweep.md](Context/session-handoffs/2026-05-17-schema-lcp-perf-sweep.md).
- **Pre-ads-launch perf pass shipped (2026-05-17)** — 8 commits across schema, LCP, AVIF posters, cache headers, ES2020 build target, hero-preload gating. **Every ad-blocking page is now under 3.5s LCP** (`/events` 6.9s → 3.1s, `/game` 7.5s → 3.2s, `/bowl` → 2.5s green band, `/vip-suite` 4.0s → 2.9s green band, `/fundraisers` 4.7s → 3.5s, `/birthday-parties-booking` 3.5s). GSC `/events` Review-Snippets error fixed via dual `@type` + 6 missing schemas added. AVIF posters site-wide saved 367 KB. The "aha" fix was `47e7479`: hero LCP preload was unconditional in Base.astro, wasting 280 KB on every pillar page that doesn't display the hero — gated to homepage only. **One action remaining: in GSC, click Validate Fix on `/events` Review Snippets issue.**
- **301 redirect map shipped** (2026-05-06) — 18 legacy URLs preserved per launch-checklist. Plus `/essential` + `/elevated` SMS short links for text marketing.
- **`/upcoming-events` rebuilt** (2026-05-06) as a real calendar driven by an Astro content collection (`src/content/events/*.md`). Empty state when zero events; multi-day cross-month range support; CTA per event. NYE 2026 is the first entry.
- **`/new-years-eve` page live** (2026-05-06) with NYE.mp4 video hero. Exists at the URL year-round; surfaces in NavDrawer Visit section between `showFrom` / `showUntil` via the new `src/config/nav-seasonal.ts` system. Body copy is voice-y placeholder until ops gives package details.
- **`/pricing` page live** (2026-05-06) — day-tabbed walk-in pricing surfaced via NavDrawer Info section. Mon-Thu collapse to 2 columns; Fri/Sat/Sun show 3 columns (Time / Trad / Suite). Specials content collection (Penny A Pin Wednesday) renders as a callout above the table. Holiday note + "verify on Google" caveat preserved.
- **"Plan an Event" CTA destination is configurable via `src/lib/links.ts`** — `PLAN_EVENT_URL` toggles between `AVERY_ON_URL` (Zite, AI sales agent under test) and `AVERY_OFF_URL` (Heyflow `event.twistedpin.com/#start`). One-line flip propagates to all 19 importing CTAs in ~90s. **Currently set to Heyflow (OFF)** matching ops reality and ads sitelinks. The earlier 2026-05-06 locked decision (Zite) was provisional pending Avery proof-out; the toggle infrastructure supersedes the lock. (Mirrors the Reserve → Roller pattern. `/events` and other educational links stay on the local /events page.)
- **Google Places API live hours** (2026-05-06 wiring; **env vars confirmed set on Vercel as of 2026-05-08**) — `src/lib/google-hours.ts` reads live hours from the Business Profile when `GOOGLE_MAPS_API_KEY` + `GOOGLE_PLACE_ID` are set; falls back to static `src/data/hours.ts` otherwise. **Live in production** — confirmed empirically by ops (hours auto-updated 2026-05-08 with no commit). All hours surfaces (SnapFooter status strip, /pricing day tabs, /faq "What are your hours?", schema `openingHoursSpecification`) read through `getLiveHours()` / `formatHoursAnswer()`. Single point of truth: edit `src/data/hours.ts` only as a fallback if Google API ever errors.
- **GoTab + Untappd menu data is wired** (2026-05-05). Daily 4am cron rebuilds. See [session-handoffs/2026-05-05-menus.md](Context/session-handoffs/2026-05-05-menus.md).
- **`/menu` hub + thumbnails** (2026-05-05) — 7th inline nav slot; horizontal-split cards with real hero photography.
- **`/privacy`, `/terms`, `/accessibility`** (2026-05-05) — three compliance pages live. Counsel review status: unknown / open — confirm with ops whether these were reviewed before the 2026-05-17 cutover.
- **`/coupon` native form → LOYALTY PLATFORM (2026-07-24 cutover; this bullet corrected 2026-08-01 — it was two migrations stale).** History: native-Patch-API form (2026-05-05) → reverted to Patch iframe (2026-05-07) → **native loyalty form (2026-07-24): posts to `/api/coupon-signup` → n8n `WF-Loyalty-Forms-Intake` → Supabase loyalty platform; grants `signup-10off` ($10 / code SAVE10 / 21-day expiry) with first-join gating.** Patch is CANCELLED and not revivable; zero live Patch dependencies remain site-wide (verified 2026-08-01). `PATCH_API_KEY` + `PATCH_ACCOUNT_ID` on Vercel are dead — remove whenever convenient.
- **DNS migration runbook** (2026-05-05) — full cutover plan in [Context/dns-migration.md](Context/dns-migration.md). User picked GoDaddy DNS.
- **Lighthouse baseline + LCP optimization** (2026-05-05) — Desktop 100/100/100/100; Mobile 86 with LCP 3.9s. **Don't re-test until real photography + final hero splice land.**
- **Vercel auto-deploy** — main-tip auto-deploys to https://twistedpin.com (production domain since 2026-05-17 cutover). Preview deploys still spin up on every branch.
- **Mobile hero video splice** — direction approved (3 sources), specific window timestamps pending user.
- **Adobe Fonts kit** — declined 2026-05-06. Substitutes (Barlow Cond / Montserrat / Roboto Slab) ship for production; user opted out of the CC subscription cost. Swap is a 5-minute CSS-variable change if direction changes later.
- **Real photography** — pillar pages still use placeholders / homepage reuses. Encoder pipeline ready (`scripts/build-snap-images.mjs`); when sources land in `Context/pictures/`, AVIFs auto-generate.
- **Outstanding ops confirmations (post-launch sweep):** VIP suite capacity, fundraiser stat, NYE packages. Phone number is real (`(815) 782-7790` in `PHONE_DISPLAY`/`PHONE_TEL` in `src/lib/schema.ts`).
- **Post-cutover GSC actions:** (1) Validate Fix on `/events` Review-Snippets error (per 2026-05-17 schema-LCP handoff). (2) Submit `/sitemap-videos.xml` (shipped in Stage 4 of 2026-05-17 punch list). (3) Spot-check that the 18 production 301 redirects are firing on real traffic. **All four GSC fixes validated 2026-05-19** — Review Snippets (from 2026-05-17), Videos uploadDate timezone, Events organizer.url+performer, Redirect error on apex `twistedpin.com`. Resolutions expected in 3-10 days via Google's natural re-crawl cadence.
- **Capacity copy + venue schema shipped 2026-05-19** — *"Up to 80 in the suite, or up to 200 for full-venue buyouts"* now appears across `/vip-suite/`, `/events/`, `/corporate-events/`, `/holiday-parties/`, `/fundraisers/`, and 5 `/why-us/*` city pages (lockport, naperville, oswego, romeoville, shorewood). `localBusinessBase()` schema gained `maximumAttendeeCapacity: 200` (venue-level); `/vip-suite/` EventVenue keeps its own 80 (suite-level). Deliberately excluded `/wedding-receptions/` + `/showers/` — both intimate-positioned; new docstring comments document the exclusion logic to prevent future-maintainer drift. Commit `17b2bed`.
- **Live Google reviews wired 2026-05-19** — extended the existing daily Places API cron to also fetch `rating` + `userRatingCount`. SnapFooter proof-card + LocalBusiness `aggregateRating` schema both read live values, falling back to `GOOGLE_RATING`/`GOOGLE_REVIEW_COUNT` constants when snapshot is missing. Zero additional API cost (same SKU, expanded field mask). Visual treatment in SnapFooter unchanged. Verified end-to-end: snapshot `live-hours.json` now carries `rating: 4.5, reviewCount: 1142` (bumped from the hardcoded 1141 — proof the data is live, not echoing constants). Commits `d8ee252` (wiring) + `6023192` (cron trailing-slash fix that made the cron actually fire — see below).
- **Cron silent-failure fixed 2026-05-19** — `vercel.json` cron `path` was `/api/cron/rebuild` (no trailing slash). `astro.config.mjs` sets `trailingSlash: 'always'` which 308-redirects every URL to the slash form. **Vercel Cron does NOT follow 3xx responses** — so the cron path needs the trailing slash to match the canonical form directly. Symptom of the bug: zero log entries for `/api/cron/rebuild` for 5 days (2026-05-14 → 2026-05-19); `live-hours.json` snapshot was empty `{}` the whole time; site fell back to static hours. One-character fix in `vercel.json`. Commit `6023192`.
- **Apex redirect type 307 → 308 (2026-05-19)** — Vercel Domains UI was defaulting to 307 Temporary Redirect for the `twistedpin.com → www.twistedpin.com` redirect. Google's Redirect error guidance specifically flags 307 on canonical URL changes (it tells Google "this page is temporarily here, don't update your records"). Flipped to 308 Permanent in the Vercel domain edit dialog. Full chain `http://twistedpin.com/` → `https://twistedpin.com/` → `https://www.twistedpin.com/` is now 308-308-200 (was 308-307-200).
- **Avery DB migrated Railway → Supabase Pro (2026-05-19, cross-repo)** — n8n hosted somewhere off-Railway + Railway Postgres + no managed pooler = chronic ECONNRESET roulette (Railway's edge proxy silently kills idle TCP connections; n8n's Postgres node doesn't validate pooled connections before reuse). Today's Railway outage (Google Cloud blocked Railway's account 22:29 UTC) was the forcing function. New Supabase Pro project `twistedpin-platform` (us-east-1, signed up via Vercel marketplace → billing flows through Vercel invoice). Canonical schema applied from `Loyalty/db/002-post-amendment-schema.sql`: public-schema-with-prefixes (per ADR-0012 amendment 2026-05-17), 10 tables, 5 enums, 3 service-account User seeds, Jon Dow as customer #1. n8n credential repointed at Supabase Transaction Pooler (port 6543, "Ignore SSL Issues" toggle ON — n8n can't walk Supabase's cert chain, standard workaround). End-to-end verified: test SMS → `avery_event E-9009780` written → status `gathering_info` → no ECONNRESET. Loyalty + TPRS still on Railway (Loyalty pending schema-impl slice; TPRS-backend-hosting decision deferred to ~2 weeks pre-launch). Full session handoff: [session-handoffs/2026-05-19-gsc-fixes-capacity-google-reviews-supabase-migration.md](Context/session-handoffs/2026-05-19-gsc-fixes-capacity-google-reviews-supabase-migration.md).
- **❌ Mixology event — CANCELLED 2026-07-26 (low signups). Do not rebuild from the notes below without re-reading this paragraph.** Jon called it off two days before the date. **Website (removed):** `/mixology-experience/` + `/reserve/mixology/` pages deleted, `mixologyPageConfig` deleted from `src/tprs/pageConfig.ts`, promo-bar entry deleted from `src/config/promos.ts` (the homepage bar now runs a single static Free-Kids-Bowling promo — the rotation JS no-ops below 2 slides), seasonal NavDrawer entry deleted from `src/config/nav-seasonal.ts`, `src/content/events/2026-07-28-mixology-experience.md` deleted (drops it from `/upcoming-events` + its Event JSON-LD), `/reserve/mixology` sitemap exclusion removed from `astro.config.mjs`, `mixology-host`/`mixology-card` sources removed from `scripts/build-snap-images.mjs`. **Both URLs 302 → `/upcoming-events/` in `vercel.json` — deliberately 302, not 301,** because the lander was the printed-QR/social target and an indexed page: if the night is ever rescheduled we want the URLs back without fighting browser-cached permanent redirects. Flip to `permanent: true` once it's clear the event isn't returning. The built `/public/snap/mixology-*.{avif,webp,jpg}` files are intentionally LEFT in place so hosted URLs keep resolving. **TPRS:** product code 500 deactivated (see below) — the 20-seat pool, the 7/28 override, and the ack form are left in place, so a reschedule is a date change plus reactivation, not a rebuild. **`scripts/seed-2026-holidays.ts` still carries a comment saying 7/28 is deliberately NOT a venue closure "because the Mixology Experience sells that night" — that rationale is now dead** but the exclusion is harmless (7/28 is a Tuesday and the venue is closed Mon–Wed anyway, plus active VIP + Traditional pool closures already block lanes that day). **3 guests had already paid** ($238 each, 2 seats × $119: Cathy Hawkins, Rita Duque, Patricia Gwaltney) — refunds + personal outreach are an ops task, not a code one. Historical context below is retained for a possible reschedule.
- **[HISTORICAL — event cancelled, see above] Mixology event (2026-07-03) — LIVE (bar one deploy).** "An Evening with America's Top Mixologist," one-night ticketed cocktail experience, Brian Van Flandern (Tue **2026-07-28, 7 PM, $119/person, 20 seats, 21+**, venue closed to the public). **TPRS migration `0089_mixology_event_seats` APPLIED to prod:** "Mixology Event Seats" pool (20 resources) + per-seat product **code 500** ($119, 1 seat/unit, 120min, cap 10/order, tax `untaxed`, gl `BOWL - Special Event`) + single `product_date_availability_override` (7/28 19:00-19:30, NO product_schedule → only that one slot ever bookable) + 2 REQUIRED ack checkboxes (21+, ticket-only). FKs COALESCE to migration-seeded fallbacks (CI-portable). **Website DEPLOYED:** lander `/mixology-experience/` (Details→Host→Experience order; Brian photo `mixology-host-*` = desktop split-hero + mobile Host section; refund policy 10-day/resale), booking `/reserve/mixology/` (`mixologyPageConfig` code 500; noindex + sitemap-excluded), `/upcoming-events` entry, seasonal NavDrawer (martini, 7/3→7/28). **Social/QR link = `twistedpin.com/mixology-experience/`.** **⚠️ OWNER-APPROVED PER SE / MICHELIN EXCEPTION (2026-07-03):** the site-wide Per Se / Thomas Keller / three-Michelin-star ban STILL holds everywhere else, but Jon explicitly authorized the framing in this page's "Your Host" section ONLY (Brian = "only mixologist tied to a three-Michelin-star distinction, for opening Thomas Keller's Per Se"). Marked as a deliberate exception in the .astro — do NOT strip in a brand sweep, do NOT propagate to other pages or the checkout copy. **Closure trap RESOLVED:** the `[auto-holiday:2026-07-28]` VENUE closure (from `scripts/seed-2026-holidays.ts`, 3am→3am/day) zeroed the event's seat pool; Jon cancelled it + added VIP+Traditional POOL closures (public closed, event open); seed script patched to not re-add 7/28. **Catalog-visibility bug FIXED:** `getBookableProductsGrouped`/`filterScheduled` only listed products with a recurring `product_schedule`, so the override-only event was invisible in the grid; patched to also list products with an active `added` override (tprs `apps/backend/src/services/customer-catalog.ts`). **✅ CATALOG FIX IS IN MAIN (confirmed 2026-07-14):** the override-only-product listing fix shipped to main as `de59695 fix(catalog): list override-only products + Mixology Experience event (0089)` — the earlier "must be merged to main" note is RESOLVED, the override-only event is visible in the booking grid in prod. Branch `feat/teacher-group-recurring-blocks` now holds only 2 unmerged **cosmetic** commits (`0090` product-card image + trim 21+ ack copy · `0091` remove prize mention from code 500) — safe on `origin`, optional polish for 7/28, NOT load-bearing (event books fine without them). Memory: [[mixology-event-build]], [[tprs-venue-closure-blocks-all-pools]].

- **`/playbook` teammate culture book LIVE (2026-07-20).** Internal Playbook + (pending) Guidebook at `twistedpin.com/playbook/`, shared password **`onefamily`** (case-insensitive, whitespace-trimmed). Password-gated SERVER-SIDE in the route frontmatter (`prerender = false`) so unauthenticated requests never receive chapter content; noindex + robots Disallow + sitemap-excluded + `isStaffPage` (no GA4/Clarity/Meta). Paged reader — one chapter per screen — because the source doc gives every chapter a "Next CTA" and the short-line pacing IS the voice. **Part One is LOCKED and transcribed verbatim** into `src/content/playbook/chapters.ts`; do not rewrite or tighten it (source of record: `Context/Twisted Pin Playbook.txt`). End-of-book signature writes `public.playbook_acknowledgments` (Supabase `twistedpin-platform`) and emails info@ via Resend — DB failure fails the request, email failure does not (the row is the record). **Part Two shipped same day** — flow is now cover+password → "who are we talking to?" (name) → hub → **The Playbook** (paged, ends in signature) or **The Guidebook** (`src/content/playbook/guidebook.ts`, contents-first, unsigned, 8 chapters from `Context/Part Two Guidebook.docx`). The name **pre-fills the signature but never auto-signs it** — an acknowledgment has to be an act taken after reading, not a login. **No email on login by design**: the signal worth having is when someone *hasn't* finished, which no notification can report, and per-login mail would train info@ to ignore the completion alert; starts land silently in `playbook_sessions`, so "started but never finished" is `where completed_at is null`. Reading time accumulates **only while the tab is visible** and is client-reported → clamped server-side → **a coaching signal, never evidence**. Contents drawer (phone) / sidebar (≥1025px) spans both books. Walls-of-text handled typographically without touching locked copy (beats <52 chars get air, per-chapter lede, "03 / 17" counter, diamond at the story→principle turn); **Photography SHIPPED 2026-07-20** (`989014c` → `fe0e8ab`): 15 Part One chapters carry Jon's real photos via an additive `photos[]` field per chapter in `chapters.ts` — the locked copy was NOT touched. Four layouts: 1 photo → full-width 3:2 hero · 2+ → two-up 4:5 gallery · `photoLayout: 'timeline'` → the captioned Pioneer→Plainfield→Twisted Pin arc · `'stack'` → full-frame natural aspect, for group panos no tile crop can hold without amputating someone. Encoder: `scripts/build-playbook-images.mjs` → `/public/playbook/<name>-{w}.{avif,webp,jpg}` — re-run after swapping any source, and reach for its `focus: {x,y}` knob when sharp's `attention` crop clips a face. **Trap:** the reader CSS renders an odd LAST gallery tile full-width at 3:2, so that source must be ENCODED 3:2 or object-fit re-crops it live and chops faces. Source photos sit in the **gitignored** `Context/Playbook and guidebook photos/`; the committed outputs are what actually serve, so a fresh clone renders fine but can't re-encode. **Also live:** `/playbook/status` (manager view — signed / stalled 3+ days / signed-an-older-version, plus a button emailing the access brief to info@), `/playbook/poster` (printable 8.5×11 for the time clock, QR → `/welcome`; **single page is guaranteed by FIT, not by a fixed height — 2026-07-23 rewrite.** The old approach pinned the footer to the bottom via `margin-top:auto` and forced `height:10.5in` + `overflow:hidden`; that spilled onto a 2nd page whenever the browser applied *any* default print margin (Chrome/Edge ~0.4in, Firefox ~0.5in) because the 10.5in sheet was then taller than the usable area and broke across pages. Now the content stack is sized to ~9.5in tall — shorter than the usable area under every default margin — and the sheet sizes to its content (`min-height:0`), so it can't paginate no matter the margin setting. QR shrank 2.5→2.0in, headline 62→54pt, logo 2.6→2.15in. **If you add/enlarge content here, keep the stack under ~9.7in or it spills again;** there is NO safe fixed height because usable area depends on the user's margin. Verified 1 page at margins 0/0.4/0.5/0.6in via headless render.), `/welcome` short link. **Signatures are version-stamped** (`playbook_version`, derived from content, server-side only) so a tip-pool or policy change flags everyone who signed the old document. **Search** lives in the contents drawer — no LLM by design (it finds the passage; the book's own words render verbatim), with aliases on the QUERY in `src/content/playbook/search-aliases.ts` — **adding a teammate's exact words there is the whole maintenance story.** **⚠️ THIS REPO IS PUBLIC: `PLAYBOOK_ADMIN_PASSWORD` has no default and the status page fails closed without it — never add a fallback; and the team password's `onefamily` fallback is published, so override it with `PLAYBOOK_PASSWORD` on Vercel.** Env: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `PLAYBOOK_SESSION_SECRET`, `PLAYBOOK_NOTIFY_FROM`, `PLAYBOOK_PASSWORD`, `PLAYBOOK_ADMIN_PASSWORD`. Memory: [[twisted-pin-playbook]].

### Watch list

- **Future family-bowling page** — name TBD per ops (NOT "Kids Bowl Free" — TM'd by another operator + venue doesn't currently run that program). When it lands: build the page, update `/free-kids-bowling/` redirect target in vercel.json, add cross-link card back to `/bowl`.

---

## Out of Scope (don't touch unless asked)

- Print and signage applications — those still follow the original brand-guidelines mood
- Rewriting `seo.md` — it's authoritative; honor the URL plan and the targets
- Inventing new logos or sub-brands

---

## Decisions Log

- **2026-09-01 — Staff-only override to book over a lane conflict (tprs PR #145, merged `52f5f0d`, LIVE and verified in prod; no migration).** Jon built a Sep 12 VIP event in `+New Paid Event` (50 guests, 6 VIP lanes, 3hr, 6:30pm, $3,560.38) and the engine refused it: *"VIP Lanes are fully booked at this time (2 of 6 reserved; requesting 6 more). Conflicts with 1 existing booking: INV-2026-00508 (Nikki Kapogiannis)."* Jon: *"I agree with this (technically) but I need to override it — I see there are 2 lanes ending at 6pm and the 30 minutes to flip will work for this event."* Then the ask that shaped the build: *"we probably just need a staff access only override to ignore the warning sort of thing — but not a lever Avery can use or deploy."* **The refusal was the FRONT BUFFER, not the lanes.** `docs/SHARED-TURNOVER-SPEC.md` carries this exact worked row — *"Event start 6:30pm (core 6:30-8:30, lock 5:30-9:30) → rejected — candidate lock reaches into Karla's core (3-6) at 5:30-6"* — i.e. a 60-min setup buffer puts the candidate's LOCK window at 5:30pm, colliding with lanes that are live until 6:00. Correct by the model, wrong about this floor on this night, and staff had no way to say so: the `CapacityExhaustedError` handler's own comment had deferred exactly this (*"No walkin-reserve override UX at slice-5 — could grow it as a future-amendment if staff hit capacity collisions operationally"*). **Shipped:** `runStrictModeCapacityCheck` gains `onViolation: "throw" | "collect"` (default `throw`, returns `CapacityExhaustedContext[]` — every existing caller ignores the return and is byte-identical), and two staff surfaces turn the refusal into a warning + confirm checkbox on the same ADR-0026 alert+force-book pattern the per-day availability override already uses: `/admin/new-event/confirm` (create) and `/admin/bookings/:id/edit-event` (engine-event edit). Wire flag `forceCapacityOverride=1`. **Five load-bearing choices, in rough order of how badly each would bite if reversed:** (1) **Avery cannot reach it** — customer checkout, the cart, `line-item-resurrect` and every Avery route call the check directly or through `avery-event-booking.ts`, and none accept or forward the flag; overselling is a judgment an accountable human makes about a specific room on a specific night, not something an agent or a guest may take. (2) **A blocking pool/venue CLOSURE is never collectable** and throws even in collect mode — a closure declares the room shut, has its own admin surface to edit, and seating guests in a closed venue is a different decision from overlapping two events; same reasoning that kept `releaseWalkinReserves` its own flag rather than a general "ignore capacity" switch. (3) **Collect mode evaluates EVERY claim** instead of short-circuiting — a `try/catch` at the call site that merely swallowed the first throw would silently skip the remaining pools' checks, overriding conflicts staff were never shown. (4) **The EDIT path carries the flag too, and that is not optional** — a booking created over a conflict reads as oversold from then on, so without it the first ordinary headcount edit is refused and the booking is permanently uneditable (a trap the create-only version would have shipped). (5) **`pool_holds` are still written** — the window honestly reads oversold afterwards and the NEXT booking is refused on real numbers; `computeCustomerAvailableCount` already clamps at 0, so the gauge and Avery's availability read correctly against an oversold pool. **Audit:** `forced_capacity_overrides` on the existing `staff_direct_booking_created` / `booking_pending_deposit_edited` payloads (pool, window, effective capacity, already-held, requested, oversold_by, and the conflicting invoices **with customer names**) — a payload field, no new discriminator, mirroring `forced_override_ids`, which is why there is no migration. **Also fixed en route:** both confirm flags are now parsed ABOVE every `confirm.eta` render and echoed back as hidden fields — both guards re-run on every submit, so confirming the availability override and THEN hitting the capacity warning used to drop the first confirmation and ping-pong between the two screens forever. **Tests:** 10 new (5 route-level, 5 on the check itself); 7 red without the change, 3 deliberate controls (throw mode still the default, an ordinary booking's audit free of the override key, a venue closure still refusing). Full backend suite 2178 passing. ⚠️ **Test-environment trap:** `blocked-capacity.test.ts` → *"keeps a future multi-date closure in the default blocked-capacity list"* is RED on a local `tprs_test` and **identically red on `origin/main`** — local accumulation (648 closures) past that list's 100-closure cap, which the test's own comment predicts. Not a regression, not a date bomb; CI runs against an empty DB. **Verified in prod** from the Render logs on Jon's real booking: `req-1p` 20:46:45Z POST `/admin/new-event/confirm` → **400** (warning + checkbox), `req-1z` 20:47:03Z same POST → **303** (booked), 18s apart, zero errors in the window. **Not in scope, still refuse as today:** `/admin/bookings/new` (line-item composer), its edit, and reschedule — engine events edit through `edit-event`, which is covered. **⏰ OPEN, and the more important half:** if *"30 minutes to flip is enough"* is generally true for VIP events, then every 6:30-after-a-6:00 booking hits this and the override becomes a standing workaround rather than an exception — the real fix is the **parked `products.front_buffer_minutes` ruling on codes 9/10** (raised in the 2026-08-31 Terrell Davis package-aware-turnover investigation, parked by Jon, *"availability is package-blind by design, don't build without a new ruling"*). **This is the second piece of evidence for that decision.** Prod's actual buffer values were NOT verifiable from the session (prod DB is Supabase, which needs OAuth; there is no Render Postgres) — check `/admin/catalog/products` → *Event Lane VIP — 3hr* → Front buffer. Also still open on the triggering booking itself, an ops question the override does not touch: the engine's *"Ask about bowler split: 50 guests on 6 VIP lanes exceeds bowling ceiling"* flag (~8/lane × 6 ≈ 48 vs 50 heads). Memory: [[tprs-staff-capacity-override]].

- **2026-08-30 — "Paying on site" settlement terms + line-item resurrect + two follow-on money fixes (tprs PRs #134/#135/#136, migration 0160 — ALL MERGED AND LIVE IN PROD, verified: enum applied, ledger 160 = journal 160, `/health` 200).** Full narrative, traps, symptom→where-to-look table, and rollback: [session-handoffs/2026-08-30-pay-on-site-hold-and-resurrect.md](Context/session-handoffs/2026-08-30-pay-on-site-hold-and-resurrect.md) — **read it before touching `pay_on_site`, `bookings.expires_at`, the Reactivate button, the deposit-link card, or `amount_due_now_cents`.** Jon's school case: PSD 202 (INV-2026-00482, 39-guest field trip, booked 8/28 for **12/16 at 10:30am CT**, $813.00, Beth Ciszek) wants an **invoice** from TPRS and pays cash/check that gets rung into the Brunswick POS — so the money must NOT appear in the TPRS CPA report. **Two of the three pieces already existed and Jon's instinct that "we solved this" was right about the money:** (1) **ADR-0027 external settlement channel** — the `Record external payment…` button writes `settlement_channel='external'` / `rail_status=NULL`, and BOTH revenue readers exclude it by explicit filter (`cpa-report.ts:608-615`, `daily-event-revenue.ts:187-188`), so it is out of CPA revenue **by construction**; (2) **ADR-0027 §5 hosted invoice** at `/m/receipt/:token` with a copyable link + `Email receipt` on the booking page. **What was missing was the ability to HOLD the booking without a card.** A staff event sits at `pending_deposit` with `expires_at` pinned to its 48h deposit-link TTL, and `booking-expiry-cron` cancels it — which is exactly how INV-2026-00482 died 8/30 09:39 CT (`booking_expired_auto_cancelled`, `reason: expired_deposit_window`), two days after it was made, with zero payment records. A December date cannot survive a 48-hour August window. **Shipped:** `booking_flag 'pay_on_site'` + `setPayOnSite()` (`services/pay-on-site.ts`) — setting it adds the flag **and NULLs `expires_at` in the same UPDATE**, which needs no cron or availability change because both already model "no deadline" (the cron's candidate query requires `expires_at IS NOT NULL`; availability counts a pending_deposit booking's pool holds when `expires_at IS NULL`) — **so the lanes stay held and nothing reaps the booking**. The hosted invoice reads **"Invoice / Due on site"** + *"Payment will be collected on site — cash or check. Nothing is due in advance."* instead of "Receipt / Unpaid" (an unpaid document is now titled **Invoice** generally — calling a nothing-paid statement a "Receipt" was the wrong word on the one surface an AR department reads). **The terms record NO money and move NO revenue — that stays ADR-0027's job.** **Three second-order surfaces closed in the same change, each of which would have bitten:** (a) the readiness digest's red reason reads **"collect on site"** not "unpaid" (the row still belongs there — someone must collect the check on the day — it just isn't a booking falling over); (b) **the one-click "Payment Past Due — Cancellation" notice now refuses a pay-on-site booking** at `eligible()` in `email-action.routes.ts` *and* is not offered in the digest — duning a school doing exactly what we asked would be the worst use of that template; (c) **`createAveryEditBooking` preserves the NULL fuse** — without it an ordinary headcount edit silently hands the booking back the deadline the terms exist to remove, and it dies 48h later (a control test asserts an ordinary booking still DOES get re-armed, so the guard is targeted). **Verified safe against every other `bookings.expires_at` writer:** `reclampExpiryToEventEnd` and the Event Hub 48h grace both carry `isNotNull(expires_at)` guards (the grace's `GREATEST` would otherwise re-arm it, since Postgres `GREATEST` ignores NULLs). **Clearing the terms deliberately does NOT re-arm a deadline** (that would schedule a real booking's death off a button that reads like an undo); the admin page states the remaining "No deposit deadline" fact outright so the state is never silent. **Deliberately NOT done:** the manual staff `Send cancellation notice` button is still available on a pay-on-site booking — Jon's 2026-07-31 ruling made that one deliberately manual staff judgment, and the "Paying on site" banner sits right next to it. **Same branch — LINE-ITEM RESURRECT, which closes the other half.** `POST /admin/bookings/:id/reactivate` required `engine_input_snapshot != null`, so it never rendered for a booking composed line-by-line in the admin composer — INV-2026-00482 included (8 lanes @ $0, 39 × pizza/pop, a `Teacher/Chaperone` override the engine could not have produced, which is *why* it was hand-composed). **That was a silent hole for a whole class of staff events, not just this one.** New `services/line-item-resurrect.ts`; the Reactivate gate drops the snapshot condition and the route DISPATCHES — snapshot present → replay the engine, absent → the new path. **The key insight: reconstructing ENGINE INPUT from line items is lossy, but nothing about reviving a booking requires it.** Line items survive cancellation intact, so they are left completely alone — **no engine run, no catalog re-resolution, no re-pricing at all**, which is strictly MORE faithful than the engine path (that one re-quotes) and preserves the ADR-0006 never-re-price-an-in-flight-booking property. Only the pool holds are rebuilt, and every input is already persisted: window from `booking_dates` (the same source-of-truth choice the engine path makes, since a reschedule moves it without rewriting any snapshot), per-pool count from `quantity_consumed_snapshot` (which already has `resource_quantity_per_unit × quantity` baked in), and only the front/back buffers read live from product config. CORE = the persisted guest window exactly; LOCK = core widened by the largest buffer among that pool's lines. **Capacity is re-checked before the holds land** (`runStrictModeCapacityCheck`, walk-in reserves released), so reviving into lanes someone else bought fails loudly instead of double-allocating — same guarantee, same primitive, as the engine path. A magic-link token is REQUIRED (reviving into a payable-by-nothing state with a live auto-cancel deadline is worse than refusing); the token slug is never mutated, so the guest's original URL starts working again. Pay-on-site terms survive a resurrect (no deadline handed back). The audit row carries `resurrect_path: 'line_items'` + `repriced: false` — its engine sibling means the opposite — and `booking.reactivated` is emitted with a byte-identical payload so the n8n consumer needs no branch. **Verified: 10 tests incl. byte-identical line-item round-trip (same row ids — not deleted-and-reinserted), the override surviving, the capacity refusal actually firing, and the four refusal shapes.** Tests overall: 169 files / 2153 green. **TWO FOLLOW-ON FIXES the same evening, both found by Shanna asking "what if they pay ONLINE, just later?" — a school books months out, does NO deposit, and pays in full the week of.** (1) **PR #135 — the deposit-link card was gated on `expires_at !== null`, the SAME field pay-on-site NULLs.** So the card carrying the payment URL, the Copy button AND the Email-deposit-link form vanished from exactly the bookings staff collect from later; INV-2026-00482 had a live valid token and no way to see or send it. Now keys on a live TOKEN (the condition that actually decides whether there is a link), with the legacy fuse still admitted so a walk-in with neither renders nothing rather than an empty card. (2) **PR #136 — a staff edit left `amount_due_now_cents` stale, so a raised booking's pay-in-full link silently demoted to a DEPOSIT link.** `editStaffBooking` already refreshes the frozen snapshot on PaymentRecords (its own comment: *"without this refresh… the balance link would charge the PRE-edit total"*) — but a `pending_deposit` booking HAS no PaymentRecord, and its intent lives in `amount_due_now_cents`. The pay page computes `collectsFullBalance = !isInitial || amount_due_now >= outstanding`, so an $813 hold edited to $1,050 collected **$813**, left the guest in `deposit_paid` owing $237, and told them a balance was due later — precisely at the "update the event, then send the link" step. Now a link that covered the WHOLE pre-edit balance re-points to the new total; real partial-deposit links keep their staff-chosen deposit (what a deposit becomes after an edit is a pricing-policy question, deliberately out of scope) and NULL already falls back to the live balance. **⚠️ The comparison MUST use the TAX-INCLUSIVE pre-edit total** (captured before line items are replaced) — `priorTotalCents` is ex-tax while `amount_due_now_cents` is written from a tax-inclusive figure, so the obvious comparison misclassifies every taxed booking. **Balance links were never affected** (`!isInitial` always charges full outstanding) — the defect was only ever initial links edited UPWARD, which a 48h deposit window made nearly unreachable and a months-long hold makes routine. Verified non-vacuous: with the fix disabled 2 of 4 new tests go red. **Prod audit at ship time: exactly ONE live `pending_deposit` booking system-wide (Beth's), correctly set at pay-in-full — no historical under-collection, no backfill.** **Jon's rulings:** for non-online events *"IDGAF how it shows"* — the invoice-wording/two-mode design was raised and DROPPED, don't rebuild it; and these events don't get a pre-payment invoice at all (*"they maybe get an invoice"* after paying). Memory: [[tprs-pay-on-site-terms]], [[tprs-line-item-resurrect]], [[tprs-edit-keeps-pay-in-full]].

- **2026-08-23 — Header width tiers retuned to MEASURED fits; VIP SUITE had been wrapping to two lines on every viewport below ~1470px.** Jon's laptop (1920×1200 @ Windows 150% = exactly 1280 CSS px) showed the nav colliding with the Reserve CTA. Root cause: the 2026-08-03 tier boundaries were never measured against the recipes' single-line widths — the 14px recipe needs 1398px but fired at 1280px, and even the 13px squeeze recipe (1273px natural) never fit its own 1025px floor after the 5/11 logo bump + 8/03 CTA bumps. Flexbox hid the overflow at most widths by silently wrapping "VIP SUITE" to two lines (confirmed at every tested width 1025–1439 on the live site). Fix (SiteHeader.astro, all recipes headless-Chrome-measured to fit at their own min-width): nav links get `white-space: nowrap` (overflow is now honest, never a wrap); tiers are 1025 x-compact (44px logo) → 1180 compact (68px logo, covers landscape iPads) → **1280 squeeze (13px nav — what 1920@150% laptops actually see)** → 1440 mid (14px nav / 15px CTA) → **1536 wide (15px nav — 1920@125% and up, unchanged from Jon's approved look)**. Logo width now steps per tier via media query — a flex-shrink valve was tried and rejected (a flex item's min-content contribution to its parent is its *width*, not its `min-width`, so nested shrink never engages). **Trap for future edits: adding/renaming a nav item or CTA requires re-measuring the tier boundaries — and measure the last nav link's right edge against the CTA cluster's left edge, not container boxes (containers report a healthy gap while their content overflows them).**

- **2026-08-09 — /admin/new-event desktop polish LIVE (tprs PRs #75+#76) · type-to-quote quick-fill DROPPED (Jon) · GAS scratch gitignored (#77, key rotation still owed).** Jon asked for a design read on the TPRS New Event Booking page: verdict "strong engine, first-draft shirt" — the ~8-decision form stretched a narrow control column across the 1400px shell. Shipped template-only in two rounds (round 1 drew "sort of better and quite a bit worse": capped card stranded LEFT of a beige void, date+time picker wrapped, ragged pairs; round 2 centered the column, bottom-aligned pairs, 2fr/1fr uneven pairs — Jon: "way way better"): 48rem centered column, side-by-side field pairs ≥900px (phone untouched), rare knobs (holiday/all-bowling/lane-override/bowling-only) in a collapsed auto-opening Adjustments fold, quote scrolls into view after Calculate + mirrored Create/Update button under the total. Same template serves edit-event. **Jon then DROPPED the "type a sentence → populate the form" quick-fill idea** ("nice enough and quick enough I don't care") — don't re-pitch. En route: the GAS quote chat app is a second Claude-backed pricing brain drifting from TPRS by pennies (kept alive pending the parallel one-brain consolidation session), its source was copied into `tprs apps/backend/src/pricing/*.txt` **with a live hardcoded Anthropic API key** — untracked, now gitignored (#77) so `git add .` can't publish it; **rotating the key + moving it to GAS Script Properties is still owed.** Process: the shared tprs tree carried that parallel session's uncommitted WIP on the same template — my hunks were extracted by diff-against-backup and committed from a throwaway worktree; the shared tree was restored byte-identical. Memory: [[tprs-new-event-desktop-polish]].

- **2026-08-07/09 — Website session: /leagues desktop pass + content, the "main floor" sweep, promo-bar reshuffle, /feedback repoint (ALL LIVE, Website only).** Commits `49ab46a` → `b092dfb`.
  - **The 8/03 desktop type pass had a hole, and /leagues is how it surfaced.** The batch swept `-section-list` across 11 pillar pages plus the global chrome; it never touched **page-scoped** CSS. So `/leagues` had correct global `t2-*` sizing (body 17px, hero sub 18px, eyebrow 14px) wrapped around league cards still rendering mobile values on a 1900px screen — 11px labels, 14.5px dates. **When a page "still looks small" after that batch, check its `<style>` block, not the globals.** Fixed desktop-only (mobile floor list was CLOSED by Jon 8/03 and the 11px /leagues labels were explicitly ruled fine there).
  - **⚠️ The type bump exposed a latent layout bug, and the fix is the interesting part.** At the 720px prose measure a card is ~340px, and "ORGANIZATIONAL MEETING" + "Aug 31, 6:30pm" needs 334px of 293px available — **41px short**. So the label wrapped in the Monday card but NOT Tuesday's (whose values were "TBD"), leaving a matched pair ~38px out of step. **Alignment and "looks small" were the same problem:** the card grid was trapped in the text column. It now breaks out to `min(940px, 100vw - 128px)` via `margin-left:50% + translateX(-50%)`, clamped to the section gutters. Rows can no longer wrap at ANY desktop width, so cards align regardless of content — which mattered immediately, because Tuesday's TBDs became real dates hours later. **Prose stays at 720px; a spec grid is not prose.** Fit verified against real Montserrat/Roboto Slab advances (+48px slack at the tightest desktop width) — that arithmetic is the guard on adding any future row.
  - **Content:** Tuesday Night Mixed dates confirmed (meeting Sept 8 6:30pm, starts Sept 15) and a **weekly fee row on both cards — "~$23 per person."** The tilde and "Estimated" are load-bearing (figure is set by the league secretary), which is also why it is **NOT** expressed as a schema Offer: structured-data prices read as firm commitments. Clears the discount-language ban — it states a cost, it does not sell a bargain.
  - **`/leagues` Event JSON-LD shipped** (closes the 2026-05-08 SEO item, blocked 3 months on "need Tuesday dates + season end"). Four events: 2 organizational meetings + 2 recurring seasons with `eventSchedule.byDay`. Season ends Mon Apr 5 / Tue Apr 6 2027 — called a guess by Jon, but each lands on its own league weekday and yields exactly 30 sessions. Built through the hardened `eventSchema()` helper so it inherits the 2026-05-19 GSC fixes. **⚠️ TIMEZONE TRAP: the season crosses two DST boundaries (CDT→CST Nov 1, back Mar 14), so `eventSchedule.startTime` is deliberately BARE + `scheduleTimezone: America/Chicago`, while `startDate` carries an explicit `-05:00`. A fixed offset on `startTime` misreports league night by an hour Nov–Mar. Do not "fix" it to match `startDate`.** All four are date-gated off the 4am cron and self-clean.
  - **"Main floor" swept site-wide, 22 → 0** (Jon banned it 8/03; never actioned). Replacement is Jon's own: **"traditional lanes"**, already the house term in two blog posts. Two resisted the drop-in swap: the Naperville blog's "bar and arcade access is shared with the main floor" → *"the rest of the venue"* (you share a bar with people, not lanes), and a wedding-receptions bullet normalized to the sibling pages' "set apart from." **Also fixed 3 internal docstrings that QUOTE the phrase as canonical copy** — leaving those would steer the next copy pass back into the ban. FAQ answers are `plainA`/`htmlA` pairs feeding `FAQPage` JSON-LD; both halves swapped together so schema and visible copy stay in sync.
  - **Promo bar:** Free-Kids-Bowling pulled a week early (Jon) → *"Leagues Now Forming!"* alone through Aug 14 → **rotates with holiday-parties Aug 15–31** → holiday-parties alone Sept 1. Leagues takes first position (entry beat) as the more time-sensitive of the pair. **This supersedes the Aug-15 kids→holiday baton pass described in the 2026-08-01 bullet.** The `/free-kids-bowling` PAGE is untouched and still self-flips to 2027-waitlist mode Aug 15. **Aug 15 wakes rotation JS that has been dormant while the bar ran a single static promo** — worth one glance that morning.
  - **`/feedback` repointed** `app.giveme5.ai/twisted-pin` → `www.gm5.ai/twisted-pin` (verified serving "Twisted Pin - Review Bridge"). Kept a 302 — second address for this vendor. **The old URL does NOT 404; it serves a generic "Review Management" page, so anything still pointing there fails silently.** `Context/consent-surface-map.md` says the check-in trigger texts the RAW vendor URL rather than `twistedpin.com/feedback` — **if still true it is pointing at the dead domain, and it lives outside this repo.** Worth repointing it at `/feedback` so the next vendor move is a one-line repo change.
  - **Still open:** desktop type sweep on `/free-kids-bowling` (18 sub-15px values, 1 desktop query), `/coupon` (8/1), `/rewards` (11px, 0), `/upcoming-events` (partial) — survey method: count page-scoped sub-15px declarations against `min-width:1025px` query count. Also `~Sept 15` in-season reframe of the league cards (TODO in file), and the leagues promo drops Sept 1 despite Tuesday's Sept 8 meeting (Jon's call, flagged).

- **2026-08-08 — Event Hub food-form gate re-keyed to what a link COLLECTS, not the purpose enum (tprs PR #69, branch `fix/event-hub-gate-full-balance`, commit `d100196` — MERGED/DEPLOYED status pending Jon).** Jon created a short-notice catered event (INV-2026-00362, Miriam Aguilar, Aug 9, 20 guests, Stars & Strikes, $1,281.74 full-pay) and the guest pay page rendered ONE step — no food form — even though the whole point of the 2026-07-31 Event Hub is *form first, payment second, and it knows which form goes with which package.* **Root cause: the gate fired only on `purpose === 'avery_final_payment'` (server.ts, at BOTH `/api/m/verify` and `/api/m/payment-intents`), and `purpose` encodes lifecycle STAGE, not what the link collects.** It's chosen from booking status at issuance, and a pay-in-full booking jumps `pending_deposit` → `fully_paid` **without ever passing `deposit_paid`** — the only state that mints a final-payment token. So a pay-in-full catered event could never be gated at any point in its life; the whole class (short-notice events — the ones with the LEAST kitchen lead time) was invisible to the feature. The zero form-request tokens were the same single cause, not a second bug: the auto-mint lives inside that same branch. Staff had no signal either — the "Catering form" chip was nested inside the balance-link card, which only renders at `deposit_paid`. **The concept already existed and the gate wasn't using it:** `verifyMagicLinkToken` computes a three-way `pay_mode` (`final` / **`full`** = initial link whose amount-due-now covers the whole balance / `deposit`) on the very same response object the gate reads. **Shipped:** one shared `linkCollectsFullBalance()` predicate in `services/magic_link.ts`, deliberately STRICTER than `pay_mode` (which fails open toward "full" since it only drives copy) — NULL amount is not full, zero balance is not full (`0 >= 0` would arm the gate vacuously and mint a form token on a settled booking); the payment-intents gate MOVED below the amount computation (it sat above the variables it reads); a gated INITIAL link tops its deposit window to 48h with the token carried by `GREATEST` (without it the gate becomes the reason the event auto-cancels and releases its lanes) — guarded to a still-ALIVE window (reviving a lapsed one re-arms pool holds the capacity gate already released = two bookings, same lanes) and ceilinged at `created_at + 14d` (links are multi-use and the pay page verifies on load, so email scanners can re-trigger it); the admin waive control promoted to its own card with lock wording scoped to statuses that can still take money; pay-page step 2 reads "Payment" unless a deposit actually preceded it. **Jon's ruling (the invariant):** *"Deposits are formless. Final payments = form IF AND ONLY IF it is a catering menu that needs a form. Full payments = same as final."* A non-blocking informational form step on deposit links was considered and REJECTED. **Two maintenance traps that can silently break the 'if and only if':** a new catering package must be added to BOTH `lib/catering-codes.ts` and `PACKAGE_FORM_NAMES` in `services/event-hub.ts` or it never gates; and food forms are matched **by `forms.name`**, so renaming one in the admin builder silently reverts its bookings to ungated. **Process note:** the first cut of the fix shipped three real defects that only an adversarial review caught (lapsed-window revival, unbounded window sliding, the admin card claiming "payment locked" on a fully-paid booking), plus a fourth the tests caught — assigning the booking's new expiry to the token SHORTENED a 72h staff link to 48h. Full suite 143 files / 1839 tests green. **Outcome for the triggering booking:** Jon sent the payment link by hand rather than make the customer wait; she paid in full 6:53pm and then submitted the Stars & Strikes form (17 answers) at 9:36pm — i.e. paid FIRST, form after, which is exactly the sequence the gate exists to prevent. Memory: [[tprs-event-hub-gate-purpose-enum]].

- **2026-08-08 — Guests CONFIRM the booked event window instead of typing one (tprs PR #73, commit `043a2dc`, DEPLOYED; prod SQL cleanup done).** Third fix of the day, from Jon's observation that the food forms asked guests to TYPE their event date/time — and **nothing ever read the answers** (his own test typed "2pm" on a 1:00 PM booking; no surface noticed). Volume check that killed my wrong "twice a year" framing: TPRS catering only started June; July alone did 10 (~120/yr annualizing, pre-Q4-ramp). Shipped: (1) pay-page WHEN row spells out *"Sunday, September 20, 2026 · 1:00 PM – 2:30 PM CT"* (weekday + end time, bolder — every event guest crosses this page, highest-coverage prevention); (2) the guest form page renders the BOOKED window from `booking_dates` as **PAGE CHROME** (never form-builder fields — every future package form inherits it, nobody can typo it per-form) + a required confirm/dispute radio, only while the event is ahead; confirm appends ONE idempotent `[Event window]` evidence line to `internal_notes`; (3) dispute **alert-and-continues** (Jon: never a payment gate) — answers save, payment stays unlockable, and info@ (a Missive inbox, so zero new outbox/n8n machinery) gets ONE email per booked window (`admin.event_window_dispute`, migration 0137) with booked-vs-expected + admin link + Missive deep-link. The 8 typed fields + 16 junk answers deleted from prod by SQL post-deploy. **Review caught 4 real defects in the first cut:** window-INSENSITIVE dedupe markers (dispute → staff fix date → guest disputes the new date would never email again — the exact contested-booking sequence); stale-tab attestation (confirming window A after staff moved to B recorded a confirmation of unseen text — hidden `event_window_shown` field now forces re-confirm); un-locked first-dispute read (concurrent submits double-page); and the email running INSIDE the guest's save transaction (an email-rail error would roll back their food answers — effects now run in their own post-commit tx, fail-open). Memory: [[tprs-food-package-guest-forms]].

- **2026-08-08 — Payment links are never valid past the event; the pay page stops giving impossible cancellation advice (tprs PR #72, commit `69179d9`, DEPLOYED).** Follow-on from the Event Hub fix the same day. Jon, seeing a two-step pay page on a short-notice event: *"A link will never be valid PAST their event date/time."* A staff deposit link is 72h and a **balance link is 168h**, so a link for tomorrow's event stayed live for days after it (a balance link for a **full week**). New `bookingEventEndSql` ceiling in `services/booking.ts`, composed into `LEAST()` at three writers — token issuance (five of six mint paths funnel through it), the Avery token-refresh UPDATE (bypasses issuance), and the gated-link 48h grace (where `GREATEST` stays outermost so the grace can still only extend) — plus `reclampExpiryToEventEnd` on BOTH staff move surfaces (`editStaffBooking`, `rescheduleBooking`), since a move-earlier otherwise re-opened the hole. **Three deliberate non-obvious choices:** anchored on event **END, never START** (`bookings.expires_at` is also the availability read-time filter and the auto-cancel fuse — a start-clamp releases the party's own lanes as they walk in, then the cron cancels the booking and `recordExternalPayment` refuses a cancelled one, so **staff can't take their cash at the desk**); **plus a 12h `SETTLEMENT_TAIL_HOURS`** for that same reason one cron tick later (tune freely — NOTHING guest-facing displays a link duration); and **never applied to an already-past event**, because a past expiry is the same field availability reads and would silently stop counting holds. **Correction worth keeping: pool holds are TIME-RANGED** — availability only counts holds overlapping the queried window, so a stale fuse on a past event blocks nothing; the "unpaid booking holds its lanes through its own event" framing overstated the capacity harm, and the real harm was always just the link working days later. **Guest copy:** the expired panel no longer promises a replacement link that can't exist (day-of guests are pointed at the front desk), and — the thing Jon actually noticed — the static *"Cancellations must be made at least 72 hours before your reservation"* line, which is impossible advice on a booking made 24 hours out, now adapts inside the window via a server-derived `booking_summary.within_cancellation_window`. (The sibling wrong-72h figure in `balance-final-notice.eta` was already fixed in #70.) **TEST TRAP, hit twice in one session:** once expiry has an event-end ceiling, any assertion on `expiresAt − createdAt` seeded INSIDE its own TTL asserts the clamp rather than the TTL — three fixtures pinned to absolute dates would have gone red on a calendar date with no code change (the 168h one within hours of being written); all now seed relative dates, the lesson `src/test/future-event-date.ts` already recorded. 143 files / 1848 tests green. **Still queued, NOT built:** the food-form event-window confirmation (replace the two typed date/time fields with a read-only booked window + confirm/dispute radio, mismatch alert to info@ with a Missive link). Memory: [[tprs-link-expiry-event-end-clamp]].

- **2026-08-03 — TPRS booking wizard desktop size pass (all /reserve* pages).** Jon's desktop review: the ≥1025px block rebuilt the *layout* (2-col grid + sticky cart rail) but kept mobile *type/control scale* — 13.5px read-to-decide copy, ~165×112px purchase tiles on a 1920 screen, a 440px phone-sheet calendar modal. Shipped the 2026-06-17 rubric (read-to-decide 15–16 / secondary 14 / 12 floor) at desktop scale in `tprs.css`: 36px H2s, 15–15.5px decide-copy, full-column duration tiles (150px min-height), 118px+ time-slot cells w/ 16px times, 22px date chips, bigger calendar trigger, 600px modal w/ ~77px cells, frame 1140→1200px. Cart rail deliberately untouched (dense ledger — 13.5px is correct). **Process:** piloted live on /reserve only behind a `.tprs-desktop-pilot` mount class (per the "mock new patterns on one page first" rule), Jon compared against the ungated /reserve/birthdays, approved after 2 rounds → gate deleted, rules folded into the shared desktop block (`38d473c` → `6e725b7` → `a616acd`). Mobile untouched. **Parked pending Jon's mobile pass:** 13.5px `.tprs-details-body` ("What's included" sell) → 15px, and the 11px cart-bar caption → 12px. **Same-day follow-through:** two-agent site-wide sizing audit ran; Jon ruled mobile FINE as-is on the global 12px `.btn`, the sticky CTA bar, and the money-page section lists, and the NavDrawer fine on both form factors — don't re-raise. `/pricing` desktop rebuilt and APPROVED (`4516e8b`): solid `#141029` card (desktop-only exception to the 2026-05-06 transparent-calendar direction; opaque on purpose — alpha bleeds the watermark through), 72px day name / 48px rates / 16px pills, hero→card gap tightened, eyebrow 14px page-scoped. **Trap discovered:** in page-scoped `<style>` blocks a desktop media query ABOVE the base rules silently loses on cascade order — /pricing shipped 3 dead rounds that way; the desktop pass must stay LAST in the style block (comment in pricing.astro). **Desktop batch SHIPPED same day (`c00aacf`, Jon: "mostly fine with anything desktop-oriented"):** 13px `-section-list` → 15.5px @1025 on 11 pillar pages (the audit's 7 + bar/bowl/game/es-bowl which carried the same recipe), global `.btn` 12→14px @1025 (component-scoped rules keep their tuned values), global `.t-eyebrow` 11→14px @1025, menu-item dialogs 480→640px + 44px close + 11px badges, /upcoming-events first desktop type rules (meta/desc 16px, CTA 13.5px). Header shipped separately (`c01a0a3`+`07f10fc`): squeeze-zone-aware tiers — 1025–1279 keeps 13px nav (7 items barely fit), ≥1280 nav 14px + CTAs 15px, ≥1440 nav 15px. **Follow-through rounds (same day):** /vip-suite hero dead-air fix approved → swept to all 13 sibling typography heroes (`418651b` — min-height 55vh/640px flex-centered voids → content-defined height); inline 11px eyebrow styles stripped from 21 H2s across 11 pages so the global 14px desktop eyebrow actually lands (verified no element-h2 rules were being guarded against); /menu/taps legibility round 2 (name 20px, meta 16px + one color tier up); **NavDrawer VIP Suite "glint"** (`58ef3c4`+`c2a764d`) — periodic light sweep, keyed to drawer-open (`aria-hidden="false"`) with the sweep at the front of the cycle (500ms after open, then ~6.5s rhythm; page-load-clocked version landed mid-rest), reduced-motion gated. **Mobile-side items CLOSED (Jon reviewed the full sub-44px tap-target list + floor fixes and ruled them not needed, end of session):** form floor fixes (12.5px SMS consent, 11px /leagues labels), the ~28px Edit targets, card-link/footer/promo-dismiss tap targets, /rewards inline 13px, footer explore links + 36px social icons — all deliberately left as-is. Don't re-raise without a new trigger (real-user complaint, Lighthouse regression, or an a11y audit).

- **2026-04-30 — Hero copy locked.** Eyebrow / headline / subhead / CTA all locked (see table above). Type stack approved with substitute fonts (Barlow Cond / Montserrat / Roboto Slab). Eyebrow shifted from Glow to warm-white; Glow reserved for primary CTA.
- **2026-04-30 — *"The bar that bowls."* deprecated.** Removed from locked copy. Reads as a tagline competing with the headline.
- **2026-04-30 — *"Built for adults. Kids will come."* revised** to *"Built for adults. Fine, bring the kids."* Concessive register replaces the Field-of-Dreams reference. Also re-classified from hero subhead to hero headline.
- **2026-04-30 — Per Se / Keller / Michelin three-star framing retired** for the website's primary positioning. *"America's Top Mixologist"* (Food Network) is the lead credential. The Per Se framing borrowed more prestige than the consulting relationship warranted and pulled the brand toward fine-dining-with-bowling, which is not the locked thesis. Per Se context may still appear in long-form `/craft-bar/` body copy.
- **2026-04-30 — Astro project initialization authorized.** Handoff doc claimed Astro was already initialized — this was wrong. Root contained only `node_modules` (sharp + image utilities), no `package.json` or `astro.config.mjs`. Initializing fresh as part of the live hero deliverable. The "don't initialize until structure planning is approved" rule is now satisfied — visuals are locked enough to ship the hero.
- **2026-04-30 — Sticky CTA bar always-visible on mobile** (vs. hide-on-scroll). Primary conversion element; ~64px is small relative to viewport. Hide-on-scroll can be A/B'd later.
- **2026-04-30 — Self-hosted fonts via `@fontsource`** (vs. Google CDN). LCP-critical: removes second-origin DNS+TLS, enables explicit preload control.
- **2026-04-30 — Vercel deploy via GitHub integration** (vs. CLI direct). Auto-deploys per branch from day one per the established workflow.
- **2026-04-30 — Hero subhead changed from credential to stats trio + menu beat.** New copy: *"28 self-serve taps · A 6-lane VIP suite · 17 traditional lanes · A chef-inspired menu"*. Reason: the old subhead *"Built by America's Top Mixologist…"* started with "Built by", which carried through from the headline *"Built for adults. Fine, bring the kids."* — readers were misimplying the mixologist had built the venue itself, not just the bar program. The credential lines moved to reserved-copy for the future cocktail/bar section H2 (see voice.md).
- **2026-04-30 — In-hero CTA killed.** *"Reserve a lane"* button removed from Hero. The global sticky bar (always-visible bottom on mobile, top-right on desktop) carries 100% of hero conversion now.
- **2026-04-30 — Headline overflow on narrow viewports fixed.** Settled on `clamp(34px, 11vw, 80px)` + `letter-spacing: -0.015em`. Verified glyph fit (not block fit) at 360 / 390 / 412 with 8.78 / 13.09 / 16.31 px slack via Range API.
- **2026-04-30 — Brand mark wired as image.** `LogoGBED_Horizontal_White` for the hero (mobile 41px / desktop 56px after two rounds of size bumps). `Logo_Horizontal_GlowInTheDark` (no GBED tagline) for the drawer header (mobile 73px / desktop 84px).
- **2026-04-30 — Drawer header *"The works."* retired.** Replaced with the GlowInTheDark logo per the Pints & Paddle / Swingers pattern.
- **2026-04-30 — Drawer rows get Lucide line icons.** Right-aligned, 24px, `currentColor`. 4 directly from Lucide; bowling pin hand-drawn in matching stroke style (Lucide doesn't ship one).
- **2026-04-30 — Mobile hero video splice direction locked.** Three sources: pour (Bank Vs Stories) → tap wall (Beer Wall) → cocktail (Best Things To Order). Specific window timestamps pending user. Currently live: Bank Vs Stories single 4s shot.
- **2026-04-30 — Workflow returned to direct-to-main pushes.** One-time `feat/hero-round-2` feature-branch round complete; iteration cadence too fast for ongoing branch ceremony. Vercel auto-deploys on every push to main.
- **2026-05-01 — Persistent SiteHeader shipped (Phase 1 of desktop architecture).** Logo + inline nav (BAR · EAT · BOWL · GAME · EVENTS · MORE ▼) + two solid CTAs (Reserve = Glow, Plan = Copper) on desktop; logo + hamburger on mobile. Resolves the long-standing desktop CTA placement question — both elements now share a single header bar. Active-page indicator: 1px Glow underline, current page only, never on hover. MORE dropdown click-only (no hover-open) for accessibility + touch reliability in 1024–1279px range.
- **2026-05-01 — Live hero copy promoted to match `/snap-test/`.** Eyebrow *PLAINFIELD, IL* and stat-trio subhead retired from `/`. New subhead: *"Plainfield's premier night out. Bowling optional."* Same headline. Brings the live hero in line with the snap-test staging hero — was reading stale on the deployed desktop.
- **2026-05-01 — Coupon banner removed from desktop chrome.** Built and shipped in Phase 1, then killed after review of the deployed treatment. Reasons: (1) didn't earn its space — read promo-y, undercut premium positioning; (2) auto-hides on scroll past hero anyway, so impact was limited; (3) coupon stays reachable via MORE dropdown (`/free-10/`) and footer — those surfaces are sufficient for the audience that wants it. Component file retained at `src/components/CouponBanner.astro` in case it returns; import commented out in `Base.astro`.
- **2026-05-01 — `.btn-copper` added globally.** Documented exception to the "Glow only on the primary CTA" rule. Reserve a Lane (Glow) and Plan an Event (Copper) are co-equal primaries for two different audiences. Contrast verified: Copper #D88B5C with Indigo Deep #0E0A1F text computes to ~6.9:1 (passes WCAG AA at 4.5:1). Mobile sticky CTA's "Plan an event" switched from outlined to copper-solid for brand consistency. **REVERSED 2026-05-01 same day** — see entry below.
- **2026-05-01 — Two co-equal primary CTAs decision REVERSED.** Reviewing the deployed Glow + Copper treatment, two saturated solid buttons competed for attention and read less premium than the brand direction calls for. New treatment, locked: *Reserve a Lane* = primary (Glow solid, Indigo Deep text); *Plan an Event* = deliberate alternate (Indigo Deep solid background + warm-white outlined border + warm-white text). Strategic frame: Reserve is highest-volume / most impulsive intent; Plan is lower-volume / higher-LTV — the motivated user finds the quieter button. **This is the restraint-as-confidence pattern, not a demotion of Plan an Event.** `.btn-copper` class removed from `global.css` (no usages remain). Same hierarchy applies in the desktop header and mobile sticky bar.
- **2026-05-01 — MORE dropdown polish.** Bumped from flat opaque (`rgba(14,10,31,0.95)` + blur 14px) to premium glass (`rgba(14,10,31,0.78)` + blur 18px saturate 140%) so the backdrop blur actually shows through. Border opacity bumped 0.10 → 0.14. Single drop-shadow replaced with a two-tier shadow (`0 8px 16px rgba(0,0,0,0.30), 0 24px 48px rgba(0,0,0,0.45)`) for depth. Picks up the persistent header's chrome vocabulary; sits between fully translucent (header) and fully opaque (modal) on the spectrum because dropdown text needs to read cleanly.
- **2026-05-01 — MORE dropdown overflow fix.** Initial build had `.site-nav ul { display: flex }` which inadvertently selected the dropdown's inner UL too — turned the dropdown into a 663px-wide horizontal flex row that overflowed off the left edge of the viewport. Scoped to `.site-nav > ul` and added explicit `display: block` on `.more-dropdown` for safety.
- **2026-05-01 — Mobile hero logo retired.** `.hero-brand` removed from `Hero.astro` (live `/`) and from the `/snap-test/` snap 1 inline hero. The persistent SiteHeader's logo is now the only logo across all routes. Resolves the mobile-hero-logo redundancy flagged in the desktop architecture brief.
- **2026-05-02 — Phase 2A shipped: parallel mobile/desktop tracks on `/snap-test/`.** Mobile snap-track preserved exactly (8 sections, scroll-snap mandatory, locked). Desktop track adds 4 weighted-height placeholder sections — Tap Wall (70vh), Cocktails (70vh), Events (60vh), EBG (70vh, hidden via CSS until 2E) — that swap in via `display: none/flex` at 1025px. New `[data-reveal]` initial state + `sectionReveal()` motion module (IO + window/.snap scroll fallback, 450ms fade-up). DOM verification at 1280×800 desktop confirms 3 visible desktop sections at 560/560/480px (70/70/60vh) + collapsed mobile-only sections; total scroll ~3200px (down from ~6400px in the old long-scroll fallback). DOM verification at 390×844 mobile confirms 8 mobile sections render unchanged. Reveal motion logic verified by bypassing RAF (headless preview tab is `document.hidden = true`, which gates RAF callbacks — production browsers will fire normally). Phase 2B–2D queued (real content for the three split sections); Phase 2E gated on social-team stills.
- **2026-05-02 — Bowl gets two copy variants.** Mobile snap 6 keeps the locked VIP-leading line + parenthetical aside (*"17 traditional lanes plus a 6-lane VIP suite. (yes, you can take it over)"*). Desktop EBG card uses a tighter no-aside variant (*"17 lanes plus a 6-lane VIP suite."*) because the 33%-width column has no room for the aside. The original handoff variant — *"17 lanes, ready when you are."* — was rejected: dropping VIP from a card labeled "BOWL" reverts the card to Bowlero positioning (we have lanes), regardless of the VIP framing in the Events section above. The thesis is "when bowling shows up on the homepage, it's the VIP suite specifically" — the card has to carry that.
- **2026-05-02 — Cocktails long-form promoted from reserved to active (desktop only).** *"Cocktails this serious aren't supposed to live at bowling alleys. Built by America's Top Mixologist."* — was held as reserved copy for "a future cocktail/bar section on `/` that has more room than the snap-test stub." The desktop Cocktails section (50/50 split, ≥640px copy column) IS that room, so the line was promoted. Mobile snap 4 stays terse with the original *"Built by America's Top Mixologist. (their words, not ours)"* — a snap-stub doesn't have the breathing space for two-sentence copy. Two-variant model matches the Bowl decision above.
- **2026-05-02 — voice.md cleanup.** Three updates landed alongside Phase 2A. (1) Hero Locked Lines table updated to reflect the live `/` matching snap-test (eyebrow retired, subhead is *"Plainfield's premier night out. Bowling optional."*) — the 2026-05-01 promotion was previously documented in the Decisions Log but voice.md still showed the old eyebrow + stat-trio. (2) New "Desktop track copy" section documents the two Bowl variants and the Cocktails long-form. (3) "Reserved copy" section now empty — Cocktails long-form promoted; deprecated entry list updated.
- **2026-05-02 — Phase 2B shipped: Tap Wall split layout + sectionVideo() helper.** Desktop section 2 replaces its Phase 2A placeholder with a 50/50 grid: Spotify-style vertical 9:16 frame on the left (responsive height `min(640px, calc(70vh - 80px))` to guarantee 80px section breathing on common viewports), copy column on the right with the locked statement/aside pattern (*"28 self-serve taps / (only wall in the area)"*). Frame chrome: 1px warm-white border at 12% opacity, 12px radius, two-tier drop-shadow. Section background dropped to Indigo Deep so the frame stands out. Reuses the existing `/snap/beerwall-mobile-{av1,h264}-1080.mp4` source — the 9:16 portrait video is frame-friendly. New `sectionVideo()` motion module enforces the single-active-video rule across `[data-section-video]` elements: per-video IntersectionObserver at threshold 0.5 plays/pauses, lazy-source promotion at ratio > 0 (saves bandwidth before user reaches Tap Wall), `muted` enforced on every state transition (audio collision structurally impossible). Stacked-section geometry (Hero 100dvh + Tap Wall 70vh > 100vh) makes ratio > 0.5 mutually exclusive, so no central state manager needed. Hero desktop video and Tap Wall video opt in via `data-section-video`; mobile videos intentionally NOT marked — the locked mobile tapwall keeps its existing inline handler. Verified at 1280×800 / 1440×900 / 1920×1080 (frame at 270×480 / 309×550 / 360×640 with 80–116px breathing). Mobile DOM unchanged at 390×844 (8 visible sections, mobile tapwall handler intact).
- **2026-05-02 — Phase 2C shipped: Cocktails mirrored split + long-form credential + solid-default cleanup.** Desktop section 3 replaces its Phase 2A placeholder with a 50/50 grid that inverts Tap Wall's layout — copy on the left, 9:16 still frame on the right, so the two F&B cornerstones alternate visually instead of stacking the same composition twice. Long-form credential line *"Cocktails this serious aren't supposed to live at bowling alleys. Built by America's Top Mixologist."* promoted from voice.md reserved-copy to active. Two complete claims set on their own lines (provocation → credential), same weight/size — the credential is the load-bearing line, not a parenthetical aside. Asset: AIV01579 (copper-shaker pour) per handoff fallback. Real cocktail video remains held for the API batch round; swap point is replacing the `<picture>` with a `<video data-section-video>` + lazy-source pattern. `scripts/build-snap-images.mjs` refactored to a `SOURCES = [{src, name}, ...]` config with skip-on-missing-source warnings — fresh worktree pattern stays smooth. Encoded `/public/snap/cocktails-bg-{540,1080,1280}.{webp,jpg}` (24.7–60.8 KB webp). **Sub-decision:** solid Indigo Deep is now the default for all `.snap-section.desktop-only` (was a Phase 2A placeholder gradient). Rationale: structural variation between sections (frame-left / frame-right / full-bleed photo / 3-card row) already does the rhythm work; gradient variety on top is doubling up, and [visual-direction.md](Context/visual-direction.md) "remove decoration that doesn't earn its space" rule applies. The Tap Wall and Cocktails explicit overrides became redundant and were removed. Frame chrome on Cocktails duplicates Tap Wall's intentionally — refactor to a shared `.media-frame` utility flagged for the third use (rule of three). Verified at 1440×900: Cocktails section 630px (70vh), grid 544/544 + 64px gap, copy at col 1 (left edge 137), frame at col 2 (left edge 862), 309×550 frame matches Tap Wall's mirrored counterpart. All four desktop section backgrounds confirmed solid Indigo Deep (`rgb(14,10,31)`) with `background-image: none`. Mobile DOM unchanged at 390×844 (8 sections, Cocktails desktop section hidden).
- **2026-05-03 — Mobile snap titles harmonized to nav labels.** Snap 5 *Food* → *Eat*, snap 7 *Arcade* → *Game* — matches the locked SiteHeader inline nav (`BAR · EAT · BOWL · GAME · EVENTS`) and the desktop cluster section titles. Wayfinding wins over voice-table preservation when the nav and the section H2 are on screen together (user clicks GAME → lands on a section labeled GAME, no mental translation). voice.md 8-snap table updated to match. Locked surface (nav) wins over unlocked label table (voice.md).
- **2026-05-03 — NavDrawer hrefs harmonized to post-Phase-1 SiteHeader IA.** Drawer was built before Phase 1 SiteHeader and pointed at phantom slugs (`/craft-bar`, `/menu`, `/find-us`). Five slots retargeted to `/bar`, `/eat`, `/vip-suite`, `/events`, `/#find-us`. 5-slot structure preserved; broader sectioned-hamburger restructure (sitemap-tldr Q7) deferred post-launch.
- **2026-05-03 — `/snap-test/` promoted to `/`.** Live `/` was just `<Hero />` since Phase 2 shipped — the 8-snap experience lived at staging. Promoted via `git mv snap-test.astro index.astro` + metadata update. `/snap-test/` route gone (404). `src/components/Hero.astro` is now unused but retained for reference. Site isn't publicly consumable yet so no redirect needed.
- **2026-05-03 — `/bar` pillar mock landed; establishes inner-page rhythm.** First inner page. Pattern: hero (full-bleed image, headline, sub — NOT snap-mechanic) → long-scroll editorial sections (image one side, copy the other, alternating) → closing CTA band → SnapFooter. Inner pages are normal long-scroll; snap is reserved for `/` where the tasting-menu format earns it. Same rhythm to be applied to `/eat`, `/bowl`, `/game`, `/events`, `/vip-suite`, `/reserve` in series. Hero photo is stub (cocktails-bg-900 standing in for the Jan 29 atmosphere shot); GoTab API integration deferred for the two "View menu" CTAs.
- **2026-05-03 — Per Se / Keller / Michelin three-star framing FULLY retired** (no carve-out). The earlier 2026-04-30 retirement allowed the Per Se anchor to appear in long-form `/craft-bar/` body copy. That carve-out is now retired — Per Se / Keller / Michelin three-star does not appear *anywhere* on the website, including long-form body. Reason: brand prefers to lean entirely on the Food Network credential without the fine-dining anchor; the long-form carve-out was creating drift between intent and implementation. Updated: voice.md (lines 126, 131, 144–145, 160), CLAUDE.md table + thesis, visual-direction.md cocktail-program description, `/bar` cocktails section body. Long-form credential anchor on `/bar/` is now: *America's Top Mixologist* (Food Network) + cocktails served in 40+ countries + the consulting-relationship framing.
- **2026-05-03 — Pin Tilt watermark opacity tuning.** 5% (functionally invisible) → 12% (too loud IRL) → 8% (still loud) → 6% (final). Single value, dialed against deployed visual not screenshots.
- **2026-05-03 — Pin Tilt watermark lifted to global `body::before`.** Was `.snap::before` scoped to the homepage only. Inner pages (`/bar`, `/eat`, every future pillar) now get the watermark automatically without per-page CSS. Implementation: `body { position: relative }` + `body::before { position: fixed; z-index: -1 }`, with `html { background }` (moved from body) so painting-order is clean. Inner-page editorial sections changed from `background: var(--indigo-deep)` to `transparent` so the watermark shows through their gutters — same rhythm as the homepage cluster sections. Hero (image bg) and closing CTA band (lifted indigo bg) naturally cover the pattern in their regions.
- **2026-05-04 — "Private" terminology fully banned, voice.md contradiction fixed.** voice.md already had the rule documented (lines 81–83: no "private bar," no "private space," no "private" as a descriptor for the VIP suite — it's *semi-private*) but **also listed "private" in the approved Words to Use list** (line 52) — direct contradiction. Removed from approved list. Swept `/vip-suite` for all 5 instances of "private": eyebrow ("Private Events" → "Group Events"), hero sub ("Six private lanes" → "Six lanes of your own"), section body ("Six private bowling lanes, separated from the main floor" → "Six bowling lanes set apart from the main floor — yours for the night when you book the suite"), section list ("6 private lanes" → "6 lanes of your own"; "private functions" → "group functions"). Also patched seo.md H2-question example ("Can I host a private corporate event?" → "Can I take over the VIP suite for a corporate event?"). User had flagged this rule multiple times — Working Preferences in this file now calls it out at top-level so it's not buried.
- **2026-05-04 — `/vip-suite` added to MORE dropdown.** Page was an orphan on desktop (only reachable via mobile NavDrawer). Added to the top of `moreItems` in SiteHeader.astro — most-likely-to-click position in the dropdown. The 5-slot inline nav stays locked at BAR · EAT · BOWL · GAME · EVENTS; VIP Suite as a tier-1 conversion page belongs in MORE rather than crowding the inline strip.
- **2026-05-04 — VIP Suite promoted from MORE dropdown to inline desktop nav.** Was at the top of MORE (added earlier today). User: "the VIP Suite should end up in the top bar, maybe to the right of Events. That's a critical enough item to put up there." Inline nav grows from 5 items to 6: BAR · EAT · BOWL · GAME · EVENTS · VIP SUITE · MORE ▼. Sits right of Events as the natural events→suite pairing. The "5-slot inline nav locked" line from earlier decisions is superseded — VIP Suite as the revenue engine earns its inline slot. Mobile NavDrawer also lists it directly, unchanged.
- **2026-05-04 — Voice rule clarifications: distinguish hard bans from positioning rules.** Earlier audit framed `bowling alley` and `family-friendly` as bans alongside `private` and `cheap` — overstated. Refinements: (a) "Bowling alley" is **fine to use descriptively** (and the brand uses it deliberately in `"This isn't your parents' bowling alley."` and `"Cocktails this serious aren't supposed to live at bowling alleys."`) — the rule is just "don't lead with it as the primary descriptor." (b) "Family-friendly" is a **TRUE characteristic** of the venue (most attendees are families) — copy positions adults-first because the adult is the one who needs convincing (inverse of Chuck E. Cheese). Don't deny families; don't market with "family-friendly fun" as the lead. (c) Hard bans elevated to Working Preferences: `private`, `cheap/discount/value/deals/budget-friendly`. Soft positioning rules stay in `voice.md` only. Updated voice.md "Generic / flat" entries with this nuance. Pre-launch copy sweep should grep for the hard-ban list, not the soft-positioning list.
- **2026-05-04 — `/contact` page killed; "Contact" in MORE scrolls to SnapFooter.** User: "I have no intention of providing an actual contact page any longer." Everything users need to contact (address, hours, phone, email) already lives in SnapFooter. Implementation: SnapFooter `<section>` got `id="find-us"`; MORE "Contact" item href changed from `/contact/` → `#find-us`; NavDrawer "Find Us" item changed from `/#find-us` → `#find-us` (same anchor pattern, scrolls in-place on whatever page rather than forcing nav to /). Label "Contact" stays in MORE — the destination is what changed, not the call-to-action language. At launch, legacy `/contact-us/` 301 redirect target should be reconsidered (probably → `/`, since hash anchors don't survive 301s — user lands on homepage and finds the footer themselves).
- **2026-05-04 — MORE dropdown polish (3 rounds same day).** Round 1: section labels ("Programs & Events" / "Useful") + divider between groups + 220→260px width + 12→13px type. Round 2: dropped section labels (categorization didn't earn its weight), added per-item divider via `li + li > a { border-top }`, bumped header gap 6→18px so the panel lands clearly below the header band. Round 3: added 16px Lucide icons left of every item (calendar / ticket / clock / trophy / star / gift / help-circle / mail / briefcase) at 0.7 opacity → 1.0 on hover, currentColor; added `/coupon` (renamed from legacy `/free-10` — old URL gets a 301 to `/coupon` at launch) and `/waitlist` to the dropdown. Item order: visit-now intents first (Upcoming Events, Coupon, Waitlist), then engagement (Leagues, Rewards), then purchase (Gift Cards), then utility (FAQ, Contact, Careers). Pages for `/coupon` and `/waitlist` not yet built — they 404 until built, same situation as `/events`/`/bowl`/`/game`/etc placeholder slugs.
- **2026-05-05 — GoTab integration shipped (cocktails + food).** Auth: OAuth Client Credentials against `https://gotab.io/api/oauth/token`. Non-standard token response (`token` / `tokenType` instead of `access_token` / `token_type`) handled in `src/lib/gotab.ts`. GraphQL endpoint `https://gotab.io/api/graph` requires `locationUuid: String!` (not `id: ID!`) — schema discovery via `/api/gotab/schema` introspection probe surfaced the correct shape after initial guesses failed. Source menus selected: "View Only Cocktail Menu" for cocktails, "View Only Menu" filtered to food categories (Drinks 62068 excluded — soft drinks/water/juice boxes aren't a food menu beat). **No prices on the website** — premium positioning, captive-audience-pricing logic (movie theater / ballpark). **Category names rendered exactly as in GoTab**, including the truncated `"Cocktails - Reimagined & Handcrafted Fav"` (user opted to leave the truncation rather than override in code; GoTab is single source of truth for naming). 3 env vars: `GOTAB_CLIENT_ID` / `GOTAB_CLIENT_SECRET` / `GOTAB_LOCATION_ID`. Build-time fetch via `src/lib/gotab-cocktails.ts` + `src/lib/gotab-food.ts`.
- **2026-05-05 — Untappd Business integration shipped (tap list).** Auth: HTTP Basic with `email:api_token` base64-encoded — no OAuth dance, the API token is the long-lived credential. REST endpoint `https://business.untappd.com/api/v1`. Hierarchy: location → menus → sections → items. Confirmed: 28 taps map exactly (23 Draft Beer + 1 NA Draft + 4 Wine on Tap) — brand thesis aligns with the wall. Visual rhythm deliberately **list-led, not photo grid** (different job than cocktail menu — beer lists are scannable text with tap-number anchors). Each row: small label thumbnail (40/48px) + Glow tap number + beer name + brewery·location·style metadata + ABV. Filtered out Untappd's generic placeholder images at the lib level so we don't render 28 identical default-beer icons. 3 env vars: `UNTAPPD_EMAIL` / `UNTAPPD_API_KEY` / `UNTAPPD_LOCATION_ID` (note: `_KEY` not `_TOKEN` — matches the Untappd dashboard naming).
- **2026-05-05 — Daily auto-rebuild cron shipped.** Menu data is fetched at build time and baked into static HTML, so without a rebuild trigger, GoTab/Untappd edits stay invisible until someone pushes code. Vercel Cron entry in `vercel.json` (`0 9 * * *` = 9 UTC = 4–5am Central, after closing) hits `/api/cron/rebuild`, which validates `Authorization: Bearer ${CRON_SECRET}` (auto-injected by Vercel Cron) then POSTs `VERCEL_DEPLOY_HOOK_URL` to queue a fresh production build. Hobby-tier cron has a 1-hour flexible window — fires somewhere in 9–10 UTC, fine for menu data. Verified working end-to-end: manual "Run" via Vercel cron page returned 200, deploy hook fired, fresh production deploy went live.
- **2026-05-05 — Menu pages extracted to `/menu/*`.** User flagged: `/bar` at ~8000px mobile (~10 viewport heights) meant <20% of users likely saw the cocktail menu, which is the primary differentiator. Extracted to three dedicated pages: `/menu/cocktails`, `/menu/taps`, `/menu/food`. Each one: typography hero (eyebrow + H1 + 1-sentence SEO sub) + the menu component + a small SEO copy block (H2 + 1–2 paragraphs of keyword-relevant context, includes the address) + SnapFooter. **No image hero** on menu pages — would rebuild the scroll problem we just solved, and these are reference pages, not editorial. `/bar` and `/eat` retained their editorial structure with the 5-pick teasers; CTAs repointed (`View what's on tap` → `/menu/taps`, `View the cocktail menu` → `/menu/cocktails`, `View the menu` → `/menu/food`). Menu URLs use `/menu/*` namespace rather than `/bar/cocktails` so the menu URLs stay stable if pillar pages ever rebrand. Hero `data-hero` attribute originally missing on the new pages — caught + fixed (initial state was `opacity: 0` waiting for JS animation that never fired).
- **2026-05-05 — Mobile sticky CTA bar: backdrop strip below pill, no gradient haze above.** User flagged the floating pill had visible page content in the 12px gap below it — read as "pill mid-page" rather than anchored. First attempt added a gradient fade from transparent (above the pill) to dark (below) — landed visible blur haze above the pill that obscured content. Reverted. Final treatment: pill bottom margin tightened 12px → 6px, plus a solid (no-gradient) backdrop strip from viewport bottom up to where the pill starts (z-index 29 vs pill's 30, same blur + Indigo Deep wash as the pill). Pill reads as seated on a continuous base. No haze.
- **2026-05-04 — `/waitlist` shipped as iframe wrapper; webhook-derived state version explored and tabled.** Page wraps `host.tablesready.com/p/waitlist/twistedpin` in brand chrome (typography hero + framed iframe + closing Reserve CTA + SnapFooter). TablesReady has no read API and no X-Frame restrictions, so iframe is the only path to live data without infrastructure. User flagged inner-iframe content as ugly; explored four paths to improve it (CSS scroll-clip hack, switch platforms, build-own + Twilio, webhook-derived state). Picked **webhook-derived state** as the right path (TablesReady webhooks fire `party.created`/`updated`/`checked_in`/`marked` — derive count by tracking adds/removes, daily 4am reset to mitigate state drift). **Tabled pending:** user upgrades TablesReady plan tier → runs webhook.site test to capture real payloads → resolves `party.checked_in` ambiguity (could mean "remove from active" or "no-op for reservations"). Full theory + revisit checklist captured in [Context/waitlist-theory.md](Context/waitlist-theory.md). User preference noted: "send them off the site if they're going to see crap" — i.e. link-out > current iframe IF we're not building webhook version yet (page-swap decision still open).

- **2026-05-05 — Build-out + perf + DNS planning session shipped.** Major work bundle (full session captured in [Context/session-handoffs/2026-05-05-build-out.md](Context/session-handoffs/2026-05-05-build-out.md)):
  - **`/menu` hub page** (7th inline nav slot between Eat and Bowl) with horizontal-split cards + real hero photography (DSC_0110 cocktail / DSC05701 food platter / BeerWallHeyFlow tap wall, square 1:1 crops via attention-based focal detection). Card-body desc tightened to 1 sentence; title scaled to clamp(26-32px) for narrower body column. Padding on inline nav links tightened 12 → 10 horizontal to fit 7 items at the 1025 breakpoint.
  - **`/privacy`, `/terms`, `/accessibility`** — three compliance pages built. Privacy + Terms ported from live twistedpin.com with light voice/format cleanup; Accessibility is a standard hospitality-venue statement (WCAG 2.1 AA target, known limitations, 5-business-day remediation SLA). All three flagged for counsel pass before launch. Last-Updated dates preserved from the live policies for Privacy + Terms; Accessibility uses today.
  - **Global `:focus-visible` ring** added to `global.css` — keyboard users now get a 2px Glow outline on Tab. Browser defaults read poorly on Indigo Deep. `:focus-visible` (not `:focus`) so mouse clicks don't trigger the ring. Components with their own focus styles (`.menu-hub-card`) continue to use those.
  - **`/rewards` rebuilt** against actual program details (front-desk kiosk + 50pts/visit + 5-tier ladder + Text Club). Voice reframes: `$10 OFF Any Open Bowling` → `$10 lane credit` (drops discount framing, keeps dollar accuracy); `Score Stellar Deals + Exclusive Discounts!` → inside-line framing; `Bogo Hour` → `Buy an hour, second hour on us`.
  - **`/coupon` native form + Patch Retention API** — replaced legacy iframe. Architecture: browser → `POST /api/coupon-signup` (Astro server endpoint) → `PATCH api.patchretention.com/v2/contacts?match:phone={phone}`. Auth requires TWO headers (Bearer + `X-Account-Id`), caught via bundle scan. Birthday parsed from MM/DD form input → `MM/DD/1900` (placeholder year for Patch's date field). Voice reframe: `$10 OFF Open Bowling Certificate!` → `$10 toward your next lane.` Native dropdowns replaced with a single MM/DD text input (selects can't be styled cross-browser). Earlier broken `POST /v2/tags` step removed — no contact-tag association endpoint exists in v2 API. **Trigger pattern is OPEN** — pending Patch support response on DOI / event listener / canonical pattern. Submissions land in Patch's contact list; coupon SMS path TBD. `PATCH_API_KEY` + `PATCH_ACCOUNT_ID` set on Vercel.
  - **DNS migration runbook** captured in [Context/dns-migration.md](Context/dns-migration.md). Captured the full DNS audit (NS, MX, SPF, DKIM, DMARC, Freshdesk CNAMEs). Flagged: SPF record looks malformed (`include.com.spf.auto.dnssmarthost.net` missing colon) and may be causing silent permerror → DMARC quarantine. Cutover plan: GoDaddy DNS (consolidate at one vendor — registrar = ownership = no dev-company cooperation needed). Includes step-by-step + risk mitigations.
  - **Lighthouse baseline + LCP optimization pass.** Pre-fix: Desktop 100/100/100/100; Mobile 86 with LCP 3.9s (yellow band, all other Web Vitals excellent). Five fixes shipped same day: (1) mobile hero serves 540w on phones ≤480px (saves ~1 MB) — single-line markup change, files were already encoded but never referenced; (2) hero poster preload was already in place; (3) Latin-only `@fontsource` subsets (CSS file 5x smaller per face, was importing Vietnamese/Cyrillic/Greek/Latin Ext on an English-only site); (4) tap wall video gains 540w variants (encoder updated + ran); (5) AVIF added to `scripts/build-snap-images.mjs` (15-30% smaller than WebP, applied to menu hub `<picture>` tags). Deferred: CRF tuning + AVIF for non-menu-hub `<picture>` tags. Full history in [Context/perf-history.md](Context/perf-history.md). **Re-test discipline:** wait until real photography + final hero splice land, then re-run PSI once and compare against the 2026-05-05 baseline.
  - **TikTok removed from SnapFooter** — user confirmed business has no TikTok presence currently. Icon removed from social row; URL constant deleted. Code comment notes how to re-add when/if account launches.

- **2026-05-06 — Redirects + events + NYE + pricing session shipped.** Major work bundle (full session captured in [Context/session-handoffs/2026-05-06-pricing-events-redirects.md](Context/session-handoffs/2026-05-06-pricing-events-redirects.md)):
  - **301 redirect map** landed in `vercel.json` — 18 legacy URLs preserved per launch-checklist (e.g., `/bowling/` → `/bowl/`, `/free-10/` → `/coupon/`, `/contact-us/` → `/`). Path-to-regexp `{/}?` for optional trailing slash; 3 truncated old-promo slugs use prefix `:rest*`. PLUS `/essential` + `/elevated` SMS short links (302) for text-marketing flows pointing at `menu.twistedpin.com/{slug}` — DO NOT REMOVE.
  - **All "Plan an Event" CTAs swapped to direct Zite link** (`https://twistedevents.zite.so/`). Mirrors the Reserve → Roller pattern. 10 conversion-intent CTAs swapped (SiteHeader, NavDrawer × 2, StickyCTABar, /vip-suite, /events closing band, /gift-cards, /faq × 2, /upcoming-events). Educational links kept on local /events.
  - **`/upcoming-events` rebuilt as real calendar** — Astro content collection (`src/content/events/*.md`) with Zod schema (title / start / end / location / cta / tentative / virtual / draft). Cards group by month; multi-day cross-month renders range form ("31–1" + "DEC/JAN"). Empty state: *"Calendar's clear right now. Want to fill it? Plan an event →"*. First entry: 2026-12-31 NYE event linking to /new-years-eve/.
  - **`/new-years-eve` page + seasonal-nav system.** Full-bleed NYE.mp4 video hero (1080×1920 source → /public/snap/nye-* in 4 variants matching beerwall pipeline). Editorial section + closing CTA band (Plan an Event Glow / Reserve a lane outlined). Body copy is voice-y placeholder until ops gives package details. **`src/config/nav-seasonal.ts`** is the new SoT for seasonal page visibility — pages exist at URL year-round but only surface in NavDrawer between `showFrom` / `showUntil`. NYE shows Nov 15 → Jan 2; today (May 6) hidden. Active items render at top of drawer section with a Glow-tinted icon. Adds `sparkles` icon to `src/lib/icons.ts`.
  - **`/pricing` page (ground-up build)** — surfaced via NavDrawer Info section (top entry, above Leagues). NOT a homepage snap (the SnapToday concept was prototyped at `/draft-today/` and explicitly rejected after user review — pricing is reference content needing a stable URL with SEO chrome, not a homepage course). Architecture: typography hero ("Walk-in pricing.") → day-tabbed calendar (transparent over page bg, no card chrome — Pin Tilt watermark shows through) → "Holidays follow Friday 5pm pricing" note → SEO copy block (H2 "How pricing works." + 3 keyword-rich paragraphs + NAP) → SnapFooter. 7-pill day toggle; all 7 days pre-rendered server-side with vanilla JS toggle. Single-rate days (Mon-Thu) collapse to 2 columns; multi-rate days (Fri/Sat/Sun) show 3 columns. TODAY pill stacks above day name. Pricing data lives in `src/data/pricing.ts` (4 distinct schedules — note Sun is reversed peak/off-peak). **Specials content collection** (`src/content/specials/*.md`) shipped at the same time — separate from events (recurring/conditional vs dated one-shots). Penny A Pin (Wed, Traditional) is the first entry, renders as Glow-bordered callout above the table; does NOT replace the cell rate (user direction: keep $35 visible, special is referenced above). Two quick-action CTAs (Check the waitlist / Get the coupon) sit between the views and the holiday note — equal-width via `flex: 1 1 200px`. Multiple typography iterations during the session (final state: day name `clamp(40,10vw,60)`, prices `clamp(28,4.5vw,36)`, all column headers 13px / 0.08em with column widths 34/33/33 to fit TRADITIONAL without overflow).
  - **Google Places API graceful fallback** wired in `src/lib/google-hours.ts`. Reads live `regularOpeningHours` from the Business Profile when `GOOGLE_MAPS_API_KEY` + `GOOGLE_PLACE_ID` are set on Vercel; falls back to static `src/data/hours.ts` otherwise. /pricing displays "verify on Google" caveat only during fallback. **Decided to stay on static + verify-link for v1** — hours don't change weekly; the cheap API field doesn't capture holiday hours; no Google billing account friction; the verify link reads as transparent, not evasive. Code stays as a primitive — flip env vars on Vercel anytime to activate.
  - **Maps URL strategy locked** (revised 2026-05-10). All venue-page surfaces (SnapFooter Google review card, `/pricing` "verify on Google", LocalBusiness `hasMap`) now point at `MAPS_VENUE_URL` in `src/lib/schema.ts` — Google's documented canonical `search/?api=1&query=Twisted+Pin&query_place_id=…` pattern. Verified on iOS Safari + Android Chrome to open the Twisted Pin place card with Save/Share/Directions exposed. Replaces the prior `maps.app.goo.gl/yyiVoLzTsHA2TNGW8` short URL (Firebase Dynamic Links — Google sunset Aug 25, 2025) AND the prior `/maps/place/?q=place_id:…` form which was flagged unreliable for venue-page surfaces in earlier testing. SnapFooter Get Directions still uses `/maps/dir/?api=1&destination=…&destination_place_id=…` (a different deep-link — directions, not venue page).
  - **"Kids Bowl Free" stripped completely.** TM'd by another operator AND venue doesn't currently run that program. Removed from /bowl (cross-link card + meta + docstring), /pricing SEO copy, vercel.json (`/free-kids-bowling/` → `/bowl/`, dropped the dead anchor), launch-checklist, media-needs. Future family-bowling page is on the roadmap, name TBD per ops.
  - **NavDrawer auto-close on link click.** Mobile bug: `#find-us` scrolled to footer but left drawer open. Fix: `navDrawer()` in `motion.client.ts` binds a click listener on every `<a href>` inside the drawer that calls `close()`. Critical for in-page anchors; also fires for cross-page + external links (clean state on back nav).
  - **SnapFooter copy:** *"Plainfield's been talking."* → *"Everyone's been talking."* Local SEO comes from citations / schema / NAP, not body-copy geo mentions. Broader phrasing matches actual review distribution.
  - **Adobe Fonts kit declined.** User opted out of the CC subscription cost. Substitutes (Barlow Cond / Montserrat / Roboto Slab) ship for production. Discussed perf trade-offs (would add ~100-300ms LCP penalty on mobile, mitigatable to ~50-150ms). Swap is a 5-minute CSS-variable change if direction changes later.

- **2026-05-07 — Patch API abandoned, /coupon + /free-kids-bowling iframe-wrapped.** Patch Retention's native-form integration shipped 2026-05-05 never resolved the canonical "web form → coupon SMS" trigger pattern with Patch support, so user opted to revert both `/coupon` and the new `/free-kids-bowling` page to the legacy live-site iframe wrappers (`c-g.co/xORo1J` and `c-g.co/OskPxh` respectively). `src/pages/api/coupon-signup.ts` deleted; can be restored from git history if Patch ever clarifies the trigger. **Vercel env vars no longer used:** `PATCH_API_KEY`, `PATCH_ACCOUNT_ID` — safe to remove. Both pages mirror `/waitlist`'s iframe-in-brand-chrome pattern.
- **2026-05-11 — Positioning reframe: adults-first → two-mode time-segmented + logo cleanup + kids FAQ.** Triggered by 9-tester user video pass on the deployed site (parent testers V3 and V5 surfaced "is this for my kids?" friction; V8 and V9 validated the locked thesis works for both parents AND childless friends). Competitive scan (Lucky Strike / Dave & Buster's / Topgolf / Bowlero) showed Twisted Pin is currently *more adult-coded than Topgolf* — defensible, but the positioning gap from the prior "adults-first, group-friendly, family-welcome in that order" formulation was reading grudging rather than co-equal. Reframe: **same room, different night — Saturday at 6 with family, Saturday at 11 without**. Adult-quality is the lever, family-welcome is the permission slip; the 1am close is a positioning asset, not just an ops fact. Three concrete shipped changes: (1) **Header logo swap** — `LogoGBED_Horizontal_White` → `twisted-pin-horizontal-glow` (clean wordmark without GBED tagline). The "GAME · BOWL · EAT · DRINK" tagline was sub-10px and illegible at 41px mobile (V5 explicitly: "subtexts look like blobs") AND it led with GAME, contradicting the bar-led thesis. Drop solves both. Header now uses the same mark as the NavDrawer — unified logo system. (2) **FAQ entry `Can I bring my kids?`** added under Hours & Visiting — canonical long-form answer using bedtime as the dividing line, not age. (3) **Voice.md "Adults-first, group-friendly, family-welcome (in that order)" section renamed** to "Adults-first, family-welcome — segmented by hour, not by audience" with the full lever/permission-slip frame documented. Working Preferences in CLAUDE.md updated to match. **Pending from same session:** homepage permission-slip beat (A/B/C copy options awaiting user pick), `/birthday-parties-booking/` page (preserves legacy slug), NavDrawer Quick Actions strip (replaces current site's CTA-tile pattern in a brand-coherent way), VIP suite photo replacement (V4 flagged current photo reads as regular lanes — kills the differentiator).
- **2026-05-07 — `/free-kids-bowling` shipped.** Reuses the legacy slug from twistedpin.com (preserves any inbound SEO and removes the prior `/free-kids-bowling/` → `/bowl/` 308 redirect from `vercel.json`). The 2026-05-06 "Kids Bowl Free stripped completely" decision is partially superseded — venue is now running the program (June 1-30 summer window), so the page exists. The TM constraint stands ("Kids Bowl Free" is still owned by another operator); the page uses **"Free Summer Bowling For Kids"** / **"Free Kids Bowling"** as the descriptive H1 + label, which doesn't infringe. Architecture: hero → iframe (signup) → editorial sections (How It Works + While The Kids Bowl) → Glow-bordered Summer Pin Pass upsell ($159.95 unlimited household, links to `ecom.roller.app/twistedpin/summerpinpass`) → SEO body block (hyper-local: Naperville, Romeoville, Shorewood, Bolingbrook, Oswego, Joliet) → SnapFooter. Surfaces in NavDrawer Visit section between Apr 15 → Sep 1 via the seasonal-nav system (same mechanic as `/new-years-eve`). Cross-link card on `/bowl` restored. Voice-tightened version of the user-supplied existing copy (dropped "premier entertainment destination" / "ultimate summer value" boilerplate, kept hyper-local SEO mentions, reframed via the brand thesis — "while the kids bowl, you get the bar program"). **Future thread (parked):** homepage "nudge" pattern for highlighting time-limited programs (Pin Pass, summer kids, NYE) without dedicating a giant CTA — exploration deferred until we have 3+ items competing for attention.

- **2026-05-17 — Brian "Curated by" reframe + chef-language ban (ads-launch hygiene).** Triggered by [Context/session-handoffs/2026-05-17-google-ads-website-needs.md](Context/session-handoffs/2026-05-17-google-ads-website-needs.md): Google Ads policy plus truth-of-relationship both fail under the old "Built by" framing. Brian Van Flandern is a CONSULTANT who **curated** the cocktail program — not staff, not owner, never on site. Likewise the kitchen is run by the F&B team, not a named chef; "chef-driven" / "chef-inspired" is a deceptive claim. Three coordinated sweeps shipped: **(1) Verb swap** — "Built by [X]" / "Designed by [X]" → "Curated by [X]" across `/bar`, `/menu/cocktails`, `/eat`, `/free-kids-bowling`, `/faq`, `/birthday-parties-booking`, `/careers`, 6 `/why-us/*-il/` pages, 2 blog posts, 3 code-comment docstrings. **(2) Chef-language removal** — `/eat`, `/free-kids-bowling`, `public/llms.txt`, `Context/seo.md`. Replacement: "from-scratch" or "built to share." **(3) Heyflow/Zite toggle made explicit** — `src/lib/links.ts` already exposed the `PLAN_EVENT_URL` switch (Avery ON/OFF) but the comment claimed Zite was "current default" while the actual export pointed at Heyflow. Comment corrected; CLAUDE.md locked decision updated to reflect that the toggle supersedes the 2026-05-06 Zite-only lock. 3 hardcoded Zite URLs in blog posts + 1 in `llms.txt` flipped to Heyflow (the toggle doesn't auto-rewrite narrative prose). Voice.md Words-to-Avoid section gained two new banned-framings blocks (Brian-relationship, chef-language) so future copy passes don't quietly drift back. The earlier (2026-04-30) Brian memory note that "marketing copy has latitude with 'built by'" is now retired — ads policy makes the looser standard untenable.

- **2026-05-17 — Schema fix + LCP/perf sweep (pre-ads launch).** Major perf bundle (full session in [Context/session-handoffs/2026-05-17-schema-lcp-perf-sweep.md](Context/session-handoffs/2026-05-17-schema-lcp-perf-sweep.md)):
  - **Schema pass (`916cc84`):** Fixed GSC `/events` "Invalid object type for field `<parent_node>`" Review Snippets error. Root cause: `localBusinessBase()` carries `aggregateRating`; Google's Review Snippet policy only allows AR under LocalBusiness/Organization/Product/Service/Event/etc., but `/events` was bare `EventVenue` (Place subtype, not LocalBusiness). Fix: dual `@type: ["EventVenue", "EntertainmentBusiness"]`. Same commit hardened `eventSchema()` location with inline NAP (closes "Missing field 'name' in location" Event-validator footgun) + added Product/Service/Event schemas to 6 pages that had none (`/birthday-parties-booking` $419/$489.90, `/summer-pin-pass` $159.95, `/leagues` Service, `/gift-cards` $25 minPrice placeholder, `/fundraisers` malformed Offer dropped, `/free-kids-bowling` Event with eventSchedule). **GSC → Validate Fix on `/events` after deploy.**
  - **LCP fix pattern (`9980370`):** Spread the `/events` 6.9s → 3.1s pattern to 11 pillar pages. For each first section video: `data-poster` → direct `poster=` + Base.astro `lcpPreloadHref`. For every section video `<source>`: bounded `media="(max-width: 480px)"` / `media="(min-width: 481px)"` so exactly one source matches per viewport tier (no doubled fetches during v.load probing). Also deferred `sectionVideo()` to window.load via motion.client.ts so initial paint doesn't compete with video probing.
  - **AVIF poster sweep (`00f3905`):** Re-encoded every section-video poster from WebP Q65 → AVIF Q50. Total savings 367 KB across pillar pages. Arcade-poster specifically went to 720w (the only one) because its content compresses 3-5× worse than other posters at 1080w — went 209 KB WebP → 79 KB AVIF (720w). Updated every `poster=` and `data-poster=` reference. Browser support 95%+ globally; older browsers see black poster frame for ~200-500ms before video paints.
  - **Vercel cache headers (`4c4e3c9`):** Extended immutable 1-year cache to `/snap/*`, `/og/*`, `/favicon-*`, `/apple-touch-icon.png`. Was only `/hero/*` and `/_astro/*` before. PSI was flagging 146 KiB per page.
  - **Vite build target ES2020 (`4c4e3c9`):** Drops polyfills for optional chaining, nullish coalescing, async/await. ~14 KiB legacy-JS saved per page. ES2020 has 99%+ mobile browser support.
  - **Hero LCP preload gating (`47e7479`):** Caught after AVIF sweep that Base.astro was emitting hero-poster.webp (104 KB) + hero-desktop-poster.webp (176 KB) preloads on EVERY page — but only homepage displays the hero. The wasted preload was competing with each page's actual LCP element. Symptom: `/vip-suite` (43 KB poster) scored LCP 2.9s while `/bowl` (24 KB poster) scored 5.2s — SMALLER poster running slower because of preload-queue contention. Fix: `{Astro.url.pathname === '/' && ...}` gate. Pillar pages now emit exactly ONE image preload (their own AVIF poster).
  - **Confirmed wins (PSI mobile, all post-`47e7479`):** `/events` 6.9s → 3.1s LCP (perf 72 → 92). `/game` 7.5s → 3.2s LCP (perf 72 → 90). `/bowl` → 2.5s LCP (perf 97 — green band). `/vip-suite` 4.0s → 2.9s LCP (perf 88 → 94 — green band). `/fundraisers` 4.7s → 3.5s LCP (perf 82 → 86). `/birthday-parties-booking` 3.5s LCP (perf 88). The variance theory that kept us second-guessing PSI mid-session was wrong — wasn't run noise, was the wasted hero preload starving connection slots on pillar pages.
  - **Still flagged for future passes:** Reduce unused JS (~60 KiB GTM — Partytown territory), forced reflow on `/game`, homepage hero AVIF (currently WebP, not blocking since homepage scores 100/954ms LCP), `/menu/taps` iframe-related slowness, `/pricing` 3.6s LCP (no section video — separate investigation).
  - **The new "LCP fix pattern" for any future page** that has typography hero → first section video is documented in the handoff doc Step-by-step section. AVIF Q50 poster + bounded media + `lcpPreloadHref`.

- **2026-05-17 — Site cutover to twistedpin.com.** New build replaced the legacy site on the production domain. Operational implications: (1) `vercel.json` redirect map is now load-bearing for real traffic — any legacy URL hitting prod relies on those 18 entries firing correctly, plus the `/essential` / `/elevated` SMS short links. (2) GSC actions (Validate Fix on `/events` Review Snippets, submit `/sitemap-videos.xml`) now affect real indexing — earlier sooner than later. (3) Copy / schema / perf changes ship straight to production from each push to main — no staging cushion. (4) Pre-launch framing in this file has been retired; launch-checklist.md is now historical. Treat any "pre-launch" or "before the new build replaces twistedpin.com" language anywhere else as stale and rewrite when you see it.

- **2026-05-18 — Audience-funnel event pages shipped + Stage 6 superseded.** 4 new pillar pages built parallel to `/events`, each capturing a distinct audience-intent search cluster: `/corporate-events/` (corp planners — `corporate event venue near me` / `team building venue`), `/holiday-parties/` (seasonal Sep 1→Jan 5 — `holiday party venue near me` / `Christmas party spots`), `/showers/` (bridal + baby — female-planner audience), `/wedding-receptions/` (intimate weddings 25-80 + rehearsal dinners). All four follow the same pillar skeleton + LCP recipe established for `/events` and `/vip-suite` (typography hero, vip-lanes-* LCP-direct, buffet-* lazy below fold, bounded `<source media>`, AVIF Q50 posters, Service schema with 8-city `areaServed`). Both reused videos remain canonical on their primary pages per `src/data/videos.ts` — no `VideoObject` duplication. 4 legacy 301s in `vercel.json` retargeted: `/corporate-parties/`, `/group-and-company-events-twisted-pin/`, `/event-spaces-for-teams-*` all now → `/corporate-events/` (were dropping into `/events/#corporate` where hash strips on 301), `/ultimate-holiday-party-venue-*` → `/holiday-parties/`. NavDrawer Visit section gained 4 entries with new icons (building-2 / tree-pine / flower / wine). 11 `/why-us/*-il/` city pages cross-linked via prose. **Stage 6 from the 2026-05-17 punch list (which planned `/events/{type}/` nested URLs for `/events/corporate`, `/events/birthdays`, `/events/holiday-parties`) is now SUPERSEDED** — top-level URLs win for organic SEO (`corporate-events` keyword-matches the search phrase more directly than `events/corporate` does), and the legacy 301s now point at the top-level URLs. If marketing later needs distinct stripped-down ads landing pages at `/events/{type}/`, that's a deliberate "Option B" build on top of the existing top-level pages — kept open as a follow-up rather than a planned stage. **`/wedding-receptions/` launches without explicit ops sign-off** that the venue takes weddings — copy is honest about what we offer (room/catering/bar/AV) without over-promising on packages or planners; reverts cleanly if ops kills it.

- **2026-06-12 — Blog + llms.txt event links route through `/events/`; never hardcode a platform URL in prose.** A 2026-06-11 Heyflow lead arrived while the `PLAN_EVENT_URL` toggle was on Zite (Avery ON) — diagnosed via the `_gl` GA4 cross-domain linker param that Heyflow captures as a hidden form field (decoded: Google Ads click June 10, submission was the visitor's 4th session, June 11 evening). Path was one of the 3 blog posts' hardcoded `event.twistedpin.com/#start` links, which bypassed the toggle. Fix (commit `d675439`): all 5 prose links in `src/content/blog/*.md` + 1 in `public/llms.txt` now point at `/events/`, whose closing CTA always carries the live toggle destination. This supersedes the 2026-05-17 "manually sweep blog/llms.txt to match the toggle" pattern — prose links `/events/` and never needs re-sweeping. Side discovery, useful for the deferred ads-attribution plan (Offline Conversion Import, path B in the cutover runbook): Heyflow's `_gl` hidden-field capture preserves a decodable `gclid` per lead — the import mechanism already exists with zero extra wiring.

- **2026-06-17 — Events on-page SEO: corporate-events use-case reframe + gclid passthrough, FAQPage on event pages, GeoCoordinates, birthday slug flip.** Two handoff specs ([session-handoffs/2026-06-16-corporate-events-usecase-reframe.md](Context/session-handoffs/2026-06-16-corporate-events-usecase-reframe.md) + [session-handoffs/2026-06-16-onpage-seo-spec.md](Context/session-handoffs/2026-06-16-onpage-seo-spec.md)) from the Google Ads "Events" campaign restructure (use-case keyword pivot ahead of the Aug 1 Q4 corporate ramp). Shipped in 4 commits:
  - **`bb9d273` — `/corporate-events/` use-case reframe + site-wide gclid passthrough.** Page leads with the ad-group use-cases (hero H1 *"Not your average team outing"*; text-led 6-card grid: Team/Staff Outings ⭐ · Employee Appreciation · Team Building · Company & Holiday Parties · Meetings & Luncheons · Fundraisers) echoing live ad creative; VIP-suite + food/bar videos kept below as proof. **New `src/scripts/gclid-passthrough.client.ts`** (wired site-wide in Base.astro): captures `gclid`/`gbraid`/`wbraid`/`gad_source`/`utm_*`/`fbclid` → sessionStorage on landing, appends to every Zite/Heyflow inquiry CTA via the URL API (query before `#hash`). **Additive to the GA4 linker** — the linker alone was landing a clean `gclid` on 0/69 inquiries (buried in `_gcl_aw`, which Zite can't read). LCP: use-case text now sits above the videos, so dropped `lcpPreloadHref` + both section videos lazy `data-poster` (text LCP, 0 image preloads). Title/Service schema widened to "Corporate Events & Team Outings". *Title decision: stays "Team Outings" (lead ad group) over the SEO spec's "Team Building" — paid scent wins the tiebreak; team-building is covered on-page (use-case card + FAQ).*
  - **`43bca24` — Phase A: FAQPage + GeoCoordinates.** `/corporate-events/` gains a FAQPage + on-page FAQ (8 search-phrased Q&As) reusing the global `t2-faq` accordion. `localBusinessBase()` gains `GeoCoordinates` (41.599276, -88.195328 from the GBP place pin; new `GEO_LAT`/`GEO_LNG` constants) — the one schema completeness-pass gap; now on every LocalBusiness-subtype page. *Spec item A4 (multi-type the homepage `BowlingAlley`+`Restaurant`+`BarOrPub`) RETIRED — contradicts the locked one-@type-per-page architecture; those types are already distributed by page. FAQ rich-snippets are gov/health-only since 2023, so the FAQPage value is AI-Overview sourcing + on-page long-tail depth, not SERP star-FAQs.*
  - **`11d2567` + `c1c7921` — Phase B: birthday slug flip + FAQ + occasions.** Renamed `birthday-parties-booking.astro` → `birthday-parties.astro`; **`/birthday-parties-booking/` now 301s → `/birthday-parties/`** (was the reverse — the clean keyword URL redirected to the cruft slug). Mirrors the `/corporate-parties/` → `/corporate-events/` consolidation. Added FAQPage (7 Q&As) + an "Occasions" section (Milestone 30/40/50 · Bachelorette & Bachelor · Sweet 16 & Teen); broadened title/meta to kids + adult/milestone; internal links repointed (`/events` ×2, `/vip-suite`, NavDrawer ×2); Product+Offer ItemList + LCP poster + kids-package CTAs unchanged. **`11d2567` was a rename-only commit** (a `git add` aborted on the removed old pathspec and silently staged nothing else); `c1c7921` landed the content + the flipped redirect — without it `/birthday-parties/` would 301 to the removed slug (404). Verified in the build: 0 stale `/birthday-parties-booking/` refs anywhere, canonical is `/birthday-parties/`, redirect flipped. **The birthday canonical is now `/birthday-parties/`** — any `/birthday-parties-booking/` references earlier in this file are historical. **Ads side (other session, NOT done in-repo):** repoint the Birthday ad-group final URL → `/birthday-parties/` and the Corporate Parties sitelink → `/corporate-events/`.

- **2026-06-17 — Booking-flow UX + type/legibility + conversion-tracking levers.** Session on the live `/reserve*` flow, `/corporate-events`, and the events calendar. Durable levers + reasoning captured in [session-handoffs/2026-06-17-booking-ux-type-conversion-levers.md](Context/session-handoffs/2026-06-17-booking-ux-type-conversion-levers.md) (read the "Reusable levers" section — this is the track record for the ongoing optimization effort). Highlights:
  - **iOS input zoom (`dd7285c`)** — 16px inputs aren't enough when a cross-origin iframe (Stripe PaymentElement) is on the page; the auto-zoom is a *top-level viewport* action, so `Base.astro` `lockViewportZoom` caps `maximum-scale=1` on the 3 booking pages only (iOS still allows manual pinch — NOT the `user-scalable=no` a11y regression).
  - **GA4→Ads `purchase` tracking restored (`a3d25bb`)** — died at the Roller→`/reserve` cutover. `purchase` event in `ConfirmationStep.tsx` (txn=invoice, value, currency; deduped) riding the Base.astro gtag stub (GA4 `G-R64WB0Y4VW` + Ads `AW-961151619`). **OPEN / not verifiable from repo: confirm GA4 marks `purchase` a Key Event + GA4→Ads import is on** — the *firing* is in code, the *counting* is a dashboard step. Client-side = lossy; truth is Stripe/TPRS.
  - **Mobile type-size rubric (`63017cf`, `9e6578d`)** — consensus 16/14/12 px (primary / secondary / floor; Lighthouse fails <12px). Bump ONLY read-to-decide/consent text + sub-floor offenders; never bloat the dense cart (13–14px line items are convention). Inputs always ≥16px.
  - **Display-Black blurs at 1× desktop (`1bd44be`)** — fine on high-DPI mobile, soft on 1× monitors. It's a DPI signature, not a size bug: step small sentence-case *display* headlines down to a cleaner face on desktop (`.corp-uc-headline` → Montserrat 700 `@media ≥1025px`), keep condensed on mobile + big uppercase headers. We only load Barlow Condensed 900, so there's no lighter condensed fallback.
  - **Pre-sale notice (`f2f5f8c`)** — `presaleNotice` on `BookingPageConfig`: self-resolving "coming soon" beat for a not-yet-on-sale page (NYE), no date hardcoded in the frontend; disappears when the engine opens sales. Annual ops: `src/tprs/NYE-ANNUAL-PLAYBOOK.md`.
  - **Events (`8bfdbb0`, `af5ae11`)** — Paint Night (Jul 21, 6–8:30pm) is the first real `/upcoming-events` entry. Long external booking URLs get a `/slug` 302 short link in `vercel.json` (`/paint-night`) — one clean, updatable link for the CTA + FB/SMS/print. `AggregateOffer` with `lowPrice == highPrice` clears GSC offer warnings; the 60-day lookahead is why NYE doesn't show until ~Nov.
  - Also: 2-up fill-the-frame duration tiles (`c89519d`), add-on row polish (`9e6578d`).

- **2026-07-04 — Google-hours midnight-spillover artifact erased the July 4 closure; Roy announced phantom hours (`ca625fd`).** Ops had July 4 correctly marked closed in Google Business Profile (the Jul 3 snapshot showed Sat: Closed), but Google's `currentOpeningHours` rolling window starts at midnight *today* — so this morning's 4am cron saw Friday night's past-midnight tail (11am–1am close) reported as a truncated period "Sat 12am–1am." Normally the day's real period overwrites that tail; on a specially-closed day it's the ONLY period, so the snapshot recorded the holiday as open 12am–1am and Roy read it to a caller (call_dfe08ad8f5f2e9d0b96fa307547). This is the leading-edge mirror of the trailing-edge 23:59 clip `mergeHours()` already repaired. Fix: `stripLeadingSpillover()` in `src/lib/google-hours.ts` drops a 12am-opening same-day period when a previous-weekday period wraps past midnight to cover it (8-case logic test incl. genuine-midnight-open guard); same commit hand-corrected the committed snapshot. Same-day mitigation: self-expiring 2026-07-04 override in the n8n `Roy — Pre-Call Dynamic Variables` code node (closed all day, "back tomorrow at noon" greeting, promo suppressed) — date-gated, inert after Jul 4, delete at next touch. Recurrence class: only holiday closures landing on Sat/Sun (days following a past-midnight close). Chain of record: Roy ← n8n `roy-pre-call` ← `twistedpin.com/api/hours/` ← daily 4am Places snapshot — Roy never calls Google directly.

- **2026-07-18 — Avery's number gained an inbound-call path; it never had one (`3c1aab2`).** Calling **779-303-0261** returned a carrier "call cannot be completed" message. Not a regression — the **Inbound Call Settings → Assigned Resource** slot on the SignalWire number ("TP Avery") had simply always been empty, so calls were rejected at the edge. A voicemail-style greeting had been intended months earlier but was never wired. SMS was never affected: **Inbound Message Settings** points at `https://callback.missiveapp.com/signalwire` and is a wholly separate path — *don't touch it when working on voice.* Fix: a cXML Script resource ("Avery Call", Call Type = Calling, Handle Using = Hosted Script) that plays a greeting and hangs up, steering callers to text instead — `<Play>https://www.twistedpin.com/audio/avery-greeting.mp3</Play><Hangup/>`. **SignalWire hosts no media** — both the cXML `<Play>` verb and the Call Flow Builder audio node fetch a configured HTTPS URL at call time, with no upload option — so the greeting lives in this repo at `public/audio/avery-greeting.mp3` and is served by Vercel. Changing the greeting is now file-replace + push; no SignalWire change, since the Script points at the URL not the file. **Ordering trap:** a failed `<Play>` fetch falls through silently to `<Hangup/>`, so assigning the resource before the URL is live gives callers dead air + instant disconnect — which reads worse than the original error (a business hanging up vs. a wrong number). Always confirm `200 audio/mpeg` before assigning, and use the `www` host (the apex 308s to `www`, so `www` avoids making `<Play>` follow a hop). Low-bitrate mono is *correct* for this file — telephony is 8 kHz narrowband; don't "upgrade" it. Verified on a real inbound call. Same commit added a `desktop.ini` ignore rule so Explorer metadata is never served out of `public/`. Memory: [[avery-phone-voice-path]].

- **2026-07-20 — `/playbook` shipped; two silent-failure bugs caught only by an end-to-end test.** Both returned HTTP 200 to the client while doing nothing, which is why "the endpoint succeeded" was never sufficient evidence. **(1) Table grants.** `playbook_acknowledgments` was created through the Supabase MCP `apply_migration`, which does NOT apply Supabase's default role grants — every insert failed `42501 permission denied`. Easy to misdiagnose because `service_role` bypasses RLS, so the instinct is to go looking at policies; RLS and table-level GRANTs are independent. Fixed with `grant insert, select … to service_role`, plus `revoke all … from anon, authenticated` because those roles retained `TRUNCATE` and **RLS does not gate TRUNCATE**. **(2) Resend sender domain.** Only `mail.twistedpin.com` is verified on the Resend account — the apex `twistedpin.com` is not, and Resend matches the domain exactly. Sending from `noreply@twistedpin.com` (then `bookings@twistedpin.com`) returned 403 and, because notification failure is deliberately non-fatal, produced signatures that saved correctly while info@ got nothing — precisely the failure the email exists to prevent. Now sends from `playbook@mail.twistedpin.com`. **TPRS checked and CLEAR** (confirmed same day from a real guest thread) — it sends from `reservations@mail.twistedpin.com`; the `bookings@twistedpin.com` hits in that repo are test fixtures, a stale comment, and one fallback. **But that fallback is a live landmine: `tprs apps/backend/src/workers/main.ts:198` reads `options.emailFromAddress ?? "bookings@twistedpin.com"` — if the from-address env var is ever unset or renamed, every booking confirmation silently stops delivering to paying guests.** Should default to the `mail.` subdomain or throw on startup; not yet done. Sequence: `49680cb` (build) → `48e6ebd` (sender fix) → `a68277d` (docs).

- **2026-07-18 — Extra Suite Birthday was $20 over the live catalog on `/birthday-parties/` (`f9b7ffc`).** The TPRS booking catalog at `/reserve/birthdays/` sells Extra Suite Birthday at **$469.90**; this page said **$489.90** in body copy, the FAQ answer, and the schema.org `Offer` that Google reads. Surfaced by the Avery KB audit — the KB had the correct figure and the marketing page was the stale one, which is the opposite of the assumption the audit started with. **Source of truth for kid-package pricing is the live booking catalog, not this page.** Root cause was two independent literals: the card display hardcoded `$489.90` while the schema read `EXTRA_SUITE_BIRTHDAY_PRICE`, so they could drift silently. The card now renders from the constant, making that class of drift impossible; the constant's docstring points at the catalog. `SUITE_BIRTHDAY_PRICE` ($419) was correct on both and is deliberately still hardcoded in its card (constant is `"419.00"`, display is `$419`) — do not "fix" that to match without changing the display format.

- **2026-07-25 — Avery migrated to Claude Opus 5. Same price, and the existing `thinking: disabled` line made it a clean swap.** Opus 4.8 is now a legacy model; `claude-opus-5` is **$5 / $25 per MTok — identical to 4.8** — with a May 2026 knowledge cutoff (vs Jan 2026) and the same tokenizer, so `max_tokens` needed no re-baselining. Five literals changed: WF1 `Build Claude Request` + `Build Retry Request`; WF2 `Build Avery Request` + `Build Retry Request` + the orphaned `Build Retry 2 Request`. The five HTTP nodes inherit the body via passthrough and needed no edit. **The four Sonnet fallbacks stay on `claude-sonnet-5`** — they exist for Anthropic API outages (529s, added after the 2026-07-08 incident), not for cost or refusals. `claude-opus-5` and `claude-sonnet-5` were added to all three PRICING tables in the Metrics Aggregator. **Why it was safe:** Opus 5's most dangerous silent change is that *omitting* `thinking` now runs adaptive thinking (on 4.8 it ran without), which would blow small `max_tokens` budgets. Avery was immune by accident — the Build nodes set `thinking: {type:'disabled'}` **explicitly**, a guard added 2026-07-21 after `claude-sonnet-5` consumed an entire budget on a thinking block and returned zero text (WF1 exec 8701). **NEW TRIPWIRE: on Opus 5, `thinking: {type:'disabled'}` is legal only at effort `high` or below — pairing it with `effort: xhigh` or `max` returns a 400.** Nothing sets `effort` today so the API default (`high`) applies; do NOT add an `effort` line to those Build nodes without removing the thinking line in the same edit. **Known-inert:** the `claude-sonnet-5` PRICING entry will not bind, because `Metrics: Initial` reads the model off the *primary* node, which is `null` on a fallback run — fallback traffic still costs as Opus in reporting until that node also reads the fallback's model. **Method note:** the model catalog bundled in the `claude-api` skill goes stale (it was cached 2026-06-24 and had no Opus 5 in it). When a model is said to have shipped, WebFetch `platform.claude.com/docs/en/about-claude/models/overview.md` and the migration guide rather than restating the cache.

- **2026-07-25 — VIP lanes sell in PAIRS (2 / 4 / 6). "We'd add a lane" describes a product we do not sell.** Surfaced by Shanna reviewing a live conversation (Genesabel Javier, 7/24), where Avery told a guest *"If your group grows past that, we'd add a lane… so no rush to lock it now."* Both halves wrong. The pairs rule was **already stated in the KB six times**, including a FAQ titled *"Why not 3 or 5 VIP lanes?"* — so this was not a knowledge gap but two reachability failures. **(1)** Every statement of the rule lived in *describe-the-suite* or *refuse-a-downgrade* contexts. The one passage that owns growth (§7 *Expansion question*) covered growth **within** the current cap and growth past 48, but had no branch for **crossing a cap**. Added: crossing a cap steps to the next pair and changes the quote. **(2)** §12 listed `headcount adjustments` alongside beer cards and platters as symmetric "small changes" adjustable up to 6 days out, **and handed Avery a quotable script** (*"no rush to decide today"*), while the asymmetry rule 40 words later had none. The scripted rule won — Avery's bad line was a near-verbatim lift. Headcount is now pulled out of that bucket with its own script: sizing DOWN is easy up to finalization, sizing UP may not be possible, and growth past a cap needs a whole extra pair. Also closed a drift where the WF2 `Build Avery Request` prompt's VIP descriptor omitted "sold in pairs of lanes" that the KB version carries. **The deadline is 6 days, not "a week"** — stated in 8+ KB locations; the looser phrasing in the original report was confirmed with Jon before editing. **Two durable lessons:** a rule that ships a customer-facing script beats a rule that does not, regardless of which is correct — so fix the script, not the framing bullet. And because the KB is a live-fetched Google Doc, the correction propagated into the *in-flight* conversation within two turns: Avery regenerated her own thread summary from "adds a lane" to "steps to the next lane pair" without intervention.
